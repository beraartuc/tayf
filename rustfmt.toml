edition = "2021"
max_width = 100
use_small_heuristics = "Max"
newline_style = "Unix"

# The following options are nightly-only as of rustfmt 1.x. To apply them,
# contributors can run `cargo +nightly fmt`. Leaving them commented prevents
# spurious warnings on stable. Re-enable here when these stabilize.
# imports_granularity = "Crate"
# group_imports = "StdExternalCrate"
