# tayf v0.6 — Span-Emitting Preview Pipeline + Config TUI Editing Core

**Tarih:** 2026-05-28
**Revision:** rev2 (post-parallel-review absorpsion)
**Yazar:** Bera (solo author) + Claude (Opus 4.7, 1M context)
**Önceki sürüm:** v0.5.7 @ `08cab92` (636 lib tests, CI yeşil)
**Bu sürümün raison d'être'i:** v0.6 iki birbirine kenetli foundation iyileştirmesini tek **BIG FULL ceremony** cycle'da bundle eder:

1. **Span-emitting preview pipeline** — `src/pipeline.rs:apply_rules` bugün yalnızca SGR byte-emit yapıyor; Config TUI'nin live preview surface'i ratatui `Span` listesi istiyor. v0.5.4'te (`spec §5.4`) sıkıntı `DOKUNULMAZ src/pipeline.rs` touch authorization'ı ertelenerek `widgets/preview.rs:7-10` "true colorized preview lands in v0.6+" forward-pointer'a dönüştürüldü.
2. **Config TUI editing core** — v0.5.4 TUI v1 read-only + ColorPicker + SaveDiff iskeleti ship etmişti; v0.5.5 reconcile + atomic-write bitirdi. Editor surface (ColorPicker → rule binding, `n` new-pattern modal, `e` inline regex source modal, Help modal, pending-edit recompile path) v0.6'ya forward-point edildi.

Bu iki scope'un coupling'i HIGH (memory `feedback_lean_process_small_subversions` bundle kararı): editor preview olmadan kör (CLAUDE.md §4 "no half-features").

**Toplam tahmin (rev2 güncellenmiş, TestBackend harness + structured PendingEdits fold dahil):** ~1300-1700 LOC source + tests. Beklenen final test count: **~676-686 lib tests** + ~3 integration suite expansion.

**Rev2 değişiklik özeti (paralel review absorpsion):**
- §2.3 DOKUNULMAZ `src/rules.rs` touch explicit authorize (yeni `compile_from_config` pub(crate) entry-point)
- §5 PreviewState gerçek alanlar; Style::to_ratatui sadece 4 bool axis (bold/italic/underline/dim); boot wiring `Compiled::load_with_theme` + `Arc<ArcSwap<Compiled>>`
- §6.5 Debouncer mevcut ms200 + `mark_edit()`/`should_recompile()` (ms300/`tickle()` yanlıştı)
- §7.1 Modal enum mevcut shipped variants korunur (Confirm/Error/QuitWithUnsavedEdits/ColorPicker/SaveDiff/FullPreview/Search/SampleSet) + 3 yeni (NewPattern/EditRegex/Help)
- §8 PendingEdits **structured-model EXTEND, REPLACE değil** — mevcut shipped `{ general, rules: HashMap<RuleId, RuleEdit>, added: Vec<NewRule>, deleted: HashSet<RuleId> }` korunur; v0.6 keystroke handler'lar bu modele yazar
- §8.3 compile_pending(snapshot, edits) — `app.snapshot.parsed.rules` source-of-truth; tempfile veya in-memory Config synthesize + `rules::compile_from_config` new pub(crate) entry-point (DOKUNULMAZ §2.3 authorize)
- §11 forward-pointer table expand (7+ unlisted site)
- §6.4 HELP_MODAL_CONTENT canonical keystroke list (events.rs grep ile cross-checked)
- §9 ReDoS guard test (`RegexBuilder::size_limit`); TestBackend harness sub-task §12 Group 2
- §13 Rust + TUI reviewer findings fold-or-defer disposition tablosu (post-review fill)

---

## 1. Önceki sürüm review-bulgu absorpsiyonu (zorunlu §1)

Memory `feedback_consume_prior_review`: yeni sub-version spec'i, önceki final cross-cutting review'unun recommendation'larını açıkça fold-or-defer eder. v0.5.7 doc-only cleanup ship döngüsüydü; final cross-cutting review yapılmadı (1-commit, lean spirit). v0.5.6 final F1 review (`docs/superpowers/reviews/2026-05-27-rust-senior-v0.5.6-final-cross-cutting-review.md`) recommendation'ları:

| Kaynak | Önerilen iş | v0.6 disposition |
|---|---|---|
| §13 BLOCKER (6 fixes) | `13 → 12` doc drift cluster | **OUT-OF-SCOPE** — landed @ `068b12b` pre-v0.5.6 tag |
| §8 NIT | `profiles.rs:799-808` stale ARN/ipv6 comment | **OUT-OF-SCOPE** — landed @ v0.5.7 `08cab92` |
| §1 NIT | `pipeline.rs:70-73` "first-match-wins" stale comment | **OUT-OF-SCOPE** — landed @ v0.5.7 `08cab92` |
| §10 NIT | FILENAME_EXTENSIONS code-side doc-comment claim | **DROP** — F1 claim incorrect, doc-comment was added @ v0.5.6 `84a7c3d` (rules.rs:131-145) |
| §13 NIT | CHANGELOG v0.5.6 "Fixed" first bullet wording tightening | **DROP** — acceptable as-is, lean spirit |
| FP audit C-4 | filename single-letter prose collision | **DEFER → v0.7+** — behavior-change risk (`make.o` regression), needs Go profile context |
| FP audit C-8 | filename ↔ fqdn Go pkg paths | **DEFER → v0.7** — needs `go` profile |
| FP audit C-9 | fqdn JWT user-config recipe → built-in | **DEFER → v0.7+** — user-config workaround sufficient |
| v0.5.5 §11 carryover #5 | multi-profile composition | **DEFER → v0.7** |
| v0.5.5 §11 carryover #8 | render snapshot tests | **DEFER → v0.7** (with audit follow-up tests bundle) |
| v0.5.4 §5.4 forward-pointer | span-emitting preview pipeline | **FOLD** — bu spec §3 + §4'ün kalbi |
| v0.5.5 §9 forward-pointer | editor TUI surface | **FOLD** — bu spec §6 |

Hiçbir önceki review item silent omission yok. v0.7 carryover'lar bu spec §14.2'de re-enumerate edilir.

---

## 2. Scope

### 2.1 In-scope (FOLD)

**A. Preview pipeline foundation (`src/pipeline.rs` DOKUNULMAZ touch authorized):**

- **A1.** `select_runs` helper extract: `apply_rules`'un adım 1-4'ünü (pre-filter → priority sort → captures/find iter → overlap reject → final start-sort) ayırıp `&mut PipelineScratch` üzerinde mutate eden, `scratch.runs`'a slice borrow döndüren helper'a refactor. Mevcut `apply_rules` byte-emit kısmı (adım 5) `select_runs` çağrısı + cursor walk olarak kalır. **Davranış byte-identical** (golden parity tests gate).
- **A2.** `apply_rules_spans` parallel API: `select_runs` reuse, owned `Vec<StyleSpan>` döner. Capture-group emit zaten `scratch.runs` üzerinden merged geldiği için ek mantık yok.
- **A3.** `StyleSpan` pub(crate) struct: `start: usize` (byte offset, inclusive), `end: usize` (byte offset, exclusive), `style: Style`. `Copy + Clone + Debug + PartialEq + Eq`.

**B. TUI preview wiring (`src/config_tui/` + `widgets/preview.rs`):**

- **B1.** `app.rs PreviewState` struct genişletme: `compiled` field type `Option<Arc<Compiled>>` → `Arc<ArcSwap<Compiled>>` non-Option; yeni `runs: Vec<Vec<StyleSpan>>`; yeni `scratch: PipelineScratch`. Stale `// reason: v0.5.5+ ...` annotations strip (memory `feedback_stale_dead_code_reason_drift`).
- **B2.** `events.rs:369` `recompile_preview` stub → real impl: sample line'ları üzerinde `apply_rules_spans` çağrısı; runs vec doldurma; `compile_error` clear/set.
- **B3.** `widgets/preview.rs:37-47` `colorize_sample` stub → span-aware render: `spans_to_line(&str, &[StyleSpan]) -> Line<'_>` cursor walk helper.
- **B4.** Initial-compute on TUI startup: `mod.rs run()` boot path'inde compiled boot + ilk `recompile_preview(&mut app)` çağrısı.
- **B5.** Stale forward-pointer strip — §11.

**C. Editor state machine — extend existing PendingEdits (no API break):**

- **C1.** `PendingEdits` mevcut shipped struct (`src/config_tui/edit.rs:104-109`) DEĞİŞMEZ: `{ general: GeneralEdits, rules: HashMap<RuleId, RuleEdit>, added: Vec<NewRule>, deleted: HashSet<RuleId> }`. v0.6 keystroke handler'lar bu modele yazar (§8.1 detay). Yeni `PendingEdit` enum / flat `Vec<PendingEdit>` proposal'ı **REJECTED** (TUI reviewer B1) — shipped data model + reconcile.rs + save.rs + 20+ call site break ederdi.
- **C2.** `src/config_tui/compile_pending.rs` yeni modül: `pub(crate) fn compile_pending(snapshot: &ConfigSnapshot, edits: &PendingEdits, theme: Option<&str>, profile: Option<&str>) -> Result<Compiled, CompileError>`. Workflow: `app.snapshot.parsed.rules: Vec<UserRule>` source-of-truth; edits applied (added + rules[].pattern/styles + deleted); synthetic `Config` build + `rules::compile_from_config` (yeni pub(crate) entry-point, §2.3 DOKUNULMAZ rules.rs touch authorize) invoke.

**D. Editor modals (`src/config_tui/app.rs Modal` enum extend + `events.rs` dispatch + widgets):**

- **D1.** ColorPicker → rule binding: [events.rs:84](src/config_tui/events.rs#L84) "color accepted" Toast::warn → `edits.rules.entry(rule_id).or_default().styles.insert(StyleKey::Default, new_style)` + `apply_pending_and_recompile` invoke.
- **D2.** `Modal::NewPattern { phase: NewPatternPhase, draft: PatternDraft }` nested-state modal: `phase ∈ {Name, Regex, Style}`. `n` keystroke initiate. Regex validate via `Regex::new` (with size_limit guard) inline error. Style sub-flow ColorPicker inline (PatternDraft owns picker_state: ColorPickerState). Commit → `edits.added.push(NewRule { name, pattern, style })`.
- **D3.** `Modal::EditRegex { rule_id: RuleId, buffer: String, error: Option<String> }`: `e` keystroke (rule selected). Buffer init = source pattern from snapshot.parsed.rules veya BUILTIN_NAMES lookup. Debouncer-driven (`mark_edit` per keystroke; `should_recompile` quiescent window'da) validate. Commit → `edits.rules.entry(rule_id).or_default().pattern = Some(new_pattern)`.
- **D4.** `Modal::Help`: `?` veya `F1`. Static const content (keystroke list); Esc dismiss; herhangi tuş Help-dismiss + **discarded** (vim/less convention, TUI reviewer I6).

**E. Pending-edit recompile path (`events.rs`):**

- **E1.** Modal commit veya ColorPicker accept → `compile_pending` invoke → `app.preview.compiled.store(Arc::new(new_compiled))` ArcSwap replace → `recompile_preview` rerun.
- **E2.** Debouncer wire (`debounce.rs` mevcut API): `Modal::EditRegex` buffer mutation'da `app.preview.debouncer.mark_edit()`; events main loop tick `app.preview.debouncer.should_recompile()` quiescent window'da `apply_pending_and_recompile` invoke.

### 2.2 Out-of-scope

- Multi-profile composition (v0.5.5 §11 carryover #5) → v0.7
- Render snapshot tests (v0.5.5 §11 carryover #8) → v0.7
- http opt-in profile (v0.5.6 CHANGELOG forward-pointer) → v0.7
- Audit follow-up tests (μs Latin-1, URL trim, ipv4 5-segment, FP C-4/C-8/C-9) → v0.7
- `Shift+D` first-run init dump, `V` alias, search filter list-side, save-diff `↑`/`↓` scroll, `apply_confirm DiscardEditsAndReload + InitFromDump` real impl, bool-axes-clear tri-state → v0.6.1 (TUI polish lean cycle)
- `d`/Delete keystroke (rule remove) → v0.6.1 (uses existing `edits.deleted` field)
- `reconcile.rs` file split (currently 871 LOC) → v0.7.x if grown >1000
- `Ctrl+R` reload binding (TUI reviewer B5: currently not bound; defer to v0.6.1 with apply_confirm scope)

### 2.3 DOKUNULMAZ touch authorization (CLAUDE.md project layout gate)

**`src/pipeline.rs` touch authorized 2026-05-28** for the following byte-bounded surface:

1. Yeni `pub(crate) fn select_runs<'a>(line: &[u8], compiled: &Compiled, scratch: &'a mut PipelineScratch) -> &'a [(usize, usize, Style)]` extract — refactor only, byte-identical output (parity tests gate).
2. Yeni `pub(crate) fn apply_rules_spans(line: &[u8], compiled_handle: &ArcSwap<Compiled>, scratch: &mut PipelineScratch) -> Vec<StyleSpan>` parallel API.
3. Yeni `pub(crate) struct StyleSpan { start, end, style }`.
4. Mevcut `apply_rules`'da adım 1-4'ün `select_runs` çağrısına delegate edilmesi (~5 satır kısaltma; byte output değişmez).

**`src/rules.rs` touch authorized 2026-05-28** for the following byte-bounded surface (Rust reviewer B1 absorpsion):

5. Yeni `pub(crate) fn compile_from_config(config: &Config, theme_name: Option<&str>, profile_name: Option<&str>) -> Result<Compiled>` entry-point. Internal impl: `compile_merged_rules`'a delegate (mevcut module-private fn). Path-based `load_with_theme` ile paralel; in-memory Config'ten compile için TUI tarafından ve gerekirse runtime'ın hot-reload path'i tarafından kullanılabilir. Mevcut tüm public(crate) symbol'ler değişmez; bu sadece additive.

**ZERO touch** for: `src/{pty, io_loop, tty_guard, signals, runtime}.rs`. `src/profiles.rs` touch yok (compile_from_config zaten profile_name'i alır + mevcut machinery'i kullanır; ProfileRule değişmez).

Authorize eden gerekçe (v0.5.6 priority field precedent §2.3 ile aynı şekil):
- Pure-additive pub(crate) entry-point; mevcut signature'lar değişmez.
- Hot path performans etkilenmez (yeni fonksiyon ayrı; mevcut `load_with_theme` aynı kalır).
- Memory `feedback_parallel_call_site_invariant_audit`: compile_from_config + load_with_theme ortak helper'a (`compile_merged_rules`) delegate — single source of truth korunur.

---

## 3. Mimari: select_runs extract + apply_rules_spans parallel

### 3.1 Mevcut `apply_rules` akışı (src/pipeline.rs:54-142)

```
apply_rules(line, compiled_handle, scratch, out):
  snapshot = compiled_handle.load_full()
  compiled = &snapshot

  scratch.{accepted_spans, runs, event_scratch, active_scratch, set_match_scratch}.clear()

  ---- Adım 1: Pre-filter ----
  scratch.set_match_scratch.extend(compiled.set.matches(line).iter())

  ---- Adım 2: Priority sort (v0.5.6) ----
  scratch.set_match_scratch.sort_by(|a, b|
    Reverse(priorities[a]).cmp(&Reverse(priorities[b]))
      .then_with(|| a.cmp(&b)))

  ---- Adım 3: Match collection (per rule, captures or find) ----
  for &i in &scratch.set_match_scratch:
    if compiled.uses_capture_styling[i]:
      for caps in re.captures_iter(line):
        m = caps.get(0).unwrap()
        if overlaps_accepted: continue
        emit_capture_runs(...) → pushes to scratch.runs
        insert_accepted(...)
    else:
      for m in re.find_iter(line):
        if overlaps_accepted: continue
        scratch.runs.push((start, end, style))
        insert_accepted(...)

  ---- Adım 4: Final start-sort ----
  scratch.runs.sort_by_key(|&(s, _, _)| s)

  ---- Adım 5: Byte-emit ----
  cursor = 0
  for &(start, end, style) in &scratch.runs:
    out.write_all(&line[cursor..start])?
    out.write_all(style.to_sgr().as_bytes())?
    out.write_all(&line[start..end])?
    out.write_all(Style::reset_sgr().as_bytes())?
    cursor = end
  out.write_all(&line[cursor..])?
```

### 3.2 v0.6 refactor — adım 1-4 → `select_runs` helper

```rust
/// Collect matched, overlap-rejected, priority-sorted style runs for one
/// line into `scratch.runs`. Steps 1-4 of [`crate::pipeline::apply_rules`].
///
/// Returns a borrow into `scratch.runs` valid until the next mutation of
/// `scratch`. The borrow holds `&mut` exclusivity over `scratch`; caller
/// cannot touch `scratch` until the borrow is dropped. For dual-emit
/// (byte + span) on the same line, materialize spans immediately
/// (`.iter().copied().collect()`) so the borrow ends.
pub(crate) fn select_runs<'a>(
    line: &[u8],
    compiled: &Compiled,
    scratch: &'a mut PipelineScratch,
) -> &'a [(usize, usize, Style)] {
    scratch.accepted_spans.clear();
    scratch.runs.clear();
    scratch.event_scratch.clear();
    scratch.active_scratch.clear();
    scratch.set_match_scratch.clear();

    scratch.set_match_scratch.extend(compiled.set.matches(line).iter());

    {
        use std::cmp::Reverse;
        let priorities = &compiled.priorities;
        scratch.set_match_scratch.sort_by(|&a, &b| {
            Reverse(priorities[a]).cmp(&Reverse(priorities[b]))
                .then_with(|| a.cmp(&b))
        });
    }

    for &i in &scratch.set_match_scratch {
        let re = &compiled.individuals[i];
        if compiled.uses_capture_styling[i] {
            for caps in re.captures_iter(line) {
                let m = caps.get(0).expect("capture 0 is always the full match");
                let (start, end) = (m.start(), m.end());
                if overlaps_accepted(&scratch.accepted_spans, start, end) {
                    continue;
                }
                emit_capture_runs(
                    &caps, start, end,
                    compiled.styles[i],
                    &compiled.group_styles[i],
                    &mut scratch.event_scratch,
                    &mut scratch.active_scratch,
                    &mut scratch.runs,
                );
                insert_accepted(&mut scratch.accepted_spans, start, end);
            }
        } else {
            for m in re.find_iter(line) {
                let (start, end) = (m.start(), m.end());
                if overlaps_accepted(&scratch.accepted_spans, start, end) {
                    continue;
                }
                scratch.runs.push((start, end, compiled.styles[i]));
                insert_accepted(&mut scratch.accepted_spans, start, end);
            }
        }
    }

    scratch.runs.sort_by_key(|&(s, _, _)| s);
    &scratch.runs
}
```

### 3.3 Mevcut `apply_rules` v0.6 sonrası

```rust
pub(crate) fn apply_rules<W: Write>(
    line: &[u8],
    compiled_handle: &ArcSwap<Compiled>,
    scratch: &mut PipelineScratch,
    out: &mut W,
) -> std::io::Result<()> {
    let snapshot: Arc<Compiled> = compiled_handle.load_full();
    let runs = select_runs(line, snapshot.as_ref(), scratch);

    let mut cursor = 0usize;
    for &(start, end, style) in runs {
        out.write_all(&line[cursor..start])?;
        let sgr = style.to_sgr();
        if !sgr.is_empty() {
            out.write_all(sgr.as_bytes())?;
        }
        out.write_all(&line[start..end])?;
        out.write_all(Style::reset_sgr().as_bytes())?;
        cursor = end;
    }
    out.write_all(&line[cursor..])?;
    Ok(())
}
```

**Byte-identical garantisi:** `select_runs` çağrısı adım 1-4'ün literal kopyası; adım 5 değişmez. Golden parity tests bunu gate'ler (§9.1 Test 1).

### 3.4 Yeni `apply_rules_spans`

```rust
/// Span-emitting variant for the Config TUI preview. Same matching +
/// overlap + priority semantics as [`apply_rules`]; returns owned spans
/// instead of writing SGR bytes.
///
/// **Capture-group styling:** rules with `uses_capture_styling[i] = true`
/// produce 1..=N spans per match per v0.3.5 inner-overrides-outer
/// semantics — identical to runtime path via shared [`select_runs`].
///
/// Returns `Vec<StyleSpan>` with byte offsets into `line` (sorted by
/// `start` ASC; non-overlapping). UTF-8 multi-byte boundary safety:
/// when `line` was derived from `&str` (e.g., `sample_input.text.lines()`
/// per `recompile_preview`) and all builtin patterns are Unicode-aware
/// (regex 1.12 default), match boundaries are char boundaries. TUI
/// consumer `spans_to_line` asserts via `is_char_boundary` debug_assert.
///
/// Snapshot Arc drop ordering: `Arc<Compiled>` dropped at function
/// return; spans own `Style` by Copy (verified in `src/style.rs:422`).
/// No dangling reference back to snapshot.
pub(crate) fn apply_rules_spans(
    line: &[u8],
    compiled_handle: &ArcSwap<Compiled>,
    scratch: &mut PipelineScratch,
) -> Vec<StyleSpan> {
    let snapshot: Arc<Compiled> = compiled_handle.load_full();
    let runs = select_runs(line, snapshot.as_ref(), scratch);
    runs.iter()
        .map(|&(start, end, style)| StyleSpan { start, end, style })
        .collect()
}
```

### 3.5 `StyleSpan` veri tipi

```rust
/// Single styled byte range produced by [`apply_rules_spans`].
///
/// `start..end` are byte offsets into the input line (half-open; `end`
/// exclusive). Style is the resolved style for the run — capture-group
/// merging (inner overrides outer) is already applied upstream.
///
/// Non-overlapping invariant: in the returned `Vec<StyleSpan>` from
/// `apply_rules_spans`, no two spans overlap. Sorted by `start` ASC.
#[derive(Copy, Clone, Debug, PartialEq, Eq)]
pub(crate) struct StyleSpan {
    pub start: usize,
    pub end: usize,
    pub style: Style,
}
```

---

## 4. Public API + invariants

### 4.1 pub(crate) surface (yeni)

| Symbol | Location | Stability |
|---|---|---|
| `fn select_runs<'a>(line: &[u8], compiled: &Compiled, scratch: &'a mut PipelineScratch) -> &'a [(usize, usize, Style)]` | `src/pipeline.rs` | pub(crate); internal helper |
| `fn apply_rules_spans(line: &[u8], compiled_handle: &ArcSwap<Compiled>, scratch: &mut PipelineScratch) -> Vec<StyleSpan>` | `src/pipeline.rs` | pub(crate); TUI consumer |
| `struct StyleSpan { pub start, pub end, pub style }` | `src/pipeline.rs` | pub(crate); fields pub(crate) |
| `fn compile_from_config(config: &Config, theme_name: Option<&str>, profile_name: Option<&str>) -> Result<Compiled>` | `src/rules.rs` | pub(crate); additive entry-point (DOKUNULMAZ §2.3 authorize) |
| `fn compile_pending(snapshot: &ConfigSnapshot, edits: &PendingEdits, theme: Option<&str>, profile: Option<&str>) -> Result<Compiled, CompileError>` | `src/config_tui/compile_pending.rs` | pub(crate); editor consumer |
| `enum CompileError { InvalidPattern { rule_name, source }, CompileFailed(Box<rules::Error>) }` | `src/config_tui/compile_pending.rs` | pub(crate); error path |
| `fn Style::to_ratatui(&self) -> ratatui::style::Style` | `src/style.rs` | pub(crate) — actual axes only: fg/bg + bold/italic/underline/dim |

**Public API surface unchanged.** `src/lib.rs` exports'lar değişmez; v0.5.7 `pub fn run` tek public export'tur.

### 4.2 Invariantlar

- **I1 — Byte-identical existing behavior:** `apply_rules` adım 5'i değişmez; refactor sonrası 18+ mevcut pipeline test byte-identical pass eder.
- **I2 — Single source of truth:** `select_runs` matching + overlap + priority logic'in tek implementation'ı. Memory `feedback_parallel_call_site_invariant_audit` — gelecek invariants (yeni priority tier, RegexSet pre-filter genişletmesi) tek yerde değişir; span-emit otomatik miras alır.
- **I3 — Non-overlapping spans:** `apply_rules_spans` çıktısı sorted-by-start + non-overlapping. `accepted_spans` invariant'i bunu zaten gate'liyor.
- **I4 — Capture-group merge invariant:** `uses_capture_styling[i] == true` rule'lar `emit_capture_runs` ile expand olur; inner-overrides-outer (v0.3.5 §1.1) korunur — `select_runs` mevcut path'i değiştirmiyor.
- **I5 — UTF-8 boundary safety (caveat):** Match start/end regex 1.12 contract ile char boundary'sinde **iff** input valid UTF-8 + Unicode-aware patterns (default). TUI source `sample_input.text: String` (valid UTF-8 garantili). `spans_to_line` `debug_assert!(line.is_char_boundary(s.start) && line.is_char_boundary(s.end))` defensive guard. `&str` slice panic on misaligned boundary; debug_assert catch-er.
- **I6 — ArcSwap snapshot lifecycle:** `apply_rules_spans` snapshot içeride yükler (caller'da `Arc<Compiled>` borrow yok). Snapshot Arc function return'da drop; spans own `Style` by Copy; no dangling reference. TUI preview rerun'da fresh snapshot alır.
- **I7 — `respect_existing_colors` preview ignored:** TUI preview path `apply_rules_spans` unconditionally — no ANSI state machine on sample_input. User'ın `respect_existing_colors = true` config'i preview'de görünmez. Pin per §14.1 #5. Test #9 negative case'i bunu pin'ler.
- **I8 — PendingEdits identity by name (RuleId), not index:** Mevcut shipped `HashMap<RuleId, RuleEdit>` model rule_id by name keyed. Sequential edit'lerin stale-index riski yok (RuleId stable).

---

## 5. TUI preview wiring

### 5.1 [src/config_tui/app.rs:155-166](src/config_tui/app.rs#L155-L166) — `PreviewState` struct güncelleme

Mevcut:
```rust
#[derive(Default, Debug)]
pub(crate) struct PreviewState {
    // reason: populated by v0.5.5+ recompile_preview body when the
    // span-emitting preview pipeline (spec §5.4 DOKUNULMAZ blocker)
    // is unblocked; v0.5.4 ships the scaffold field only.
    #[allow(dead_code)]
    pub(crate) compiled: Option<Arc<crate::rules::Compiled>>,
    pub(crate) compile_error: Option<String>,
    pub(crate) debouncer: crate::config_tui::debounce::Debouncer,
}
```

v0.6 sonrası:
```rust
#[derive(Debug)]
pub(crate) struct PreviewState {
    pub(crate) compiled: std::sync::Arc<arc_swap::ArcSwap<crate::rules::Compiled>>,
    pub(crate) compile_error: Option<String>,
    pub(crate) debouncer: crate::config_tui::debounce::Debouncer,
    pub(crate) runs: Vec<Vec<crate::pipeline::StyleSpan>>,
    pub(crate) scratch: crate::pipeline::PipelineScratch,
}
```

Değişiklikler:
- `compiled: Option<Arc<Compiled>>` → `compiled: Arc<ArcSwap<Compiled>>` (non-Option; boot'ta initialize)
- `#[allow(dead_code)]` strip (artık populated)
- Yeni `runs: Vec<Vec<StyleSpan>>` — line-bazlı span listesi
- Yeni `scratch: PipelineScratch` — `apply_rules_spans` reentrant scratch
- `Default` impl manuel (boot'ta App::from_snapshot konstrüksiyon)

### 5.2 [src/config_tui/events.rs:369](src/config_tui/events.rs#L369) — `recompile_preview` real impl

```rust
fn recompile_preview(app: &mut App) {
    app.preview.compile_error = None;
    app.preview.runs.clear();
    for line in app.sample_input.text.lines() {
        let spans = crate::pipeline::apply_rules_spans(
            line.as_bytes(),
            &app.preview.compiled,
            &mut app.preview.scratch,
        );
        app.preview.runs.push(spans);
    }
}
```

**Edge case (TUI reviewer I1):** `str::lines()` strips trailing `\n` ve elides trailing empty line. `"\n\n"` → `["", ""]`. Test #43 (§9.6 multi_line_sample) sample sonu `\n` ile biten input'larda exact line count assert eder.

### 5.3 [src/config_tui/widgets/preview.rs:37-47](src/config_tui/widgets/preview.rs#L37-L47) — `colorize_sample` span-aware render

```rust
fn colorize_sample(app: &App) -> Vec<Line<'_>> {
    let mut lines: Vec<Line> = Vec::new();
    if let Some(err) = &app.preview.compile_error {
        lines.push(Line::from(Span::raw(format!("⚠ {err}"))));
        lines.push(Line::from(""));
    }
    for (line_text, spans) in app.sample_input.text.lines().zip(&app.preview.runs) {
        lines.push(spans_to_line(line_text, spans));
    }
    lines
}

fn spans_to_line<'a>(line: &'a str, spans: &[StyleSpan]) -> Line<'a> {
    let mut out: Vec<Span<'a>> = Vec::new();
    let mut cursor = 0usize;
    for s in spans {
        debug_assert!(line.is_char_boundary(s.start), "span start not on char boundary");
        debug_assert!(line.is_char_boundary(s.end), "span end not on char boundary");
        if cursor < s.start {
            out.push(Span::raw(&line[cursor..s.start]));
        }
        out.push(Span::styled(&line[s.start..s.end], s.style.to_ratatui()));
        cursor = s.end;
    }
    if cursor < line.len() {
        out.push(Span::raw(&line[cursor..]));
    }
    Line::from(out)
}
```

`Style::to_ratatui` mapping (TUI reviewer B4 — actual axes only, real `Color` enum):

```rust
impl Style {
    /// Convert to ratatui's `Style` representation. Maps the 16 named
    /// colors + 8 bright + Indexed + Rgb to ratatui's Color enum;
    /// bool axes (bold/italic/underline/dim) to ratatui Modifier flags.
    /// Note: tayf has no `strikethrough` or `reverse` axes (see
    /// `src/style.rs:418-420` v0.1 scope note).
    pub(crate) fn to_ratatui(&self) -> ratatui::style::Style {
        let mut s = ratatui::style::Style::default();
        if let Some(fg) = self.fg { s = s.fg(color_to_ratatui(fg)); }
        if let Some(bg) = self.bg { s = s.bg(color_to_ratatui(bg)); }
        let mut m = ratatui::style::Modifier::empty();
        if self.bold      { m |= ratatui::style::Modifier::BOLD; }
        if self.italic    { m |= ratatui::style::Modifier::ITALIC; }
        if self.underline { m |= ratatui::style::Modifier::UNDERLINED; }
        if self.dim       { m |= ratatui::style::Modifier::DIM; }
        s.add_modifier(m)
    }
}

fn color_to_ratatui(c: Color) -> ratatui::style::Color {
    use ratatui::style::Color as R;
    match c {
        Color::Black => R::Black,
        Color::Red => R::Red,
        // ... all 16 named + 8 bright mapped 1:1 ...
        Color::Indexed(n) => R::Indexed(n),
        Color::Rgb(r, g, b) => R::Rgb(r, g, b),
    }
}
```

**Placement (TUI reviewer N3 decision):** `Style::to_ratatui` defined inline in `src/style.rs` impl block via `#[cfg(feature = "tui")]` OR placed in `src/config_tui/style_ratatui.rs` as a free function `fn to_ratatui(s: Style) -> ratatui::Style`. **Decision §14.1 #11:** `src/config_tui/style_ratatui.rs` — keeps `src/style.rs` ratatui-free (no core coupling).

### 5.4 Initial-compute on TUI startup + Preview boot wiring

`App::from_snapshot(snapshot)` (mevcut [app.rs:202-225](src/config_tui/app.rs#L202-L225)) signature genişletilir: theme+profile arg'lar veya `PreviewState::new` helper. Boot wiring:

```rust
// In App::from_snapshot or new App::boot helper:
let theme_name = snapshot.parsed.theme.as_deref();
let profile_name = snapshot.parsed.profile.as_deref();
let synth_config = synthesize_config_from_snapshot(&snapshot);  // helper in compile_pending.rs
let compiled = match crate::rules::compile_from_config(&synth_config, theme_name, profile_name) {
    Ok(c) => c,
    Err(e) => {
        // Boot-time compile failure: show banner, fallback to empty Compiled
        // (compile_error: Some(e.to_string()), runs: empty)
        // TUI still operates — user can edit + recompile.
        crate::rules::Compiled::empty()
    }
};
let preview_compiled = std::sync::Arc::new(arc_swap::ArcSwap::from_pointee(compiled));
let preview = PreviewState {
    compiled: preview_compiled,
    compile_error: /* Some(e.to_string()) or None per match above */,
    debouncer: Debouncer::default(),
    runs: Vec::new(),
    scratch: crate::pipeline::PipelineScratch::default(),
};
// ... rest of App struct construction
// After app constructed, before first frame draw:
recompile_preview(&mut app);
```

**Decision §14.1 #6:** Boot-time compile failure NOT fatal; compile_error banner shown + TUI proceeds with empty Compiled (graceful degradation; user can edit + recompile).

### 5.5 Pending-edit recompile path

```rust
fn apply_pending_and_recompile(app: &mut App) {
    let theme = app.snapshot.parsed.theme.as_deref();
    let profile = app.snapshot.parsed.profile.as_deref();
    match crate::config_tui::compile_pending::compile_pending(
        &app.snapshot, &app.edits, theme, profile,
    ) {
        Ok(new_compiled) => {
            app.preview.compiled.store(std::sync::Arc::new(new_compiled));
            app.preview.compile_error = None;
            recompile_preview(app);
        }
        Err(err) => {
            app.preview.compile_error = Some(err.to_string());
            // runs not cleared — leave previous render visible with error banner.
        }
    }
}
```

Modal commit handlers (D1-D4) bu helper'ı invoke eder. Debouncer-driven `Modal::EditRegex` de quiescent window'da bunu çağırır (Esc cancel'de NOT — `mark_edit` çağrılmaz; pending tickled olmaz).

---

## 6. Editor scope detay

### 6.1 D1 — ColorPicker → rule binding

[src/config_tui/events.rs:84](src/config_tui/events.rs#L84) bugün:
```rust
app.toast = Some(Toast::warn("color accepted (binding to selected rule lands in v0.6+)"));
```

v0.6 sonrası: ColorPicker `accept(color)` outcome'unda:
1. Selected rule (`app.focus.patterns.selected_idx`) → `RuleId` resolve (catalog `builtin_rule_names` vs `snapshot.parsed.rules[idx]`)
2. `app.edits.rules.entry(rule_id).or_default().styles.insert(StyleKey::Default, NewStyle { fg: Some(Some(color)), ..NewStyle::default() })`
3. `apply_pending_and_recompile(app)` invoke
4. Toast::ok "color bound to rule {name}"

**~80-120 LOC** (events.rs handler + RuleId resolver + unit tests).

### 6.2 D2 — `Modal::NewPattern` nested-state modal

`Modal` enum'a yeni variant:
```rust
Modal::NewPattern {
    phase: NewPatternPhase,
    draft: PatternDraft,
}

#[derive(Debug)]
pub(crate) enum NewPatternPhase {
    Name,
    Regex,
    Style,
}

#[derive(Debug)]
pub(crate) struct PatternDraft {
    pub(crate) name: String,
    pub(crate) pattern: String,
    pub(crate) pattern_error: Option<String>,
    pub(crate) picker_state: crate::config_tui::widgets::color_picker::ColorPickerState,
    pub(crate) draft_style: NewStyle,
}
```

Phase transitions:
- `Name` — text input; Enter validates name (non-empty, `[A-Za-z0-9_-]+`, len ≤ 64 — TUI reviewer N2: hyphen allowed); advances to `Regex`. Esc cancels modal.
- `Regex` — text input; Enter on valid pattern (validate via `RegexBuilder::new(&pat).size_limit(REGEX_SIZE_LIMIT_BYTES).build()`) advances to `Style`. `pattern_error` displays inline. Esc backs to `Name` (preserves draft).
- `Style` — ColorPicker invoked inline using `draft.picker_state`. Color accept commits `edits.added.push(NewRule { name, pattern, style: draft_style.into() })` + `apply_pending_and_recompile` + dismiss modal. Esc backs to `Regex` (preserves draft + picker_state per TUI reviewer I4).

[src/config_tui/tabs/patterns.rs:113](src/config_tui/tabs/patterns.rs#L113) Toast::warn strip; `n` keystroke dispatch.

**~280-420 LOC** (Modal variant + PatternDraft + 3 phase render fns + ColorPicker inline integration + dispatch + unit tests).

### 6.3 D3 — `Modal::EditRegex` inline regex source modal

```rust
Modal::EditRegex {
    rule_id: crate::config_tui::edit::RuleId,
    buffer: String,
    error: Option<String>,
}
```

`e` keystroke (when rule selected on Patterns tab):
1. RuleId resolve from `selected_idx` (builtin → `RuleId::Builtin(&'static name)`; user → `RuleId::UserConfig(name)`).
2. Buffer init = source pattern from snapshot.parsed.rules veya BUILTIN_NAMES catalog lookup; existing `edits.rules.get(&rule_id).pattern` overlay applied if pending edit.
3. Modal opens; text input editing → `app.preview.debouncer.mark_edit()` per keystroke.
4. Main event loop tick: `if app.preview.debouncer.should_recompile()` → temporary validate `RegexBuilder::new(&buffer).size_limit(...).build()`. If valid, simulate edit (`edits.rules.entry(rule_id).or_default().pattern = Some(buffer.clone())` temporarily applied + recompile) → if compile OK, preview updates; if compile failed, error banner. Commit only on Enter.
5. Enter on valid + clean compile → `edits.rules.entry(rule_id).or_default().pattern = Some(buffer)` permanent + `apply_pending_and_recompile` + dismiss.
6. Esc cancels (PendingEdits unchanged; debouncer remaining tick discarded — verify mark_edit/pending semantics).

[src/config_tui/tabs/patterns.rs:121](src/config_tui/tabs/patterns.rs#L121) Toast::warn strip.

**~250-350 LOC** (Modal variant + dispatch + debouncer wire + temporary edit shadow + tests).

### 6.4 D4 — `Modal::Help`

```rust
Modal::Help
```

`?` veya `F1` keystroke. Static const content (TUI reviewer I7 — canonical handler list cross-checked against events.rs + tabs/*):

```rust
const HELP_MODAL_CONTENT: &str = "\
tayf Config TUI — Keybindings
─────────────────────────────────────
Navigation
  Tab / Shift+Tab  Cycle tabs forward / backward
  1..4             Jump to tab by index
  j / k            Move selection down / up
  g / G            First / last entry
  h / l            Toggle list-pane / detail focus
  Space            Activate / toggle entry

Editing (Patterns tab)
  n                New pattern modal
  e                Edit pattern regex source
  c                Open color picker for selected rule
  o                Override builtin rule
  r                Reset user override

Display
  s                Set sample input
  p                Toggle live preview strip
  Shift+P          Full preview overlay
  / (forward-slash) Search filter

Persistence
  Ctrl+S / Ctrl+W  Save
  ? / F1           This help
  Esc              Dismiss modal / cancel edit
  Ctrl+C / q       Quit
";
```

[src/config_tui/events.rs:121,:126](src/config_tui/events.rs#L121) Toast::warn strip. `?` ve `F1` dispatch. Esc dismiss; **any other key dismisses Help and is discarded** (vim/less convention; TUI reviewer I6).

**~100-150 LOC** (Modal variant + render + dispatch + tests).

### 6.5 Debouncer ms200 (REVISION rev2) + Modal::EditRegex interaction

**Reality check (TUI reviewer B3):** `src/config_tui/debounce.rs:5` `const DEBOUNCE_WINDOW: Duration = Duration::from_millis(200);`. API: `mark_edit()` + `should_recompile()`. **No change to debouncer mechanism in v0.6.** Spec rev1'in ms300 + `tickle()` önerisi yanlıştı.

- Debounce window: **200ms (mevcut)**. Bench follow-up §14.5 değişikliği ms300/ms500 önerse, ayrı commit + tests/debounce.rs:50-74 assertion'ları güncellenir.
- `Modal::EditRegex` buffer mutation: per-keystroke `app.preview.debouncer.mark_edit()` çağırır.
- Main event loop tick (~100ms): `if app.preview.debouncer.should_recompile()` → `apply_pending_and_recompile` (with `edits.rules[rule_id].pattern = Some(buffer.clone())` shadow).
- Esc cancel: debouncer `pending` flag stale kalabilir → consume-once semantic ile bir kere fire'lar (no-op compile aynı buffer'a karşı). Acceptable behavior; alternative: `mark_edit_clear` helper eklenebilir (v0.6.1 nice-to-have).

---

## 7. Modal state machine

### 7.1 Modal enum (v0.6 sonrası) — extend existing shipped variants

Mevcut shipped (app.rs:91-101, **DEĞİŞMEZ**):
```rust
pub(crate) enum Modal {
    Confirm { msg: String, action: ConfirmAction },
    Error(String),
    QuitWithUnsavedEdits,
    ColorPicker(crate::config_tui::widgets::color_picker::ColorPickerState),
    SaveDiff,
    FullPreview,
    Search,
    SampleSet,
}
```

v0.6 EKLENECEK variant'lar (additive):
```rust
pub(crate) enum Modal {
    // ... mevcut 8 variant DEĞİŞMEZ ...

    // v0.6 yeni
    NewPattern { phase: NewPatternPhase, draft: PatternDraft },
    EditRegex { rule_id: RuleId, buffer: String, error: Option<String> },
    Help,
}
```

### 7.2 Focus stealing + Esc paths

| Mevcut Modal | `?`/F1 davranışı | Esc davranışı | Other modal keystroke davranışı |
|---|---|---|---|
| None | Modal::Help açar | (no-op) | n/e/c/etc. tab-dependent dispatch |
| Confirm/Error/QuitWithUnsavedEdits/ColorPicker/SaveDiff/FullPreview/Search/SampleSet | (no-op) — mevcut modal odak çalar | Mevcut modal cancels | Mevcut modal hard-blocks |
| NewPattern | (no-op) | Phase'e göre back/cancel (§6.2) | NewPattern hard-blocks |
| EditRegex | (no-op) | Cancel (debouncer pending consume on next tick) | EditRegex hard-blocks |
| Help | (no-op) — `?` re-press closes too | Close | **Any key dismisses Help, key DISCARDED** (TUI reviewer I6, vim/less convention) |

### 7.3 ColorPicker invoke nesting

`Modal::NewPattern { phase: Style }` ColorPicker'ı pure-function render olarak invoke eder:
- `widgets/color_picker.rs::render(frame, area, &ColorPickerState)` mevcut pure-function — ✓
- `dispatch_key(&mut ColorPickerState, KeyEvent) -> ColorPickerOutcome` mevcut — ✓
- `PatternDraft` `picker_state: ColorPickerState` field tutar (TUI reviewer I5 absorpsion)
- NewPattern Style phase render: `color_picker::render(frame, area, &draft.picker_state)` + key dispatch → `draft.picker_state` mutates → `ColorPickerOutcome::Accepted(color)` triggers commit; `Outcome::Cancelled` → phase back to Regex (preserves picker_state for resume).

Modal stack'i tek seviyeli kalır; ColorPicker pure-function reuse.

---

## 8. PendingEdits state diagram (REVISED — extend existing shipped struct)

### 8.1 PendingEdits — mevcut shipped struct DEĞİŞMEZ

[src/config_tui/edit.rs:104-109](src/config_tui/edit.rs#L104-L109):
```rust
#[derive(Default, Debug)]
pub(crate) struct PendingEdits {
    pub(crate) general: GeneralEdits,
    pub(crate) rules: HashMap<RuleId, RuleEdit>,
    pub(crate) added: Vec<NewRule>,
    pub(crate) deleted: HashSet<RuleId>,
}
```

v0.6 keystroke handler'lar bu modele yazar:

| Editor action | Mutates |
|---|---|
| D1 ColorPicker accept | `edits.rules.entry(rule_id).or_default().styles.insert(StyleKey::Default, NewStyle { fg: Some(Some(color)), .. })` |
| D2 NewPattern commit | `edits.added.push(NewRule { name, pattern, style: NewStyle { fg: Some(Some(color)), .. } })` |
| D3 EditRegex commit | `edits.rules.entry(rule_id).or_default().pattern = Some(new_pattern)` |
| (v0.6.1) `d` Delete | `edits.deleted.insert(rule_id)` |

**Rule identity by `RuleId` (HashMap key), NOT index.** Stale-index riski yok (Rust reviewer + TUI reviewer I10 absorpsion).

**No `PendingEdit` enum eklenir** — TUI reviewer B1: spec rev1'in flat `Vec<PendingEdit>` proposal'ı shipped data model'i kırardı (reconcile.rs + save.rs + 20+ call site break ederdi).

### 8.2 Lifecycle

| Olay | PendingEdits davranışı |
|---|---|
| TUI boot | `PendingEdits::default()` (empty) |
| Modal commit (D1-D3) | mutate per §8.1 table + `apply_pending_and_recompile` |
| Modal cancel | PendingEdits değişmez |
| Ctrl+S save | save success → `edits.clear()`; save fail → unchanged |
| (v0.6.1) Confirm DiscardEditsAndReload | `edits.clear()` + snapshot reload + recompile_preview |
| (v0.6.1) Confirm InitFromDump | `edits.clear()` + dump apply + recompile_preview |
| `is_dirty()` consulted | quit-confirm modal trigger; save button visibility |

### 8.3 compile_pending semantiği

`src/config_tui/compile_pending.rs` yeni modül:

```rust
//! `compile_pending` — synthesize Config from ConfigSnapshot + PendingEdits
//! and invoke `rules::compile_from_config` for live preview recompile.

use std::sync::Arc;

use crate::config::Config;
use crate::config_tui::edit::{PendingEdits, RuleId, StyleKey};
use crate::config_tui::snapshot::ConfigSnapshot;
use crate::rules::{self, Compiled, REGEX_SIZE_LIMIT_BYTES};

#[derive(Debug)]
pub(crate) enum CompileError {
    InvalidPattern { rule_name: String, source: String },
    CompileFailed(Box<crate::error::Error>),
}

impl std::fmt::Display for CompileError {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::InvalidPattern { rule_name, source } => {
                write!(f, "invalid regex for rule '{rule_name}': {source}")
            }
            Self::CompileFailed(e) => write!(f, "{e}"),
        }
    }
}

pub(crate) fn compile_pending(
    snapshot: &ConfigSnapshot,
    edits: &PendingEdits,
    theme_name: Option<&str>,
    profile_name: Option<&str>,
) -> Result<Compiled, CompileError> {
    // 1. Snapshot'tan UserRule listesi clone
    let mut user_rules = snapshot.parsed.rules.clone();

    // 2. edits.rules apply (pattern + styles overlay)
    apply_rule_overlays(&mut user_rules, &edits.rules)?;

    // 3. edits.deleted apply
    user_rules.retain(|r| !edits.deleted.contains(&RuleId::UserConfig(r.name.clone())));

    // 4. edits.added apply (validate regex via RegexBuilder size_limit guard)
    for new_rule in &edits.added {
        regex::bytes::RegexBuilder::new(&new_rule.pattern)
            .size_limit(REGEX_SIZE_LIMIT_BYTES)
            .dfa_size_limit(REGEX_SIZE_LIMIT_BYTES)
            .build()
            .map_err(|e| CompileError::InvalidPattern {
                rule_name: new_rule.name.clone(),
                source: e.to_string(),
            })?;
        user_rules.push(new_rule_to_user_rule(new_rule));
    }

    // 5. Synth Config + invoke rules::compile_from_config
    let synth = synth_config_from_snapshot_and_rules(snapshot, user_rules);
    rules::compile_from_config(&synth, theme_name, profile_name)
        .map_err(|e| CompileError::CompileFailed(Box::new(e)))
}
```

**ReDoS guard (Rust reviewer I8):** `RegexBuilder::size_limit(REGEX_SIZE_LIMIT_BYTES).dfa_size_limit(REGEX_SIZE_LIMIT_BYTES)` — same as `rules.rs:989-991` invocation. Test #50 explicit (`compile_pending_rejects_oversized_pattern_with_size_limit_error`).

`apply_rule_overlays` + `new_rule_to_user_rule` + `synth_config_from_snapshot_and_rules` — internal helpers. RuleId::UserConfig variant doğrudan match; RuleId::Builtin/Embedded/DiskProfile variant'ları yes (v0.6 D1-D3'de override capability) — config'e `styles_override` veya benzer mekanizma ile yansıtılır (mevcut UserConfig override yolu).

---

## 9. Test plan

### 9.1 pipeline.rs unit tests (~10-12 yeni)

**Parity gate (refactor zero-regression):**
1. `select_runs_extract_preserves_apply_rules_byte_output` — 8 sample input × current builtin rules + 1 capture-styled rule; pre/post refactor byte-identical SGR output.
2. `select_runs_extract_preserves_capture_group_runs` — synthetic capture-styled rule golden.

**Yeni API correctness:**
3. `apply_rules_spans_returns_empty_vec_for_no_match`
4. `apply_rules_spans_returns_single_span_for_single_match`
5. `apply_rules_spans_respects_priority_sort`
6. `apply_rules_spans_respects_overlap_rejection`
7. `apply_rules_spans_returns_capture_subspans_when_uses_capture_styling`
8. `apply_rules_spans_byte_offsets_point_into_line`
9. `apply_rules_spans_utf8_multibyte_span_boundary_is_char_boundary` (Turkish chars + duration match)
10. `apply_rules_spans_byte_emit_parity`
11. `apply_rules_spans_priority_extreme_values_do_not_overflow`
12. `apply_rules_spans_sorted_by_start_ascending_invariant`

### 9.2 src/config_tui/style_ratatui.rs unit tests (~6 yeni)

13. `style_to_ratatui_named_colors_map_one_to_one` (16 named + 8 bright)
14. `style_to_ratatui_indexed_color_maps_to_ratatui_color_indexed`
15. `style_to_ratatui_rgb_color_maps_to_ratatui_color_rgb`
16. `style_to_ratatui_bool_axes_combine_modifiers` (bold/italic/underline/dim all 4 — TUI reviewer B4)
17. `style_to_ratatui_default_style_yields_empty_ratatui_style`
18. `style_to_ratatui_semantics_match_to_sgr_for_color_and_axes`

### 9.3 src/config_tui/compile_pending.rs unit tests (~10 yeni)

19. `compile_pending_empty_edits_returns_byte_identical_snapshot`
20. `compile_pending_added_rule_appears_in_compiled`
21. `compile_pending_added_rule_invalid_pattern_errs_with_rule_name`
22. `compile_pending_rule_pattern_override_updates_existing` (HashMap<RuleId, RuleEdit>.pattern)
23. `compile_pending_rule_pattern_override_invalid_errs`
24. `compile_pending_rule_style_override_updates_existing` (HashMap.styles[StyleKey::Default])
25. `compile_pending_deleted_rule_dropped_from_compiled` (verify deletion)
26. `compile_pending_error_display_includes_rule_name_and_source` (memory `feedback_test_assertion_specificity` byte-pinned `assert_eq!`)
27. `compile_pending_rejects_oversized_pattern_with_size_limit_error` (Rust reviewer I8 ReDoS guard)
28. `compile_pending_compose_added_modified_deleted_in_single_call`

### 9.4 src/config_tui/edit.rs PendingEdits tests (~2 yeni — most coverage already exists)

29. `edits_rules_entry_or_default_pattern_round_trip` (v0.6 keystroke handler write pattern verified)
30. `edits_added_push_new_rule_marks_dirty` (already exists @ edit.rs:163-173 — verify no regression)

### 9.5 src/config_tui/events.rs editor dispatch tests (~10 yeni)

31. `color_picker_commit_binds_to_selected_rule_via_styles_default_key`
32. `color_picker_commit_invokes_recompile_preview`
33. `new_pattern_modal_name_phase_validates_alphanumeric_and_hyphen`
34. `new_pattern_modal_advances_to_regex_on_valid_name`
35. `new_pattern_modal_regex_phase_validates_inline_with_size_limit`
36. `new_pattern_modal_advances_to_style_on_valid_regex`
37. `new_pattern_modal_commit_appends_added_new_rule`
38. `edit_regex_modal_marks_debouncer_on_keystroke`
39. `edit_regex_modal_recompiles_on_quiescent_window` (uses `should_recompile()` post mark_edit)
40. `help_modal_any_key_dismisses_and_discards`

### 9.6 src/config_tui/widgets/preview.rs tests (~5 yeni)

41. `render_mini_uses_compiled_spans_for_colorization` (TestBackend frame.buffer cell scrape)
42. `colorize_sample_handles_no_spans_yields_raw_line`
43. `colorize_sample_handles_multi_line_sample_with_trailing_newline` (TUI reviewer I1 — `"\n\n"` → `["",""]`)
44. `colorize_sample_compile_error_banner_overrides_render`
45. `spans_to_line_cursor_walk_emits_gap_span_styled_span_tail`

### 9.7 Integration tests (~3 yeni / expanded)

46. **`tests/integration_tui_preview.rs`** — boots Config TUI via TestBackend, navigates to Patterns tab, asserts `frame.buffer().to_string()` contains expected colorized cells for sample's ipv4 substring. Requires TestBackend harness (§9.8).
47. **`tests/integration_tui_editor.rs`** — boot TUI, press `n`, type pattern name "test_pattern", advance, type regex `\d+`, advance, pick color from picker, commit; assert sample preview rerenders.
48. **`tests/integration_tui_help_modal.rs`** — boot TUI, press `?`, assert HELP_MODAL_CONTENT visible; press `j` → Help dismisses + `j` discarded (cursor unchanged).

### 9.8 TestBackend harness sub-task (TUI reviewer I8)

**`tests/common/tui_harness.rs`** yeni helper modülü (~80-120 LOC):
- `pub(crate) fn boot_tui_with_sample(sample: &str) -> (App, ratatui::Terminal<ratatui::backend::TestBackend>)` — ConfigSnapshot::empty() + sample assignment + initial recompile_preview + TestBackend with fixed 80×24 size.
- `pub(crate) fn draw_frame(term, app) -> ratatui::buffer::Buffer` — invoke main render fn, return buffer.
- `pub(crate) fn buffer_contains_styled_cells(buf, substring, expected_style) -> bool` — find cells matching `substring` text, verify their `Style` matches `expected_style`.
- `pub(crate) fn buffer_lines(buf) -> Vec<String>` — extract per-line text.

Group 2'ye sub-task; LOC budget +80-120 (group bumped to ~400-450).

### 9.9 Total beklenen count

- Mevcut baseline: 636 lib tests
- Yeni: ~45 (§9.1-9.6)
- Integration: 3 yeni dosya, ~5 test fonksiyonu

**Beklenen final lib count: 681 ± 5** (memory `feedback_enumerate_tests_for_invariant_claims` discipline — implementer'ın §9'da iteration sırasında ±5 yeni test eklemesi normal).

### 9.10 Bench follow-up

`benches/preview_compile.rs` (yeni) — `compile_pending` incremental compile latency baseline. Hedef: 12 builtin + 5 user rule snapshot + 1 ModifyPattern edit → median <5ms (NOT "cold cache" — Rust reviewer N4). Eğer >5ms median, v0.6.1'e itelenir VEYA debouncer window ms300+'e revise.

---

## 10. CHANGELOG draft

```markdown
## [0.6.0] - 2026-XX-XX

### Added
- Span-emitting preview pipeline: `apply_rules_spans` API for TUI consumers
  alongside the existing byte-emit `apply_rules`. Shared core via new
  `select_runs` helper guarantees single source of truth for matching,
  overlap rejection, and priority resolution (`src/pipeline.rs`).
- Config TUI live preview: mini-preview strip and Shift+P full overlay
  now render the user's sample input with actual rule styling, including
  capture-group substyling. Initial compute on TUI startup.
- Config TUI editing core:
  - `c` keystroke on selected rule opens ColorPicker; accepting binds the
    color to the rule via PendingEdits.
  - `n` keystroke opens a 3-phase new-pattern modal (Name → Regex → Style).
    Inline regex validation with `RegexBuilder::size_limit` ReDoS guard.
  - `e` keystroke opens an inline regex source editor for the selected
    rule. Debouncer-driven preview recompile (200ms quiescent window).
  - `?` / `F1` opens a Help modal listing all keybindings.
- `src/config_tui/compile_pending.rs` module: `compile_pending(snapshot,
  edits, theme, profile)` rebuilds Compiled from a ConfigSnapshot +
  PendingEdits delta; backs the live preview recompile path.
- `src/rules.rs::compile_from_config(config, theme, profile)` pub(crate)
  entry-point — additive (does not change existing `load_with_theme`).
- `src/config_tui/style_ratatui.rs`: `Style::to_ratatui()` helper for
  TUI consumers (kept out of core `src/style.rs` to avoid ratatui dep).

### Changed
- Stale forward-pointers stripped: 13+ `// reason: v0.5.5+ ...` annotations
  and 8+ `Toast::warn` v0.6+ stubs replaced by real implementations or
  removed (see spec §11 for exact count).

### Fixed
- Config TUI live preview no longer shows raw sample text; renders
  colorized output matching runtime behavior.

(Behavior changes to existing rule application: NONE. `apply_rules`
byte-emit path is byte-identical to v0.5.7 via golden parity tests.)
```

---

## 11. Stale forward-pointer enumeration (EXPANDED — TUI reviewer B6, Rust reviewer I6)

Memory `feedback_stale_dead_code_reason_drift`: v0.6 cleanup-pass'i bu listede her satırı strip eder. Implementer'ın §12 group 8 commit'inden önce listeyi grep ile re-doğrulaması zorunlu.

### 11.1 `// reason: v0.X.Y+ ...` annotations

| Lokasyon | Mevcut | v0.6 aksiyon |
|---|---|---|
| app.rs:61 | `// reason: written by h/l/Enter dispatch; v0.5.5+ wires the render-side` | STRIP |
| app.rs:69 | (aynı pattern Themes) | STRIP |
| app.rs:77 | (aynı pattern Profiles) | STRIP |
| app.rs:105 | `// reason: v0.5.5+ "Discard edits and reload" Confirm path` | KEEP — v0.6.1 defer |
| app.rs:111 | `// reason: v0.5.5+ Shift+D init-from-dump Confirm path` | KEEP — v0.6.1 defer |
| app.rs:159 | `// reason: populated by v0.5.5+ recompile_preview body` | STRIP (populated artık) |
| snapshot.rs:7-8 | `// reason: ParsedConfigView typed view consumed by v0.6+ live-preview` | STRIP (consumer landed) |
| edit.rs:6-11 | `// reason: RuleId variants ... wires land in v0.6+` | PARTIAL STRIP — v0.6 D1/D2/D3 wires UserConfig + UpsertRule paths; Builtin/Embedded/DiskProfile variants still v0.6.1+ (override path). Update comment text. |
| edit.rs:#[allow(dead_code)] line 12 | module-level allow | KEEP — Builtin/Embedded/DiskProfile RuleId variants + StyleKey::Numbered/Named hâlâ unused |
| debounce.rs:14-17 | `// editor (currently a v0.6+ Toast::warn stub on the `e` key) wires.` | STRIP (e editor landed) |
| events.rs:168 | `// Shift+D first-run init (§9.6) lands in v0.5.5+.` | KEEP — v0.6.1 defer |
| events.rs:280 | `// should_quit on success via a separate flag (deferred to v0.6+ ...)` | KEEP — v0.6.1 defer |
| events.rs:322 | `/// §12.1.1 / §9.6 (`DiscardEditsAndReload` / `InitFromDump` are v0.5.5+).` | KEEP — v0.6.1 defer |
| events.rs:367 | `/// rebuild lands in v0.6+ when the span-emitting preview pipeline is` | STRIP (landed) |
| widgets/preview.rs:7-10 | module doc `v0.5.4 simplification ... lands in v0.6+` | STRIP (landed) |
| widgets/preview.rs:36 | `colorize_sample` doc-comment `True colorized preview is deferred to v0.6+` | STRIP (landed) |
| widgets/sample_set.rs:5 | (sample set widget v0.6+) | VERIFY scope — if not in v0.6, KEEP with rationale |
| widgets/save_diff.rs:15-16,:120,:159 | save_diff modal v0.6+ forward-pointers | KEEP — save-diff merge reconciliation v0.7 scope |
| save.rs:15,:424 | v0.6+ Shift+D dump-backup + per-key conflict UI | KEEP — v0.6.1 / v0.7 defer |
| tabs/patterns.rs:6 | `(full new-pattern editor lands in v0.6+)` module doc | STRIP — landed in this spec |
| tabs/patterns.rs:49 | `Press 'e' to edit the regex source (lands in v0.6+ inline editor)` | STRIP — landed |
| tabs/profiles.rs:55 + :87 | profile override v0.6+ | KEEP — profile edit out-of-scope §2.2 |
| tabs/themes.rs:81 | theme edit v0.6+ | KEEP — themes tab edit out-of-scope §2.2 |

**Total identified: ~24 sites; STRIP ~10, PARTIAL STRIP ~1, KEEP ~13. Implementer'ın `git grep "v0\.5\.5+\|v0\.6+\|reason: " src/config_tui/` final = §11.1 STRIP + KEEP set ile birebir match etmeli; herhangi unlisted site final cross-cutting review BLOCKER.**

### 11.2 `Toast::warn("...v0.6+...")` stubs

| Lokasyon | Mevcut | v0.6 aksiyon |
|---|---|---|
| events.rs:84 | `"color accepted (binding to selected rule lands in v0.6+)"` | REPLACE — D1 wire |
| events.rs:121,:126 | `"help overlay lands in v0.6+ (Modal::Help wiring deferred)"` | REPLACE — D4 wire |
| events.rs:352 | `"this action lands in v0.5.5+ (init-from-dump and discard-reload deferred)"` | KEEP — v0.6.1 defer |
| tabs/patterns.rs:113 | `"new-pattern editor lands in v0.6+"` | REPLACE — D2 wire |
| tabs/patterns.rs:121 | `"inline regex source editor lands in v0.6+"` | REPLACE — D3 wire |
| tabs/themes.rs:81 | theme edit Toast::warn | KEEP — themes tab edit out-of-scope |
| tabs/profiles.rs:87 | profile edit Toast::warn | KEEP — profile edit out-of-scope |

### 11.3 Cargo.toml + CHANGELOG version bumps

`Cargo.toml` `version = "0.5.7"` → `"0.6.0"`. CHANGELOG `[0.6.0]` block (§10). Memory `feedback_test_assertion_specificity` — §10 CHANGELOG "13+" wording final §11.1 strip count'a göre kesin sayıyla replace (e.g., "10 annotations + 5 Toast::warn stubs").

---

## 12. Logical group breakdown (plan phase hint)

Memory `feedback_lean_process_small_subversions` per-group lean review pattern:

| Group | Scope | Tahmini LOC | Reviewer focus |
|---|---|---|---|
| 1 | Preview foundation: `select_runs` extract + `apply_rules_spans` + `StyleSpan` + parity tests (§9.1 #1-2, #3-12) | ~320 | Rust — refactor correctness, lifetime/borrow, parity gate |
| 2 | TUI preview render: `style_ratatui::to_ratatui` + app.rs PreviewState field + events.rs recompile_preview + widgets/preview.rs span render + initial-compute boot + TestBackend harness (§9.8) + §9.2 + §9.6 + integration_tui_preview test | ~430 | TUI — Line/Span construction, UTF-8 boundary, frame.buffer assertion, harness reusability |
| 3 | rules.rs DOKUNULMAZ touch: `compile_from_config` pub(crate) entry-point (additive) + tests | ~80 | Rust — additive correctness, signature parity with load_with_theme, byte-bounded touch verify |
| 4 | compile_pending + ReDoS guard: `src/config_tui/compile_pending.rs` + CompileError + §9.3 + §9.4 | ~280 | Rust — error path threading, ReDoS guard, snapshot.parsed.rules consumption |
| 5 | D1 ColorPicker → rule binding + apply_pending_and_recompile + §9.5 #31-32 | ~150 | TUI — focus stealing, RuleId resolution, recompile path |
| 6 | D2 Modal::NewPattern nested-state modal + ColorPicker inline phase + §9.5 #33-37 | ~350 | TUI — state machine transitions, validate paths, ColorPicker integration |
| 7 | D3 Modal::EditRegex + debouncer wire + temporary edit shadow + §9.5 #38-39 + §9.10 bench (decision-pending) | ~280 | TUI — debouncer interaction, validate buffer, Esc-cancel pending semantics |
| 8 | D4 Modal::Help + HELP_MODAL_CONTENT const + §9.5 #40 + integration_tui_help_modal test | ~150 | TUI — overlay render, dismiss path, key-discarded semantic |
| 9 | Stale forward-pointer strip (§11) + integration_tui_editor + CHANGELOG + version bump | ~80 | Cross-cutting (final review prep) |

**Group bundle disiplini:** Her group atomic commit. Group 5 D1'in incremental size'ı küçük (~150) — Group 6 D2 ile bundle ETMEZ çünkü D1 D2'nin precondition'ı (D2 Style phase ColorPicker inline kullanır → D1'in ColorPicker accept handler'ı önce çalışmalı). Group 3 (rules.rs touch) Group 4 (compile_pending) öncesi şart — compile_from_config landmek zorunda.

**Total tahmin:** ~2120 LOC (source ~1100 + tests ~1020). Beklenen final ~681 ± 5 lib tests.

---

## 13. Spec-phase parallel review absorption (rev1 → rev2)

Memory `feedback_spec_phase_parallel_review`: rev1 spec'i yazıldıktan sonra iki opus 4.7 senior reviewer parallel dispatch edildi. Her bulgu FOLD / DEFER / DROP işaretli; FOLD bulguları rev2 body'sine entegre edildi.

### 13.1 Rust senior findings

| Finding | Severity | Disposition |
|---|---|---|
| B1 — `compile_merged_rules` signature mismatch (module-private, takes `&[BuiltinRule]`) | 🔴 BLOCKER | **FOLD** §2.3 — rules.rs DOKUNULMAZ touch authorize edildi; `compile_from_config` yeni pub(crate) entry-point §4.1 surface table'da. Group 3 separate sub-task. |
| B2 — `derive_user_rules_from_snapshot` Compiled'dan reconstruction imkansız | 🔴 BLOCKER | **FOLD** §8.3 — `app.snapshot.parsed.rules` source-of-truth (ConfigSnapshot zaten populated). `derive_user_rules_from_snapshot` mimari yok; eski §14.3 #1 open Q kapatıldı + §14.1 pinned decision'a taşındı. |
| B3 — `Preview.compiled` boot wiring — TUI ayrı process, runtime ArcSwap yok | 🔴 BLOCKER | **FOLD** §5.4 — boot path explicit `compile_from_config` çağrısı + `Arc::new(ArcSwap::from_pointee(...))` wrap; failure path → empty Compiled + compile_error banner (§14.1 #6). |
| I1 — `select_runs<'a>` doc-comment incomplete | 🟡 IMPORTANT | **FOLD** §3.2 — doc-comment "borrow holds `&mut` exclusivity; materialize spans immediately" cümlesi eklendi. |
| I2 — `Style` Copy bound verified | 🟡 IMPORTANT | **DROP** — verification only; no action. |
| I3 — ArcSwap snapshot drop ordering | 🟡 IMPORTANT | **FOLD** §3.4 + §4.2 I6 — "Snapshot Arc dropped at function return; spans own Style by Copy" cümlesi eklendi. |
| I4 — `respect_existing_colors` preview path'te yok | 🟡 IMPORTANT | **FOLD** §4.2 I7 + §14.1 #5 — preview ignores `respect_existing_colors`; pin + negative test (§9.1 #9 covers via no-ANSI sample). |
| I5 — UTF-8 boundary `bytes::Regex` caveat | 🟡 IMPORTANT | **FOLD** §3.4 + §4.2 I5 + §5.3 — `debug_assert!(line.is_char_boundary(...))` in `spans_to_line`; `sample_input.text: String` guarantees UTF-8 input. |
| I6 — §11 audit missing ~7 sites | 🟡 IMPORTANT | **FOLD** §11 — expanded to ~24 sites with STRIP/KEEP disposition per site. |
| I7 — PendingEdits stale-index — RemoveRule deferred makes v0.6 safe | 🟡 IMPORTANT | **FOLD** §4.2 I8 + §8.1 — RuleId-keyed HashMap eliminates stale-index entirely (additional B1 cross-reference). |
| I8 — `Regex::new` ReDoS attack surface | 🟡 IMPORTANT | **FOLD** §8.3 — `RegexBuilder::size_limit(REGEX_SIZE_LIMIT_BYTES).dfa_size_limit(...)` guard; test #27 explicit. |
| N1 — `select_runs` rustdoc resolvable path | 🔵 NIT | **FOLD** §3.2 — `[`crate::pipeline::apply_rules`]` form. |
| N2 — Name validation len ≤ 32 arbitrary | 🔵 NIT | **FOLD** §6.2 — rationale: TOML key + display column budget; tightened to ≤ 64 (TUI reviewer N2 fold). |
| N3 — frame.buffer scrape pattern — confirm v0.5.4 precedent | 🔵 NIT | **FOLD** §9.8 — TestBackend harness explicit sub-task (no v0.5.4 precedent; new infrastructure). |
| N4 — CHANGELOG fuzzy "13+" count | 🔵 NIT | **FOLD** §11.3 — final count post strip ($N1 + N2 stripped, KEEP set). |
| N5 — §15 audit checklist missing CHANGELOG + Cargo.toml same-commit verify | 🔵 NIT | **FOLD** §15 — added "CHANGELOG + Cargo.toml version bump same commit verified" line. |
| N6 — External dep forward-pointers | 🔵 NIT | **DROP** — out-of-scope source-only spec. |
| N7 — `Modal::EditRegex` vs `Modal::NewPattern{Regex}` debouncer sharing | 🔵 NIT | **FOLD** §6.5 — pinned: only `Modal::EditRegex` uses debouncer; `Modal::NewPattern{phase: Regex}` inline-per-keystroke validate (no compile, just `RegexBuilder::build()` syntax check; compile lands only on commit). |

### 13.2 TUI/ratatui senior findings

| Finding | Severity | Disposition |
|---|---|---|
| B1 — PendingEdits redesign breaks shipped HashMap-based model | 🔴 BLOCKER | **FOLD** §8 — full rewrite: `PendingEdit` enum + flat `Vec<PendingEdit>` REJECTED; mevcut shipped struct EXTEND. v0.6 keystroke handler'lar `edits.rules.entry(rule_id).or_default()...` ile yazar. §8.1 table mapping. |
| B2 — Preview.compiled boot path | 🔴 BLOCKER | **FOLD** — Rust reviewer B3 ile aynı; §5.4 explicit boot wiring. |
| B3 — Debouncer API + interval mismatch (ms200, `mark_edit`/`should_recompile`, not ms300/tickle) | 🔴 BLOCKER | **FOLD** §6.5 — corrected to ms200 + `mark_edit()`/`should_recompile()`. Spec rev1's ms300 + `tickle()` was incorrect. |
| B4 — `Style::to_ratatui` axis enumeration wrong (no Hex, no Default; no strikethrough, no reverse) | 🔴 BLOCKER | **FOLD** §5.3 — actual `Color` enum (16 named + 8 bright + Indexed + Rgb); actual axes (bold/italic/underline/dim ONLY); §9.2 #16 covers 4 axes. |
| B5 — §11 KEEP/STRIP inconsistency + Ctrl+R not bound | 🔴 BLOCKER | **FOLD** §11.1 + §2.2 — Ctrl+R defer to v0.6.1 explicit; §8.2 lifecycle table'da Ctrl+R row marked (v0.6.1); DiscardEditsAndReload/InitFromDump KEEP rationale clarified. |
| B6 — §11 missed sites (7+) | 🔴 BLOCKER | **FOLD** §11.1 — expanded to ~24 sites. |
| I1 — `str::lines()` strips trailing `\n` | 🟡 IMPORTANT | **FOLD** §5.2 — edge case documented; test #43 covers trailing-`\n` line count. |
| I2 — UTF-8 defensive assertion | 🟡 IMPORTANT | **FOLD** §5.3 — Rust reviewer I5 ile aynı. |
| I3 — Line<'a> borrow + Paragraph render scope | 🟡 IMPORTANT | **FOLD** §4.2 — invariant'a "Line<'a> borrows from app.sample_input.text; render scope mandatory" eklendi. |
| I4 — NewPattern Esc back preserves draft | 🟡 IMPORTANT | **FOLD** §6.2 + §14.1 #10 — pinned: Esc backs preserves draft + picker_state. |
| I5 — NewPattern Style phase needs ColorPickerState | 🟡 IMPORTANT | **FOLD** §6.2 + §7.3 — PatternDraft `picker_state: ColorPickerState` field. |
| I6 — Help focus stealing semantic ("keystroke processes" wrong; should be discarded) | 🟡 IMPORTANT | **FOLD** §6.4 + §7.2 — vim/less convention: any key dismisses Help, key DISCARDED. |
| I7 — HELP_MODAL_CONTENT vs actual handlers cross-check | 🟡 IMPORTANT | **FOLD** §6.4 — rewrite based on real events.rs + tabs/patterns.rs handlers. |
| I8 — frame.buffer TestBackend harness missing | 🟡 IMPORTANT | **FOLD** §9.8 — explicit sub-task in Group 2 with LOC budget. |
| I9 — compile_pending snapshot-driven | 🟡 IMPORTANT | **FOLD** — Rust reviewer B2 ile aynı; §8.3 snapshot.parsed.rules source-of-truth. |
| I10 — Rule identity by name (RuleId), not idx | 🟡 IMPORTANT | **FOLD** §4.2 I8 + §6.3 + §8.1 — Modal::EditRegex `rule_id: RuleId`; HashMap key. |
| N1 — apply_rules_spans byte-offsets-into-line doc-comment | 🔵 NIT | **FOLD** §3.4 doc-comment. |
| N2 — Name allows hyphen — Rust reviewer N2 fold ile birlikte | 🔵 NIT | **FOLD** §6.2 — `[A-Za-z0-9_-]+` len ≤ 64. |
| N3 — Style::to_ratatui placement (core vs TUI module) | 🔵 NIT | **FOLD** §5.3 + §14.1 #11 — placed in `src/config_tui/style_ratatui.rs` (not core src/style.rs). |
| N4 — "cold cache" wording | 🔵 NIT | **FOLD** §9.10 — "median <5ms" wording. |
| N5 — v0.5.7 commit reference | 🔵 NIT | **DROP** — verified ✓ (08cab92). |
| N6 — EN/TR audit clean | 🔵 NIT | **DROP** — verification only. |
| N7 — Group 2 LOC budget bump | 🔵 NIT | **FOLD** §12 — Group 2 ~300 → ~430 LOC (TestBackend harness sub-task). |

**Net (rev1 → rev2):** 9 BLOCKER + 18 IMPORTANT + 14 NIT = **37 fold + 4 drop = 41 disposition**. Memory `feedback_consume_prior_review` discipline'i strict — silent omission yok.

---

## 14. Open questions + defer items + decisions to pin

### 14.1 Pinned decisions (in spec body)

1. **Modal::NewPattern architecture:** Nested-phase (`Modal::NewPattern { phase, draft }`), not flat variants. §6.2.
2. **`compile_pending` placement:** `src/config_tui/compile_pending.rs` yeni modül; delegates to `rules::compile_from_config` (new pub(crate) entry-point, §2.3 DOKUNULMAZ rules.rs touch authorize). §4.1.
3. **Help modal content:** Static const `HELP_MODAL_CONTENT` (events.rs grep-canonical). §6.4 + §14.4 audit checklist mandate.
4. **Debouncer interval:** **200ms (mevcut shipped — NOT ms300)**; `mark_edit()`/`should_recompile()` API. §6.5 + §9.10 bench post-decision.
5. **PendingEdits clear policy:** Commit mutates (per §8.1 table); Save success clears; Reload (v0.6.1) clears; Save failure unchanged. §8.2.
6. **Boot-time compile failure:** NOT fatal; show compile_error banner + fallback to `Compiled::empty()`. §5.4.
7. **All modal text English** (memory `feedback_review_calibration_en_tr`); spec/comments Türkçe.
8. **`StyleSpan`** owned `Style` (Copy), byte offsets, pub(crate) struct + fields.
9. **ColorPicker invoke in NewPattern.Style phase:** Inline (pure-function render with PatternDraft.picker_state), not modal-stack. §7.3.
10. **NewPattern Esc back preserves draft + picker_state** (TUI reviewer I4). §6.2.
11. **`Style::to_ratatui` placement:** `src/config_tui/style_ratatui.rs` (NOT core `src/style.rs`) to avoid ratatui dep in core. §5.3.
12. **PendingEdits structured-model extend:** mevcut shipped `{ general, rules: HashMap<RuleId, RuleEdit>, added: Vec<NewRule>, deleted: HashSet<RuleId> }` DEĞİŞMEZ; v0.6 handler'lar bu modele yazar. §8.1.
13. **Rule identity by RuleId (HashMap key), not index.** Stale-index riski yok. §4.2 I8.
14. **compile_pending snapshot-driven:** `app.snapshot.parsed.rules` source-of-truth; `derive_user_rules_from_snapshot` mimari yok. §8.3.
15. **`respect_existing_colors` preview path'te ignored** (no ANSI state machine). §4.2 I7.
16. **`Modal::NewPattern{phase: Regex}` inline-per-keystroke syntax validate** (`RegexBuilder::build()` only; no compile); `Modal::EditRegex` debouncer-driven compile. §6.5 N7 fold.

### 14.2 Defer items (v0.6.1 ve sonrası)

- v0.6.1: TUI polish — Shift+D init dump, V alias, search filter list-side, save-diff scroll, apply_confirm DiscardEditsAndReload + InitFromDump real impl, bool-axes-clear tri-state, `d`/Delete keystroke (uses existing `edits.deleted`), Ctrl+R reload binding, `mark_edit_clear` debouncer helper (nice-to-have).
- v0.7: http opt-in profile + multi-profile composition + render snapshot tests + audit follow-up tests (μs Latin-1, URL trim, ipv4 5-segment, FP C-4/C-8/C-9 reconsider).
- v0.7.x: reconcile.rs file split if grown >1000 LOC after v0.6.1.

### 14.3 Open questions for implementation-phase decision

1. **`Modal::EditRegex` Esc-cancel debouncer state:** Pending mark stays after Esc; next quiescent tick fires no-op compile against original buffer. Acceptable v0.6 baseline; `mark_edit_clear` helper as nice-to-have v0.6.1 cleanup.
2. **Builtin rule pattern edit overlay mechanism:** `Modal::EditRegex` on a builtin rule writes to `edits.rules.entry(RuleId::Builtin(name)).or_default().pattern`. compile_pending must apply this overlay to the builtin's pattern in the synth Config. Verify rules.rs `compile_from_config` supports builtin-pattern override OR add an overlay step in compile_pending. Plan-phase pin.

### 14.4 Help modal audit checklist (gelecek implementer'lar için)

When adding a new keystroke handler in `events.rs` or `tabs/*.rs`:
1. Update `HELP_MODAL_CONTENT` const (in events.rs or wherever the const lives).
2. Add a test entry in `integration_tui_help_modal.rs` asserting the new keystroke line appears.
3. Final cross-cutting reviewer checks: `git diff` shows both handler code AND `HELP_MODAL_CONTENT` update.

### 14.5 Bench follow-up decision

Eğer `compile_pending` incremental compile median >5ms (12 builtin + 5 user rule snapshot + 1 ModifyPattern edit), implementer hemen plan'a `benches/preview_compile.rs` bench ekler. Eğer >20ms, debouncer interval ms500'e çıkar (debounce.rs:5 const + tests/debounce.rs:50-74 byte-pinned assertions update) veya incremental-compile (RegexSet rebuild skip if rules.len() unchanged) v0.6.1'e itelenir.

---

## 15. Memory mandates audit checklist (final cross-cutting prep)

Implementer her group commit'inden önce ve final cross-cutting öncesi bu listeyi grep ile gate'ler:

- [ ] `feedback_consume_prior_review`: §1'de tüm v0.5.6 F1 NIT + v0.5.5 carryover'lar fold-or-defer enumerate (✅ §1).
- [ ] `feedback_spec_phase_parallel_review`: §13 Rust + TUI senior post-review absorpsion tamamlandı (✅ rev2 §13).
- [ ] `feedback_cross_cutting_review_value`: final opus 4.7 v0.5.7..v0.6 diff review (~1700-2200 LOC); reviewer mandate'ine per-group audit log + checklist eklenir.
- [ ] `feedback_lean_process_small_subversions`: BIG FULL ceremony — group bundle disiplini §12'de korunmuş; her group lean review.
- [ ] `feedback_test_assertion_specificity`: §9.3 #26 (`CompileError` Display) byte-pinned `assert_eq!`; §9.6 #41-45 (TUI preview render) frame.buffer cell-pinned assertions; loose `.contains()` yok.
- [ ] `feedback_duplicate_formatter_audit`: `grep -nE 'format!.*rule "|format!.*message: format!' src/{pipeline,config_tui,style}.rs` zero hit.
- [ ] `feedback_parallel_call_site_invariant_audit`: `select_runs` tek source of truth — `apply_rules` + `apply_rules_spans` ikisi de aynı helper'a delegate. `compile_from_config` + `load_with_theme` ikisi de `compile_merged_rules`'a delegate.
- [ ] `feedback_review_calibration_en_tr`: Tüm modal text + HELP_MODAL_CONTENT İngilizce; spec Türkçe; identifier'lar İngilizce.
- [ ] `feedback_stale_dead_code_reason_drift`: §11.1 + §11.2 her satır tick'lenmiş; `git grep "v0\.5\.5+\|v0\.6+\|reason: " src/config_tui/` final = §11 dispositions ile birebir match.
- [ ] `feedback_collision_pin_pattern`: v0.6 yeni collision pin'i yok; existing `_yields_to_*_v0_X_Y_limitation` pin'leri unchanged.
- [ ] `feedback_enumerate_tests_for_invariant_claims`: §9 invariants iddialarına test isimleri eşleştirildi (parity gate #1-#2, capture-group #7, UTF-8 #9, perf #11, ReDoS guard #27).
- [ ] `feedback_reload_precedence_snapshot`: PendingEdits + reload precedence (§8.2 v0.6.1 Ctrl+R reload PendingEdits.clear() — `reload.rs` precedence chain'ine yeni input olarak app.edits eklenmemeli; snapshot pattern korunmuş).
- [ ] `feedback_toml_edit_025_quirks`: `compile_pending` toml_edit'e dokunmaz (snapshot.doc cached); reconcile.rs unchanged.
- [ ] `feedback_parallel_session_scope`: this spec written from single session; no parallel session conflict expected during implementation.
- [ ] DOKUNULMAZ audit: `git diff --name-only v0.5.7..HEAD | grep -E 'src/(pty|io_loop|tty_guard|signals|runtime)\.rs'` = empty. `src/pipeline.rs` touch surface = §2.3 items 1-4. `src/rules.rs` touch surface = §2.3 item 5 (additive only — `compile_from_config` new pub(crate)).
- [ ] CLAUDE.md §3 security: `apply_rules_spans` no new attack surface (no I/O, no shell-out, no config-eval); `compile_pending` `RegexBuilder::size_limit` ReDoS guard active.
- [ ] CLAUDE.md §4 zero-tech-debt: no `// TODO` introduced without issue ref; no commented-out code; CHANGELOG `[0.6.0]` block + `Cargo.toml` version bump landed in same commit cycle.
- [ ] `Modal` enum extension: mevcut 8 shipped variant unchanged; 3 yeni variant (NewPattern, EditRegex, Help) additive only.

---

**Bu spec rev2.** Parallel reviewer findings tam absorbe edildi (40 FOLD + 4 DROP). writing-plans skill ile plan üretilebilir.
