# tayf v0.6 — Span-Emitting Preview Pipeline + Config TUI Editing Core

**Tarih:** 2026-05-28
**Yazar:** Bera (solo author) + Claude (Opus 4.7, 1M context)
**Önceki sürüm:** v0.5.7 @ `08cab92` (636 lib tests, CI yeşil)
**Bu sürümün raison d'être'i:** v0.6 iki birbirine kenetli foundation iyileştirmesini tek **BIG FULL ceremony** cycle'da bundle eder:

1. **Span-emitting preview pipeline** — `src/pipeline.rs:apply_rules` bugün yalnızca SGR byte-emit yapıyor; Config TUI'nin live preview surface'i ratatui `Span` listesi istiyor. v0.5.4'te (`spec §5.4`) sıkıntı `DOKUNULMAZ src/pipeline.rs` touch authorization'ı ertelenerek `widgets/preview.rs:7-10` "true colorized preview lands in v0.6+" forward-pointer'a dönüştürüldü. Ayrıca `events.rs:369` `recompile_preview` stub, `debounce.rs:17` "editor wires" stub bu blocker'ın eşliğinde uyandırma bekliyor.

2. **Config TUI editing core** — v0.5.4 TUI v1 read-only + ColorPicker + SaveDiff iskeleti ship etmişti; v0.5.5 reconcile + atomic-write bitirdi. Editor surface (ColorPicker → rule binding, `n` new-pattern modal, `e` inline regex source modal, Help modal, pending-edit recompile path) v0.6'ya forward-point edildi. 8 yerden Toast::warn stub geliyor ([events.rs:121,:126,:352](src/config_tui/events.rs#L121); [tabs/patterns.rs:113,:121](src/config_tui/tabs/patterns.rs#L113); [tabs/themes.rs:81](src/config_tui/tabs/themes.rs#L81); [tabs/profiles.rs:87](src/config_tui/tabs/profiles.rs#L87); [debounce.rs:17](src/config_tui/debounce.rs#L17); [edit.rs:8](src/config_tui/edit.rs#L8); [app.rs:61,:69,:77,:105,:111,:159](src/config_tui/app.rs)).

Bu iki scope'un coupling'i HIGH (memory `feedback_lean_process_small_subversions` bundle kararı): editor preview olmadan kör (CLAUDE.md §4 "no half-features"). v0.6 ikisini tek atomik release'te bitirir.

**Toplam tahmin:** ~1200-1600 LOC source + tests (memory `feedback_lean_process_small_subversions` BIG FULL ceremony eşiği, v0.5.4 TUI-monolith referansı). Beklenen final test count: **~671-681 lib tests** + ~3 integration suite expansion.

---

## 1. Önceki sürüm review-bulgu absorpsiyonu (zorunlu §1)

Memory `feedback_consume_prior_review`: yeni sub-version spec'i, önceki final cross-cutting review'unun recommendation'larını açıkça fold-or-defer eder. v0.5.7 doc-only cleanup ship döngüsüydü; final cross-cutting review yapılmadı (1-commit, lean spirit). v0.5.6 final F1 review (`docs/superpowers/reviews/2026-05-27-rust-senior-v0.5.6-final-cross-cutting-review.md`) recommendation'ları:

| Kaynak | Önerilen iş | v0.6 disposition |
|---|---|---|
| §13 BLOCKER (6 fixes) | `13 → 12` doc drift cluster | **OUT-OF-SCOPE** — landed @ `068b12b` pre-v0.5.6 tag |
| §8 NIT | `profiles.rs:799-808` stale ARN/ipv6 comment | **OUT-OF-SCOPE** — landed @ v0.5.7 `08cab92` |
| §1 NIT | `pipeline.rs:70-73` "first-match-wins" stale comment | **OUT-OF-SCOPE** — landed @ v0.5.7 `08cab92` |
| §10 NIT | FILENAME_EXTENSIONS code-side doc-comment claim | **DROP** — F1 claim incorrect, doc-comment was added @ v0.5.6 `84a7c3d` (rules.rs:131-145) |
| §13 NIT | CHANGELOG v0.5.6 "Fixed" first bullet wording tightening | **DROP** — acceptable as-is, lean spirit |
| FP audit C-4 | filename single-letter prose collision | **DEFER → v0.7+** — behavior-change risk (`make.o` regression), needs Go profile context |
| FP audit C-8 | filename ↔ fqdn Go pkg paths | **DEFER → v0.7** — needs `go` profile |
| FP audit C-9 | fqdn JWT user-config recipe → built-in | **DEFER → v0.7+** — user-config workaround sufficient |
| v0.5.5 §11 carryover #5 | multi-profile composition | **DEFER → v0.7** |
| v0.5.5 §11 carryover #8 | render snapshot tests | **DEFER → v0.7** (with audit follow-up tests bundle) |
| v0.5.4 §5.4 forward-pointer | span-emitting preview pipeline | **FOLD** — bu spec §3 + §4'ün kalbi |
| v0.5.5 §9 forward-pointer | editor TUI surface | **FOLD** — bu spec §6 |

Hiçbir önceki review item silent omission yok. v0.7 carryover'lar bu spec §14.2'de re-enumerate edilir.

---

## 2. Scope

### 2.1 In-scope (FOLD)

**A. Preview pipeline foundation (src/pipeline.rs DOKUNULMAZ touch authorized):**

- **A1.** `select_runs` helper extract: `apply_rules`'un adım 1-4'ünü (pre-filter → priority sort → captures/find iter → overlap reject → final start-sort) ayırıp `&mut PipelineScratch` üzerinde mutate eden, `scratch.runs`'a slice borrow döndüren helper'a refactor. Mevcut `apply_rules` byte-emit kısmı (adım 5) `select_runs` çağrısı + cursor walk olarak kalır. **Davranış byte-identical** (golden parity tests gate).
- **A2.** `apply_rules_spans` parallel API: `select_runs` reuse, owned `Vec<StyleSpan>` döner. Capture-group emit zaten `scratch.runs` üzerinden merged geldiği için ek mantık yok.
- **A3.** `StyleSpan` pub(crate) struct: `start: usize` (byte offset, inclusive), `end: usize` (byte offset, exclusive), `style: Style`. `Copy + Clone + Debug + PartialEq + Eq`.

**B. TUI preview wiring (src/config_tui/ + widgets):**

- **B1.** `app.rs Preview` struct genişletme: `pub(crate) runs: Vec<Vec<StyleSpan>>` (line-bazlı span listesi); stale `// reason: v0.5.5+ ...` annotations strip (memory `feedback_stale_dead_code_reason_drift`).
- **B2.** `events.rs:369` `recompile_preview` stub → real impl: sample line'ları üzerinde `apply_rules_spans` çağrısı; runs vec doldurma; `compile_error` clear/set.
- **B3.** `widgets/preview.rs:37-47` `colorize_sample` stub → span-aware render: `spans_to_line(&str, &[StyleSpan]) -> Line<'_>` cursor walk helper.
- **B4.** Initial-compute on TUI startup: `mod.rs run()` boot path'inde ilk `recompile_preview(&mut app)` çağrısı.
- **B5.** Stale forward-pointer strip — §11.

**C. Editor state foundation (src/config_tui/edit.rs + new compile_pending module):**

- **C1.** `PendingEdits` state expansion: `AddRule { name, pattern, style }`, `ModifyPattern { rule_idx, new_pattern }`, `ModifyStyle { rule_idx, new_style }`, `RemoveRule { rule_idx }` variants. v0.5.5 iskeleti hazır (`/// construction wires land in v0.6+` annotation strip).
- **C2.** `src/config_tui/compile_pending.rs` yeni modül: `pub(crate) fn compile_pending(snapshot: &Compiled, edits: &PendingEdits) -> Result<Compiled, CompileError>`. PendingEdits → `Vec<UserRule>` transform + `rules::compile_merged_rules` delegate (rules.rs DOKUNULMAZ touch'a gerek yok — public(crate) zaten).

**D. Editor modals (src/config_tui/app.rs Modal enum + events.rs dispatch + widgets):**

- **D1.** ColorPicker → rule binding: [events.rs:84](src/config_tui/events.rs#L84) "color accepted" Toast::warn → `PendingEdits::ModifyStyle` commit + `recompile_preview` invoke.
- **D2.** `Modal::NewPattern { phase: NewPatternPhase, draft: PatternDraft }` nested-state modal: `phase ∈ {Name, Regex, Style}`. `n` keystroke initiate. Regex validate via `Regex::new` inline error. Style sub-flow ColorPicker invoke. Commit → `PendingEdits::AddRule`.
- **D3.** `Modal::EditRegex { rule_idx, buffer, error }`: `e` keystroke (rule selected). Buffer init = rule.pattern. Debouncer-driven validate (`Regex::new`). Commit → `PendingEdits::ModifyPattern`.
- **D4.** `Modal::Help`: `?` veya `F1`. Static const content (keystroke list); Esc dismiss.

**E. Pending-edit recompile path (events.rs):**

- **E1.** PendingEdits commit veya Modal commit → `compile_pending` invoke → `app.preview.compiled` ArcSwap snapshot replace → `recompile_preview` rerun.
- **E2.** Debouncer wire (`debounce.rs:17` stub gerçek): `Modal::EditRegex` buffer mutation'da debouncer tick; quiescent window'da compile_pending + recompile.

### 2.2 Out-of-scope

- Multi-profile composition (v0.5.5 §11 carryover #5) → v0.7
- Render snapshot tests (v0.5.5 §11 carryover #8) → v0.7
- http opt-in profile (v0.5.6 CHANGELOG forward-pointer) → v0.7
- Audit follow-up tests (μs Latin-1, URL trim, ipv4 5-segment, FP C-4/C-8/C-9) → v0.7
- `Shift+D` first-run init dump, `V` alias, search filter list-side, save-diff `↑`/`↓` scroll, `apply_confirm DiscardEditsAndReload + InitFromDump` real impl, bool-axes-clear tri-state → v0.6.1 (TUI polish lean cycle)
- `reconcile.rs` file split (currently 871 LOC) → v0.7.x if grown >1000

### 2.3 DOKUNULMAZ touch authorization (CLAUDE.md project layout gate)

**src/pipeline.rs touch authorized 2026-05-28** for the following byte-bounded surface:

1. Yeni `pub(crate) fn select_runs<'a>(line: &[u8], compiled: &Compiled, scratch: &'a mut PipelineScratch) -> &'a [(usize, usize, Style)]` extract — refactor only, byte-identical output (parity tests gate).
2. Yeni `pub(crate) fn apply_rules_spans(line: &[u8], compiled_handle: &ArcSwap<Compiled>, scratch: &mut PipelineScratch) -> Vec<StyleSpan>` parallel API.
3. Yeni `pub(crate) struct StyleSpan { start, end, style }`.
4. Mevcut `apply_rules`'da adım 1-4'ün `select_runs` çağrısına delegate edilmesi (~5 satır kısaltma; byte output değişmez).

**ZERO touch** for: `src/{pty, io_loop, tty_guard, signals, runtime, rules}.rs`. `src/profiles.rs` touch yok (compile_pending rules.rs'in public(crate) `compile_merged_rules`'ını delegate eder; ProfileRule değişmez).

Authorized eden gerekçe: v0.5.6 priority field precedent'i (spec §2.3) — yeni pub(crate) API surface, capture-group invariant korunmuş, byte-identical existing behavior. Memory `feedback_parallel_call_site_invariant_audit`: tek source-of-truth (`select_runs`); span-emit ve byte-emit ortak helper ile invariants drift edemez.

---

## 3. Mimari: select_runs extract + apply_rules_spans parallel

### 3.1 Mevcut `apply_rules` akışı (src/pipeline.rs:54-142)

```
apply_rules(line, compiled_handle, scratch, out):
  snapshot = compiled_handle.load_full()
  compiled = &snapshot

  scratch.{accepted_spans, runs, event_scratch, active_scratch, set_match_scratch}.clear()

  ---- Adım 1: Pre-filter ----
  scratch.set_match_scratch.extend(compiled.set.matches(line).iter())

  ---- Adım 2: Priority sort (v0.5.6) ----
  scratch.set_match_scratch.sort_by(|a, b|
    Reverse(priorities[a]).cmp(&Reverse(priorities[b]))
      .then_with(|| a.cmp(&b)))

  ---- Adım 3: Match collection (per rule, captures or find) ----
  for &i in &scratch.set_match_scratch:
    if compiled.uses_capture_styling[i]:
      for caps in re.captures_iter(line):
        m = caps.get(0).unwrap()
        if overlaps_accepted: continue
        emit_capture_runs(...) → pushes to scratch.runs
        insert_accepted(...)
    else:
      for m in re.find_iter(line):
        if overlaps_accepted: continue
        scratch.runs.push((start, end, style))
        insert_accepted(...)

  ---- Adım 4: Final start-sort ----
  scratch.runs.sort_by_key(|&(s, _, _)| s)

  ---- Adım 5: Byte-emit ----
  cursor = 0
  for &(start, end, style) in &scratch.runs:
    out.write_all(&line[cursor..start])?
    out.write_all(style.to_sgr().as_bytes())?
    out.write_all(&line[start..end])?
    out.write_all(Style::reset_sgr().as_bytes())?
    cursor = end
  out.write_all(&line[cursor..])?
```

### 3.2 v0.6 refactor — adım 1-4 → `select_runs` helper

```rust
/// Collect matched, overlap-rejected, priority-sorted style runs for one
/// line into `scratch.runs`. Steps 1-4 of [`apply_rules`] (pre-filter →
/// priority sort → captures/find iter → overlap reject → final start-sort).
///
/// Returns a borrow into `scratch.runs` valid until the next mutation of
/// `scratch`. The caller picks the sink: SGR byte-emit for runtime, span
/// collection for TUI preview.
pub(crate) fn select_runs<'a>(
    line: &[u8],
    compiled: &Compiled,
    scratch: &'a mut PipelineScratch,
) -> &'a [(usize, usize, Style)] {
    scratch.accepted_spans.clear();
    scratch.runs.clear();
    scratch.event_scratch.clear();
    scratch.active_scratch.clear();
    scratch.set_match_scratch.clear();

    scratch.set_match_scratch.extend(compiled.set.matches(line).iter());

    {
        use std::cmp::Reverse;
        let priorities = &compiled.priorities;
        scratch.set_match_scratch.sort_by(|&a, &b| {
            Reverse(priorities[a]).cmp(&Reverse(priorities[b]))
                .then_with(|| a.cmp(&b))
        });
    }

    for &i in &scratch.set_match_scratch {
        let re = &compiled.individuals[i];
        if compiled.uses_capture_styling[i] {
            for caps in re.captures_iter(line) {
                let m = caps.get(0).expect("capture 0 is always the full match");
                let (start, end) = (m.start(), m.end());
                if overlaps_accepted(&scratch.accepted_spans, start, end) {
                    continue;
                }
                emit_capture_runs(
                    &caps, start, end,
                    compiled.styles[i],
                    &compiled.group_styles[i],
                    &mut scratch.event_scratch,
                    &mut scratch.active_scratch,
                    &mut scratch.runs,
                );
                insert_accepted(&mut scratch.accepted_spans, start, end);
            }
        } else {
            for m in re.find_iter(line) {
                let (start, end) = (m.start(), m.end());
                if overlaps_accepted(&scratch.accepted_spans, start, end) {
                    continue;
                }
                scratch.runs.push((start, end, compiled.styles[i]));
                insert_accepted(&mut scratch.accepted_spans, start, end);
            }
        }
    }

    scratch.runs.sort_by_key(|&(s, _, _)| s);
    &scratch.runs
}
```

### 3.3 Mevcut `apply_rules` v0.6 sonrası

```rust
pub(crate) fn apply_rules<W: Write>(
    line: &[u8],
    compiled_handle: &ArcSwap<Compiled>,
    scratch: &mut PipelineScratch,
    out: &mut W,
) -> std::io::Result<()> {
    let snapshot: Arc<Compiled> = compiled_handle.load_full();
    let runs = select_runs(line, snapshot.as_ref(), scratch);

    let mut cursor = 0usize;
    for &(start, end, style) in runs {
        out.write_all(&line[cursor..start])?;
        let sgr = style.to_sgr();
        if !sgr.is_empty() {
            out.write_all(sgr.as_bytes())?;
        }
        out.write_all(&line[start..end])?;
        out.write_all(Style::reset_sgr().as_bytes())?;
        cursor = end;
    }
    out.write_all(&line[cursor..])?;
    Ok(())
}
```

**Byte-identical garantisi:** `select_runs` çağrısı adım 1-4'ün literal kopyası; adım 5 değişmez. Golden parity tests bunu gate'ler (§9.1 Test 1).

### 3.4 Yeni `apply_rules_spans`

```rust
/// Span-emitting variant for the Config TUI preview. Same matching +
/// overlap + priority semantics as [`apply_rules`]; returns owned spans
/// instead of writing SGR bytes.
///
/// **Capture-group styling:** rules with `uses_capture_styling[i] = true`
/// produce 1..=N spans per match per v0.3.5 inner-overrides-outer
/// semantics — identical to runtime path via shared [`select_runs`].
///
/// Returns `Vec<StyleSpan>` with byte offsets into `line` (sorted by
/// `start` ASC; non-overlapping). UTF-8 multi-byte boundary safety:
/// regex 1.12 contract guarantees match boundaries are char boundaries.
pub(crate) fn apply_rules_spans(
    line: &[u8],
    compiled_handle: &ArcSwap<Compiled>,
    scratch: &mut PipelineScratch,
) -> Vec<StyleSpan> {
    let snapshot: Arc<Compiled> = compiled_handle.load_full();
    let runs = select_runs(line, snapshot.as_ref(), scratch);
    runs.iter()
        .map(|&(start, end, style)| StyleSpan { start, end, style })
        .collect()
}
```

### 3.5 `StyleSpan` veri tipi

```rust
/// Single styled byte range produced by [`apply_rules_spans`].
///
/// `start..end` are byte offsets into the input line (half-open; `end`
/// exclusive). Style is the resolved style for the run — capture-group
/// merging (inner overrides outer) is already applied upstream.
///
/// Non-overlapping invariant: in the returned `Vec<StyleSpan>` from
/// `apply_rules_spans`, no two spans overlap. Sorted by `start` ASC.
#[derive(Copy, Clone, Debug, PartialEq, Eq)]
pub(crate) struct StyleSpan {
    pub start: usize,
    pub end: usize,
    pub style: Style,
}
```

---

## 4. Public API + invariants

### 4.1 pub(crate) surface (yeni)

| Symbol | Location | Stability |
|---|---|---|
| `fn select_runs<'a>(line: &[u8], compiled: &Compiled, scratch: &'a mut PipelineScratch) -> &'a [(usize, usize, Style)]` | `src/pipeline.rs` | pub(crate); internal helper |
| `fn apply_rules_spans(line: &[u8], compiled_handle: &ArcSwap<Compiled>, scratch: &mut PipelineScratch) -> Vec<StyleSpan>` | `src/pipeline.rs` | pub(crate); TUI consumer |
| `struct StyleSpan { pub start, pub end, pub style }` | `src/pipeline.rs` | pub(crate); fields pub(crate) |
| `fn compile_pending(snapshot: &Compiled, edits: &PendingEdits) -> Result<Compiled, CompileError>` | `src/config_tui/compile_pending.rs` | pub(crate); editor consumer |

**Public API surface unchanged.** `src/lib.rs` exports'lar değişmez; v0.5.7 `pub fn run` tek public export'tur.

### 4.2 Invariantlar

- **I1 — Byte-identical existing behavior:** `apply_rules` adım 5'i değişmez; refactor sonrası 18+ mevcut pipeline test byte-identical pass eder.
- **I2 — Single source of truth:** `select_runs` matching + overlap + priority logic'in tek implementation'ı. Memory `feedback_parallel_call_site_invariant_audit` — gelecek invariants (yeni priority tier, RegexSet pre-filter genişletmesi) tek yerde değişir; span-emit otomatik miras alır.
- **I3 — Non-overlapping spans:** `apply_rules_spans` çıktısı sorted-by-start + non-overlapping. `accepted_spans` invariant'i bunu zaten gate'liyor.
- **I4 — Capture-group merge invariant:** `uses_capture_styling[i] == true` rule'lar `emit_capture_runs` ile expand olur; inner-overrides-outer (v0.3.5 §1.1) korunur — `select_runs` mevcut path'i değiştirmiyor.
- **I5 — UTF-8 boundary safety:** Match start/end regex 1.12 contract ile char boundary'sinde. TUI consumer `str::from_utf8_unchecked(&line[span.start..span.end])` veya `&line_str[span.start..span.end]` (line `&str` ise) güvenli.
- **I6 — ArcSwap snapshot lifecycle:** `apply_rules_spans` snapshot içeride yükler (caller'da `Arc<Compiled>` borrow yok). TUI preview rerun'da fresh snapshot alır.

---

## 5. TUI preview wiring

### 5.1 [src/config_tui/app.rs](src/config_tui/app.rs) — `Preview` struct genişletme

Mevcut:
```rust
pub(crate) struct Preview {
    pub compile_error: Option<String>,
    pub debouncer: Debouncer,
    pub compiled: Option<Arc<ArcSwap<Compiled>>>,  // populated by v0.5.5+ recompile_preview
}
```

v0.6 sonrası:
```rust
pub(crate) struct Preview {
    pub compile_error: Option<String>,
    pub debouncer: Debouncer,
    pub compiled: Arc<ArcSwap<Compiled>>,
    pub runs: Vec<Vec<StyleSpan>>,
    pub scratch: PipelineScratch,
}
```

Değişiklikler:
- `compiled: Option<...>` → `compiled: Arc<...>` (boot'ta runtime'ın handle'ından clone — `Option` artık gerek yok)
- Yeni `runs: Vec<Vec<StyleSpan>>` — line-bazlı span listesi
- Yeni `scratch: PipelineScratch` — `apply_rules_spans` reentrant scratch
- Stale `// reason: v0.5.5+ ...` annotations strip ([app.rs:61,:69,:77,:105,:111,:159](src/config_tui/app.rs))

### 5.2 [src/config_tui/events.rs:369](src/config_tui/events.rs#L369) — `recompile_preview` real impl

```rust
fn recompile_preview(app: &mut App) {
    app.preview.compile_error = None;
    app.preview.runs.clear();
    for line in app.sample_input.text.lines() {
        let spans = crate::pipeline::apply_rules_spans(
            line.as_bytes(),
            &app.preview.compiled,
            &mut app.preview.scratch,
        );
        app.preview.runs.push(spans);
    }
}
```

### 5.3 [src/config_tui/widgets/preview.rs:37-47](src/config_tui/widgets/preview.rs#L37-L47) — `colorize_sample` span-aware render

```rust
fn colorize_sample(app: &App) -> Vec<Line<'_>> {
    let mut lines: Vec<Line> = Vec::new();
    if let Some(err) = &app.preview.compile_error {
        lines.push(Line::from(Span::raw(format!("⚠ {err}"))));
        lines.push(Line::from(""));
    }
    for (line_text, spans) in app.sample_input.text.lines().zip(&app.preview.runs) {
        lines.push(spans_to_line(line_text, spans));
    }
    lines
}

fn spans_to_line<'a>(line: &'a str, spans: &[StyleSpan]) -> Line<'a> {
    let mut out: Vec<Span<'a>> = Vec::new();
    let mut cursor = 0usize;
    for s in spans {
        if cursor < s.start {
            out.push(Span::raw(&line[cursor..s.start]));
        }
        out.push(Span::styled(&line[s.start..s.end], s.style.to_ratatui()));
        cursor = s.end;
    }
    if cursor < line.len() {
        out.push(Span::raw(&line[cursor..]));
    }
    Line::from(out)
}
```

`Style::to_ratatui(&self) -> ratatui::style::Style` yeni helper [src/style.rs](src/style.rs) — pub(crate). Converts `Color::Indexed/Hex/Default` + bool axes → `ratatui::style::Style`. ~30 LOC + ~6 test.

### 5.4 Initial-compute on TUI startup

[src/config_tui/mod.rs](src/config_tui/mod.rs) `run()` boot path'inde, ilk frame draw'dan önce `recompile_preview(&mut app)` çağrısı. Test: `tui_starts_with_populated_preview_runs`.

### 5.5 Pending-edit recompile path

[src/config_tui/events.rs] yeni helper:

```rust
fn apply_pending_and_recompile(app: &mut App) {
    let snapshot = app.preview.compiled.load_full();
    match compile_pending::compile_pending(snapshot.as_ref(), &app.pending_edits) {
        Ok(new_compiled) => {
            app.preview.compiled.store(Arc::new(new_compiled));
            app.preview.compile_error = None;
            recompile_preview(app);
        }
        Err(err) => {
            app.preview.compile_error = Some(err.to_string());
        }
    }
}
```

Modal commit handlers (D1-D4) bu helper'ı invoke eder. Debouncer-driven Modal::EditRegex de quiescent window'da bunu çağırır.

---

## 6. Editor scope detay

### 6.1 D1 — ColorPicker → rule binding

[src/config_tui/events.rs:84](src/config_tui/events.rs#L84) bugün:
```rust
app.toast = Some(Toast::warn("color accepted (binding to selected rule lands in v0.6+)"));
```

v0.6 sonrası: ColorPicker `accept(color)` outcome'unda:
1. Selected rule (`app.selected_rule_idx`) tespit
2. `PendingEdits::ModifyStyle { rule_idx, new_style }` append (mevcut style + new color)
3. `apply_pending_and_recompile(app)` invoke
4. Toast::info "color bound to rule {name}"

**~80-120 LOC** (events.rs handler + edit.rs PendingEdit variant + unit tests).

### 6.2 D2 — `Modal::NewPattern` nested-state modal

`Modal` enum'a yeni variant:
```rust
Modal::NewPattern {
    phase: NewPatternPhase,
    draft: PatternDraft,
}

enum NewPatternPhase {
    Name,
    Regex,
    Style,
}

struct PatternDraft {
    name: String,
    pattern: String,
    pattern_error: Option<String>,
    style: Option<Style>,
}
```

Phase transitions:
- `Name` — text input; Enter validates name (non-empty, alphanumeric+underscore, len ≤ 32); advances to `Regex`. Esc cancels modal.
- `Regex` — text input; live debouncer-validated via `Regex::new(&draft.pattern)`; `pattern_error` displays inline. Enter on valid pattern advances to `Style`. Esc backs to `Name`.
- `Style` — ColorPicker invoke; `accept(color)` commits `PendingEdits::AddRule { name, pattern, style }` + `apply_pending_and_recompile` + dismiss modal. Esc backs to `Regex`.

[src/config_tui/tabs/patterns.rs:113](src/config_tui/tabs/patterns.rs#L113) Toast::warn strip; `n` keystroke dispatch.

**~250-400 LOC** (Modal variant + PatternDraft + 3 phase render fns + dispatch + unit tests).

### 6.3 D3 — `Modal::EditRegex` inline regex source modal

```rust
Modal::EditRegex {
    rule_idx: usize,
    buffer: String,
    error: Option<String>,
}
```

`e` keystroke (when rule selected on Patterns tab):
1. Buffer init = `app.compiled.individuals[rule_idx].as_str()` (regex source)
2. Modal opens; text input editing
3. Debouncer-driven (ms300, §6.5) validate: `Regex::new(&buffer)` → error inline
4. Enter on valid → `PendingEdits::ModifyPattern { rule_idx, new_pattern }` + `apply_pending_and_recompile` + dismiss
5. Esc cancels (PendingEdits unchanged)

[src/config_tui/tabs/patterns.rs:121](src/config_tui/tabs/patterns.rs#L121) Toast::warn strip.

**~200-300 LOC** (Modal variant + dispatch + debouncer wire + tests).

### 6.4 D4 — `Modal::Help`

```rust
Modal::Help
```

`?` veya `F1` keystroke. Static const content (decisions §14.4):

```rust
const HELP_MODAL_CONTENT: &str = "\
tayf Config TUI — Keybindings
─────────────────────────────────────
Navigation
  h/l            Switch tab
  j/k            Move selection
  Tab            Cycle focus

Editing
  n              New pattern (Patterns tab)
  e              Edit pattern regex source
  c              Open color picker for selected rule

Display
  s              Cycle sample input
  P              Toggle live preview
  Shift+P or V   Full preview overlay
  ?  F1          This help modal

Persistence
  Ctrl+S         Save (atomic write + diff modal)
  Ctrl+R         Reload config from disk

  Esc            Dismiss modal / cancel edit
  Ctrl+C  q      Quit
";
```

[src/config_tui/events.rs:121,:126](src/config_tui/events.rs#L121) Toast::warn strip. `?` ve `F1` dispatch. Esc dismiss.

**~100-150 LOC** (Modal variant + render + dispatch + tests).

### 6.5 Debouncer ms300 default

[src/config_tui/debounce.rs:17](src/config_tui/debounce.rs#L17) stub gerçek:
- Default interval ms300 (compile pahalı: 12 builtin + N user rules + N profile rules → RegexSet rebuild benchmark TBD pre-tag)
- `Modal::EditRegex` buffer mutation `debouncer.tickle()` çağırır
- `events.rs check_debounce` quiescent window'da `apply_pending_and_recompile` invoke
- Bench follow-up: `benches/throughput.rs` veya yeni `benches/preview_compile.rs` (decisions §14.5) — incremental compile latency baseline; gerekirse v0.6.1'e itelenir

---

## 7. Modal state machine

### 7.1 Modal enum (v0.6 sonrası)

```rust
pub(crate) enum Modal {
    Search(SearchState),       // v0.5.4
    SaveDiff(SaveDiffState),   // v0.5.4
    ColorPicker(ColorPickerState),  // v0.5.4
    Confirm(ConfirmState),     // v0.5.4

    // v0.6 yeni
    NewPattern { phase: NewPatternPhase, draft: PatternDraft },
    EditRegex { rule_idx: usize, buffer: String, error: Option<String> },
    Help,
}
```

### 7.2 Focus stealing + Esc paths

| Mevcut Modal | `?`/F1 davranışı | Esc davranışı | Other modal keystroke davranışı |
|---|---|---|---|
| None | Modal::Help açar | (no-op) | n/e tab-dependent |
| Search/SaveDiff/ColorPicker/Confirm | (no-op) — mevcut modal odak çalar | Mevcut modal cancels | Mevcut modal hard-blocks |
| NewPattern | (no-op) | Phase'e göre back/cancel (§6.2) | NewPattern hard-blocks |
| EditRegex | (no-op) | Cancel | EditRegex hard-blocks |
| Help | Toggle (close) | Close | Help dismisses, keystroke processes |

### 7.3 ColorPicker invoke nesting

`Modal::NewPattern { phase: Style }` ColorPicker'ı nasıl invoke eder? İki seçenek:
- **A:** ColorPicker'ı sub-modal olarak yığ (Modal::ColorPicker + return_modal saklı)
- **B:** NewPattern Style phase rendered as ColorPicker UI inline, accept outcome NewPattern.draft.style'a yazar

**Tavsiye + karar:** B (inline ColorPicker phase). Modal stack'i tek seviyeli kalır; accept outcome `Modal::None` dönmek yerine NewPattern commit'i tetikler. Bu ColorPicker widget'ının pure-function render'ını gerekir (`render_color_picker(frame, area, &state)`) ki hem standalone hem inline kullanılabilsin. Mevcut ColorPickerState `pub(crate)`; refactor minimal.

---

## 8. PendingEdits state diagram

### 8.1 PendingEdits variants (v0.6 sonrası)

```rust
pub(crate) enum PendingEdit {
    AddRule { name: String, pattern: String, style: Style },
    ModifyPattern { rule_idx: usize, new_pattern: String },
    ModifyStyle { rule_idx: usize, new_style: Style },
}

pub(crate) struct PendingEdits {
    pub edits: Vec<PendingEdit>,
}
```

`RemoveRule` variant'i v0.6'da scope dışı — `d`/Delete keystroke handler v0.6.1'e defer (CLAUDE.md §4 YAGNI). v0.6.1'de eklendiğinde tek-satır enum variant + dispatch.

`rule_idx` semantiği: `app.compiled.individuals` snapshot'ında index. Stale-index problemi (rule list mutate olduktan sonra eski idx geçersiz): v0.6'da PendingEdits sequential apply; her ModifyX `compile_pending` ile re-resolve edilmez — idx başlangıç snapshot'ta hangiyse o kalır. Memory `feedback_consume_prior_review` v0.7+ revisit (multi-edit composability).

### 8.2 Lifecycle

| Olay | PendingEdits davranışı |
|---|---|
| TUI boot | PendingEdits::new() (empty) |
| Modal commit (D1-D3) | edits.push(...) + apply_pending_and_recompile |
| Modal cancel | PendingEdits değişmez |
| Ctrl+S save | save success → PendingEdits::clear(); save fail → PendingEdits unchanged |
| Ctrl+R reload | PendingEdits::clear() + compiled snapshot reload from disk + recompile_preview |
| Confirm DiscardEditsAndReload (v0.6.1) | PendingEdits::clear() + reload |

### 8.3 compile_pending semantiği

```rust
pub(crate) fn compile_pending(
    snapshot: &Compiled,
    edits: &PendingEdits,
) -> Result<Compiled, CompileError> {
    // 1. Snapshot'tan mutable rule list extract: Vec<UserRule>
    let mut working: Vec<UserRule> = derive_user_rules_from_snapshot(snapshot);

    // 2. Edits sequential apply
    for edit in &edits.edits {
        match edit {
            PendingEdit::AddRule { name, pattern, style } => {
                Regex::new(pattern).map_err(|e| CompileError::InvalidPattern {
                    rule_name: name.clone(),
                    source: e.to_string(),
                })?;
                working.push(UserRule { name: name.clone(), pattern: pattern.clone(), style: *style, priority: None });
            }
            PendingEdit::ModifyPattern { rule_idx, new_pattern } => { ... }
            PendingEdit::ModifyStyle { rule_idx, new_style } => { ... }
        }
    }

    // 3. compile_merged_rules invoke
    rules::compile_merged_rules(&working).map_err(CompileError::CompileFailed)
}
```

`CompileError` yeni enum `src/config_tui/compile_pending.rs`'de — `InvalidPattern { rule_name, source }`, `CompileFailed(rules::CompileError)`. Display impl memory `feedback_test_assertion_specificity` ile byte-pinned test.

`derive_user_rules_from_snapshot` — `Compiled` snapshot'ı UserRule listesine geri çevirir (v0.6 için: builtin'ler skip, user-rule'ları reconstruct). v0.7'de reconcile.rs'den paylaşılabilir.

---

## 9. Test plan

### 9.1 pipeline.rs unit tests (~10-12 yeni)

**Parity gate (refactor zero-regression):**
1. `select_runs_extract_preserves_apply_rules_byte_output` — 8 sample input × current builtin rules + 1 capture-styled rule; pre/post refactor byte-identical SGR output.
2. `select_runs_extract_preserves_capture_group_runs` — synthetic capture-styled rule golden (mevcut `apply_rules_with_capture_styling_rule_emits_multi_run_match`'in span variant'i).

**Yeni API correctness:**
3. `apply_rules_spans_returns_empty_vec_for_no_match`
4. `apply_rules_spans_returns_single_span_for_single_match` — `192.168.1.1` line → 1 ipv4 span
5. `apply_rules_spans_respects_priority_sort` — aws.arn (priority 200) vs interior region (100): arn span wins
6. `apply_rules_spans_respects_overlap_rejection` — nested case
7. `apply_rules_spans_returns_capture_subspans_when_uses_capture_styling` — multi-run capture rule
8. `apply_rules_spans_byte_offsets_point_into_line` — `line[span.start..span.end]` slice valid
9. `apply_rules_spans_utf8_multibyte_span_boundary_is_char_boundary` — Turkish chars line + duration match (memory `feedback_review_calibration_en_tr` exception — test data legitimate)
10. `apply_rules_spans_byte_emit_parity` — `apply_rules_spans` çıktısı → SGR-reconstructed = `apply_rules` byte output (cross-check)
11. `apply_rules_spans_priority_extreme_values_do_not_overflow` — i32::MAX/MIN priorities (v0.5.6 invariant inherit)
12. `apply_rules_spans_sorted_by_start_ascending_invariant`

### 9.2 src/style.rs unit tests (~6 yeni)

13. `style_to_ratatui_indexed_color_maps_to_ratatui_color`
14. `style_to_ratatui_hex_color_maps_to_ratatui_rgb`
15. `style_to_ratatui_default_color_yields_reset`
16. `style_to_ratatui_bool_axes_combine_modifiers`
17. `style_to_ratatui_default_style_yields_empty_ratatui_style`
18. `style_to_ratatui_byte_identical_with_to_sgr_semantics` — bool axes & color set in to_sgr → set in ratatui

### 9.3 src/config_tui/compile_pending.rs unit tests (~8 yeni)

19. `compile_pending_empty_edits_returns_byte_identical_snapshot`
20. `compile_pending_add_rule_appends_to_compiled`
21. `compile_pending_add_rule_invalid_pattern_errs_with_rule_name`
22. `compile_pending_modify_pattern_updates_existing`
23. `compile_pending_modify_pattern_invalid_errs`
24. `compile_pending_modify_style_swaps_style_only`
25. `compile_pending_remove_rule_drops_by_idx`
26. `compile_pending_error_display_includes_rule_name_and_source` — memory `feedback_test_assertion_specificity` `assert_eq!` byte-pinned

### 9.4 src/config_tui/edit.rs PendingEdits unit tests (~4 yeni)

27. `pending_edits_starts_empty`
28. `pending_edits_add_rule_appends`
29. `pending_edits_clear_after_save_success`
30. `pending_edits_unchanged_on_save_failure`

### 9.5 src/config_tui/events.rs editor dispatch tests (~10 yeni)

31. `color_picker_commit_binds_to_selected_rule_via_modify_style`
32. `color_picker_commit_invokes_recompile_preview`
33. `new_pattern_modal_name_phase_validates_alphanumeric`
34. `new_pattern_modal_advances_to_regex_on_valid_name`
35. `new_pattern_modal_regex_phase_validates_inline`
36. `new_pattern_modal_advances_to_style_on_valid_regex`
37. `new_pattern_modal_commit_appends_pending_edit_add_rule`
38. `edit_regex_modal_recompiles_on_debounce_quiescent`
39. `edit_regex_modal_error_does_not_clear_buffer`
40. `help_modal_dismissed_on_esc`

### 9.6 src/config_tui/widgets/preview.rs tests (~5 yeni)

41. `render_mini_uses_compiled_spans_for_colorization` — App fixture, sample contains ipv4; rendered Paragraph contains ratatui Span with ipv4 style
42. `colorize_sample_handles_no_spans_yields_raw_line`
43. `colorize_sample_handles_multi_line_sample` — 3-line sample, 3 Lines emitted
44. `colorize_sample_compile_error_banner_overrides_render`
45. `spans_to_line_cursor_walk_emits_gap_span_styled_span_tail`

### 9.7 Integration tests (~3 yeni / expanded)

46. **`tests/integration_tui_preview.rs`** — boots Config TUI, navigates to Patterns tab, asserts `frame.buffer` contains expected colorized cells for sample's ipv4 substring (modifier + color attribute). Headless ratatui buffer scrape pattern (v0.5.4 precedent).
47. **`tests/integration_tui_editor.rs`** — boot TUI, press `n`, type pattern name "test_pattern", advance, type regex `\d+`, advance, pick color from picker, commit; assert sample preview rerenders with new rule (frame.buffer scan).
48. **`tests/integration_tui_help_modal.rs`** — boot TUI, press `?`, assert HELP_MODAL_CONTENT lines visible, press Esc, assert main view restored.

### 9.8 Total beklenen count

- Mevcut baseline: 636 lib tests
- Yeni: ~45 (§9.1-9.6)
- Integration: 3 yeni dosya, ~5 test fonksiyonu

**Beklenen final lib count: 681 ± 5** (memory `feedback_enumerate_tests_for_invariant_claims` discipline — implementer'ın §9'da iteration sırasında ±5 yeni test eklemesi normal).

### 9.9 Bench follow-up

`benches/preview_compile.rs` (yeni) — `compile_pending` incremental compile latency baseline. Hedef: 12 builtin + 5 user rule snapshot + 1 ModifyPattern edit → <5ms cold cache. Eğer fail ederse §14.5 decision'a göre v0.6.1'e itelenir.

---

## 10. CHANGELOG draft

```markdown
## [0.6.0] - 2026-XX-XX

### Added
- Span-emitting preview pipeline: `apply_rules_spans` API for TUI consumers
  alongside the existing byte-emit `apply_rules`. Shared core via new
  `select_runs` helper guarantees single source of truth for matching,
  overlap rejection, and priority resolution (`src/pipeline.rs`).
- Config TUI live preview: mini-preview strip and Shift+P / V full overlay
  now render the user's sample input with actual rule styling, including
  capture-group substyling. Initial compute on TUI startup.
- Config TUI editing core:
  - `c` keystroke on selected rule opens ColorPicker; accepting binds the
    color to the rule via PendingEdits.
  - `n` keystroke opens a 3-phase new-pattern modal (Name → Regex → Style).
    Inline regex validation; commit appends `PendingEdits::AddRule`.
  - `e` keystroke opens an inline regex source editor for the selected
    rule. Debouncer-driven preview recompile (ms300).
  - `?` / `F1` opens a Help modal listing all keybindings.
- `src/config_tui/compile_pending.rs` module: `compile_pending(snapshot, edits)`
  rebuilds Compiled from a PendingEdits delta; backs the live preview
  recompile path.
- `src/style.rs`: `Style::to_ratatui()` helper for TUI consumers.

### Changed
- Stale forward-pointers stripped: 13+ `// reason: v0.5.5+ ...` annotations
  and 8+ `Toast::warn` v0.6+ stubs replaced by real implementations or
  removed (see spec §11).

### Fixed
- Config TUI live preview no longer shows raw sample text; renders
  colorized output matching runtime behavior.

(Behavior changes to existing rule application: NONE. `apply_rules`
byte-emit path is byte-identical to v0.5.7 via golden parity tests.)
```

---

## 11. Stale forward-pointer enumeration

Memory `feedback_stale_dead_code_reason_drift`: v0.6 cleanup-pass'i bu listede her satırı strip eder. Implementer'ın §12 group 8 commit'inden önce listeyi grep ile re-doğrulaması zorunlu.

### 11.1 `// reason: v0.5.5+ ...` annotations (strip)

| Lokasyon | Mevcut | v0.6 aksiyon |
|---|---|---|
| [app.rs:61](src/config_tui/app.rs#L61) | `// reason: written by h/l/Enter dispatch; v0.5.5+ wires the render-side` | STRIP |
| [app.rs:69](src/config_tui/app.rs#L69) | `// reason: written by Enter dispatch; v0.5.5+ ...` | STRIP |
| [app.rs:77](src/config_tui/app.rs#L77) | (aynı pattern) | STRIP |
| [app.rs:105](src/config_tui/app.rs#L105) | `// reason: v0.5.5+ "Discard edits and reload" Confirm path; tag declared` | KEEP — v0.6.1'e defer (apply_confirm scope) |
| [app.rs:111](src/config_tui/app.rs#L111) | `// reason: v0.5.5+ Shift+D init-from-dump Confirm path; tag declared` | KEEP — v0.6.1'e defer |
| [app.rs:159](src/config_tui/app.rs#L159) | `// reason: populated by v0.5.5+ recompile_preview body when the` | STRIP (populated artık) |
| [snapshot.rs:7-8](src/config_tui/snapshot.rs#L7-L8) | `// reason: ParsedConfigView typed view consumed by v0.6+ live-preview` | STRIP (consumer landed) |
| [edit.rs:8-9](src/config_tui/edit.rs#L8-L9) | `// by TUI state machine; construction wires land in v0.6+ when non-UserConfig` | STRIP (wires landed) |
| [debounce.rs:17](src/config_tui/debounce.rs#L17) | `// editor (currently a v0.6+ Toast::warn stub on the `e` key) wires.` | STRIP (e editor landed) |

### 11.2 `Toast::warn("...v0.6+...")` stubs (strip / replace)

| Lokasyon | Mevcut | v0.6 aksiyon |
|---|---|---|
| [events.rs:84](src/config_tui/events.rs#L84) | `"color accepted (binding to selected rule lands in v0.6+)"` | REPLACE — D1 wire |
| [events.rs:121,:126](src/config_tui/events.rs#L121) | `"help overlay lands in v0.6+ (Modal::Help wiring deferred)"` | REPLACE — D4 wire |
| [events.rs:352](src/config_tui/events.rs#L352) | `"this action lands in v0.5.5+ (init-from-dump and discard-reload deferred)"` | KEEP — v0.6.1'e defer (apply_confirm scope) |
| [tabs/patterns.rs:113](src/config_tui/tabs/patterns.rs#L113) | `"new-pattern editor lands in v0.6+"` | REPLACE — D2 wire |
| [tabs/patterns.rs:121](src/config_tui/tabs/patterns.rs#L121) | `"inline regex source editor lands in v0.6+"` | REPLACE — D3 wire |
| [tabs/themes.rs:81](src/config_tui/tabs/themes.rs#L81) | (theme edit Toast) | KEEP — v0.6 themes tab edit out-of-scope |
| [tabs/profiles.rs:87](src/config_tui/tabs/profiles.rs#L87) | (profile edit Toast) | KEEP — v0.6 profiles tab edit out-of-scope |

### 11.3 Cargo.toml + CHANGELOG version bumps

[Cargo.toml] `version = "0.5.7"` → `"0.6.0"`. CHANGELOG `[0.6.0]` block (§10).

---

## 12. Logical group breakdown (plan phase hint)

Memory `feedback_lean_process_small_subversions` per-group lean review pattern:

| Group | Scope | Tahmini LOC | Reviewer focus |
|---|---|---|---|
| 1 | Preview foundation: `select_runs` extract + `apply_rules_spans` + `StyleSpan` + parity tests (§9.1 #1-2, #3-12) | ~300 | Rust — refactor correctness, lifetime/borrow, parity gate |
| 2 | TUI preview render: `Style::to_ratatui` + app.rs Preview field + events.rs recompile_preview + widgets/preview.rs span render + initial-compute boot + §9.2 + §9.6 + integration_tui_preview test | ~300 | TUI — Line/Span construction, UTF-8 boundary, frame.buffer assertion |
| 3 | compile_pending + PendingEdits expansion: `src/config_tui/compile_pending.rs` + edit.rs variants + CompileError + §9.3 + §9.4 | ~250 | Rust — error path threading, snapshot reconstruction |
| 4 | D1 ColorPicker → rule binding + apply_pending_and_recompile + §9.5 #31-32 | ~120 | TUI — focus stealing, recompile path |
| 5 | D2 Modal::NewPattern nested-state modal + ColorPicker inline phase + §9.5 #33-37 | ~350 | TUI — state machine transitions, validate paths |
| 6 | D3 Modal::EditRegex + debouncer wire + §9.5 #38-39 + §9.9 bench (decision-pending) | ~250 | TUI — debouncer interaction, validate buffer |
| 7 | D4 Modal::Help + HELP_MODAL_CONTENT const + §9.5 #40 + integration_tui_help_modal test | ~150 | TUI — overlay render, dismiss path |
| 8 | Stale forward-pointer strip (§11) + integration_tui_editor + CHANGELOG + version bump | ~50 | Cross-cutting (final review prep) |

**Group bundle disiplini:** Her group atomic commit. Group 4 D1'in incremental size'ı küçük (~120) — Group 5 D2 ile bundle ETMEZ çünkü D1 D2'nin precondition'ı (D2 Style phase ColorPicker inline kullanır → D1'in ColorPicker accept handler'ı önce çalışmalı).

---

## 13. Spec-phase parallel review absorption (post-review fold-or-defer)

Memory `feedback_spec_phase_parallel_review`: bu spec rev1 yazıldıktan sonra iki opus 4.7 senior reviewer parallel dispatch edilir:

- **Rust senior:** `select_runs` extract correctness, lifetime + borrow rules, StyleSpan API stability, ArcSwap snapshot lifecycle, capture-group merge invariant, `compile_pending` error path threading, PendingEdits state machine soundness, single-source-of-truth invariant (memory `feedback_parallel_call_site_invariant_audit`).
- **TUI/ratatui senior:** Line/Span construction, UTF-8 boundary safety in `spans_to_line`, frame.buffer assertion patterns, debouncer interaction with Modal::EditRegex, preview ↔ events.rs ↔ widgets/preview.rs dataflow, Modal nested-phase architecture correctness, focus stealing + Esc paths, Help modal accessibility (color contrast, no flash), Toast::warn strip enumeration completeness.

**Format:** Her reviewer §13.1/§13.2'ye severity-coded (🔴 BLOCKER / 🟡 IMPORTANT / 🔵 NIT) bulgu yazar. Spec implementer rev2 öncesi her bulguyu FOLD / DEFER / DROP olarak işaretler.

### 13.1 Rust senior findings (post-review filled)

_(post-review filled — TBD)_

### 13.2 TUI/ratatui senior findings (post-review filled)

_(post-review filled — TBD)_

---

## 14. Open questions + defer items + decisions to pin

### 14.1 Pinned decisions (in spec body)

1. **Modal::NewPattern architecture:** Nested-phase (`Modal::NewPattern { phase, draft }`), not flat variants. §6.2.
2. **`compile_pending` placement:** `src/config_tui/compile_pending.rs` yeni modül; rules.rs DOKUNULMAZ touch yok. §4.1.
3. **Help modal content:** Static const `HELP_MODAL_CONTENT`. Spec §6.4 + §14.4'te audit checklist mandate.
4. **Debouncer interval:** ms300 default; §14.5 bench follow-up gerekirse v0.6.1.
5. **PendingEdits clear policy:** Commit append; Save success clear; Reload clear; Save failure unchanged. §8.2.
6. **All modal text English** (memory `feedback_review_calibration_en_tr`); spec/comments Türkçe.
7. **`StyleSpan`** owned `Style` (Copy), byte offsets, pub(crate) struct + fields.
8. **ColorPicker invoke in NewPattern.Style phase:** Inline (pure-function render), not modal-stack. §7.3.
9. **Stale-index in PendingEdits:** Sequential apply against boot snapshot; multi-edit composability deferred to v0.7+. §8.1.

### 14.2 Defer items (v0.6.1 ve sonrası)

- v0.6.1: TUI polish — Shift+D init dump, V alias, search filter list-side, save-diff scroll, apply_confirm DiscardEditsAndReload + InitFromDump real impl, bool-axes-clear tri-state.
- v0.7: http opt-in profile + multi-profile composition + render snapshot tests + audit follow-up tests (μs Latin-1, URL trim, ipv4 5-segment, FP C-4/C-8/C-9 reconsider).
- v0.7.x: reconcile.rs file split if grown >1000 LOC after v0.6.1.

### 14.3 Open questions for implementation-phase decision

1. **`derive_user_rules_from_snapshot`** signature ve approach: Compiled'dan UserRule'ları reconstruct etme stratejisi. Yedek seçenek: App boot'ta config'ten UserRule listesini saklamak (`app.user_rules: Vec<UserRule>`) — Compiled snapshot'a güvenmemek. Plan phase'de pin.
2. **`compile_pending` perf:** 12 builtin + 5 user rule snapshot + 1 ModifyPattern → RegexSet rebuild süresi. Bench yoksa baseline yok. Decision §14.5.
3. **Modal::EditRegex debouncer kullanım:** Buffer her keystroke'da tickle mı, yoksa tick interval'ı sample-edit ile paylaşıyor mu? Plan phase'de pin (TUI senior review eder).

### 14.4 Help modal audit checklist (gelecek implementer'lar için)

When adding a new keystroke handler in `events.rs`:
1. Update `HELP_MODAL_CONTENT` const in `events.rs` (or wherever the const lives).
2. Add a test entry in `integration_tui_help_modal.rs` asserting the new keystroke line appears.
3. Final cross-cutting reviewer checks: `git diff` shows both `events.rs` handler AND `HELP_MODAL_CONTENT` update.

### 14.5 Bench follow-up decision

Eğer `compile_pending` incremental compile >5ms cold cache (12 builtin + 5 user rule + 1 ModifyPattern), implementer hemen plan'a `benches/preview_compile.rs` bench ekler. Eğer >20ms, debouncer interval ms500'e çıkar veya incremental-compile (RegexSet rebuild skip if rules.len() unchanged) v0.6.1'e itelenir.

---

## 15. Memory mandates audit checklist (final cross-cutting prep)

Implementer her group commit'inden önce ve final cross-cutting öncesi bu listeyi grep ile gate'ler:

- [ ] `feedback_consume_prior_review`: §1'de tüm v0.5.6 F1 NIT + v0.5.5 carryover'lar fold-or-defer enumerate (✅ §1).
- [ ] `feedback_spec_phase_parallel_review`: §13 Rust + TUI senior post-review absorpsion tamamlandı.
- [ ] `feedback_cross_cutting_review_value`: final opus 4.7 v0.5.7..v0.6 diff review (~1500-2000 LOC); reviewer mandate'ine per-group audit log + checklist eklenir.
- [ ] `feedback_lean_process_small_subversions`: BIG FULL ceremony — group bundle disiplini § 12'de korunmuş; her group lean review.
- [ ] `feedback_test_assertion_specificity`: §9.3 #26 (`CompileError` Display) byte-pinned `assert_eq!`; §9.6 #41-45 (TUI preview render) frame.buffer cell-pinned assertions; loose `.contains()` yok.
- [ ] `feedback_duplicate_formatter_audit`: `grep -nE 'format!.*rule "|format!.*message: format!' src/{pipeline,config_tui,style}.rs` zero hit.
- [ ] `feedback_parallel_call_site_invariant_audit`: `select_runs` tek source of truth — invariant audit table'da `apply_rules` + `apply_rules_spans` ikisi de aynı helper'a delegate ediyor.
- [ ] `feedback_review_calibration_en_tr`: Tüm modal text + HELP_MODAL_CONTENT İngilizce; spec Türkçe; identifier'lar İngilizce.
- [ ] `feedback_stale_dead_code_reason_drift`: §11 strip listesi her satır tick'lenmiş; `git grep "v0\.5\.5+\|v0\.6+" src/config_tui/` final = §11.1/§11.2 KEEP listesi ile birebir match.
- [ ] `feedback_collision_pin_pattern`: v0.6 yeni collision pin'i yok (sadece editing); existing `_yields_to_*_v0_X_Y_limitation` pin'leri unchanged.
- [ ] `feedback_enumerate_tests_for_invariant_claims`: §9 invariants iddialarına test isimleri eşleştirildi (parity gate #1-#2, capture-group #7, UTF-8 #9, perf #11 priority extremes).
- [ ] `feedback_reload_precedence_snapshot`: PendingEdits + reload precedence (§8.2 Ctrl+R reload PendingEdits.clear() — `reload.rs` precedence chain'ine yeni input olarak app.pending_edits eklenmemeli; snapshot pattern korunmuş).
- [ ] `feedback_toml_edit_025_quirks`: `compile_pending` toml_edit'e dokunmaz; reconcile.rs unchanged.
- [ ] `feedback_parallel_session_scope`: this spec written from single session; no parallel session conflict expected during implementation.
- [ ] DOKUNULMAZ audit: `git diff --name-only v0.5.7..HEAD | grep -E 'src/(pty|io_loop|tty_guard|signals|runtime|rules)\.rs'` = empty; `src/pipeline.rs` touch surface = §2.3 byte-bounded list.
- [ ] CLAUDE.md §3 security: `apply_rules_spans` no new attack surface (no I/O, no shell-out, no config-eval); `compile_pending` `Regex::new` standard validate path (ReDoS bench inherited from existing patterns).
- [ ] CLAUDE.md §4 zero-tech-debt: no `// TODO` introduced without issue ref; no commented-out code; CHANGELOG `[0.6.0]` block landed in same commit cycle.

---

**Bu spec rev1.** Parallel reviewer dispatch sonrası rev2 ile §13 doldurulur, sonra writing-plans skill ile plan üretilir.
