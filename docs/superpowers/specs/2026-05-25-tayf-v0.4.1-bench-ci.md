# tayf v0.4.1 — Bench-CI + Regression Detection + v0.3.7 Carryover Cleanup

**Tarih:** 2026-05-25
**Kapsam:** v0.4 minor'ünün ikinci ve son sub-version'ı. Üç ortogonal iş: (1) `Compiled.set` RegexSet pre-filter v0.4.0'da nakde çevrildi — şimdi her PR'da `cargo bench` otomatik çalışıp baseline'a karşı %20+ regression varsa workflow annotation; (2) v0.3.7 → v0.4.0 → v0.4.1 carryover zincirinin son halkası, `src/rules.rs:964` ve `:993`'teki bare `unreachable!()` arm'lar → `unreachable!("reason")` per CLAUDE.md §2; (3) bench baseline'ı git'te canonical artifact (`benches/baselines/<version>/<os>.json`), release ceremony'ye baseline write step entegre. v0.4 minor temiz kapanır; v0.5 brainstorm carryover taşımaz.
**Umbrella:** `docs/superpowers/specs/2026-05-24-tayf-v0.3.6-v0.4-vision.md` §3.3. Umbrella'nın §3.3'teki üç tercih (a/b/c) içinden **(b) `estimates.json` artifact parsing**'i normative yapar — tool-install gerektirmez, GHA action'larında `jq` ile okunur.
**Prereqs:** v0.4.0 yayınlanmış (✅ shipped 2026-05-25, tag `v0.4.0`, SHA `c0eb945`). v0.4.0 final cross-cutting review (`docs/superpowers/reviews/2026-05-25-rust-senior-v0.4.0-final-cross-cutting-review.md`) CARRYOVER_FINDINGS — C-1 + 4 Recommendations bu spec'in §2'sine fold edildi (memory `feedback_consume_prior_review` mandatesi).

---

## 1. Motivasyon

**Bench regression detection neden v0.4.1.** v0.3.5 → v0.4.0 boyunca her sub-version'da BASELINE.md güncellendi ve per-group floor (`apply_rules/*` >%5 review-gate, >%20 release-block; `passthrough/write_all` >%25 review-gate) elle uygulandı. v0.4.0 captures-heavy +7.93% regression'ını şu süreçle yakaladım: spec yazımı sırasında `cargo bench` elle çalıştırdım, sayıları gördüm, profile dispatch ile root-cause buldum, ship + document tradeoff'a karar verildi. **Bu süreç bir insanın elle bench çalıştırıp sayıları okumasına bağlı.** PR review akışında, açan kişi de reviewer da bench çalıştırmazsa regression CI yeşil geçer. v0.3.6 review pattern'i bunu öngördü; v0.4.0 captures-heavy'yi tesadüfen yakaladık (final cross-cutting review zorunluğu sayesinde). Sürdürülebilir değil.

**Çözüm:** her PR'da CI bench'i otomatik çalıştırır, yeni rakamları repo'da canonical olarak duran v0.4.0 baseline'a karşı karşılaştırır, threshold breach varsa workflow annotation üretir (PR UI'da inline warning'ler). PR'ı durdurmaz (sub-µs jitter koruması; `passthrough/write_all` v0.3.2 → v0.3.3'te %9.6 noise dalgalandı — false positive auto-fail kabul edilemez). Kullanıcı isterse `[bench-ci-strict]` PR label'i yapıştırarak threshold breach'i auto-fail'a yükseltir (opt-in escalation).

**C-1 carryover neden v0.4.1.** v0.3.7 final cross-cutting review N-4 (bare `unreachable!()` → `unreachable!("reason")` per CLAUDE.md §2) v0.4.0 cycle'a recommended fold edildi ama silently dropped — v0.4.0 final cross-cutting review bunu C-1 olarak tekrar gündeme aldı. Üç sub-version'lık carryover zinciri (v0.3.7 → v0.4.0 → v0.4.1) bir tane fazla; v0.4.1 cleanly closes. ~3 LOC mekanik iş; atomic warm-up commit, v0.4.0 Fix Z pattern'i.

**Baseline storage neden git-canonical.** Şu an `benches/BASELINE.md` human-readable summary; CI workflow'unun karşılaştırabileceği machine-readable rakam yok. `cargo-criterion` install opsiyonu (umbrella §3.3 (a)) tool install + version pinning getirir; `estimates.json` parsing (umbrella §3.3 (b)) `jq` ile direkt okunur, GHA runner'larda `jq` zaten var (ubuntu-latest 1.7, macos-latest 1.8.1 — preinstalled). Umbrella §3.3 (b) `change/` subdir'inden bahsediyor; spec `new/` subdir kullanır — git-canonical baseline tuttuğumuz için `--save-baseline` çağırmıyoruz, dolayısıyla `change/` üretilmiyor (§10'da divergence olarak explicit; rev2 senior review I-3). Repo'da canonical baseline = `benches/baselines/<version>/<os>.json` (per-OS, çünkü ubuntu-latest ile macos-latest absolute numbers tamamen farklı; like-for-like için her OS kendi baseline'ını taşır).

---

## 2. Değişiklik envanteri

### 2.1 Fix C — `src/rules.rs:964` + `:993` bare `unreachable!()` → `unreachable!("reason")`

**Mevcut ([src/rules.rs:964](src/rules.rs#L964)):**
```rust
RuleSource::Builtin => unreachable!(),
```

**Mevcut ([src/rules.rs:993](src/rules.rs#L993)):**
```rust
RuleSource::Builtin => unreachable!(),
```

**Yeni (her ikisi de aynı pattern, sibling arm'ın doc-comment'inden alıntı):**

`:964` (KeyMalformed grammar check sonrası):
```rust
RuleSource::Builtin => unreachable!(
    "Builtin rules ship with grammar-valid styles keys (validated at \
     constructor time via builtin_rules()); this arm is reachable only \
     through UserConfig/Theme paths handled above."
),
```

`:993` (OutOfRange capture-group index check sonrası):
```rust
RuleSource::Builtin => unreachable!(
    "Builtin rules ship with capture-group indices < captures_len \
     (validated at constructor time via builtin_rules()); this arm is \
     reachable only through UserConfig/Theme paths handled above."
),
```

**Kontrat:** behavior değişmez (her iki arm halen `unreachable!`, hot path girmiyor). CLAUDE.md §2 mandate "library code'da `unreachable!("reason")` reason explanation gerekir" karşılanır. Sibling arm `:938-942` (ZeroForbidden) zaten uzun-form Reason taşıyor — bu iki arm da ona uyumlu olur.

**Niçin v0.4.1 ilk commit:** atomic, hot path'a sıfır dokunma, byte-identical compile output. v0.4.0 Fix Z pattern'i. Risk-free warm-up.

### 2.2 `benches/baselines/<version>/<os>.json` — hand-curated machine-readable snapshot

**Schema:**
```json
{
  "version": "v0.4.0",
  "tag": "v0.4.0",
  "commit": "c0eb945",
  "date": "2026-05-25",
  "host": "GitHub Actions ubuntu-latest",
  "runner_class": "ubuntu-latest",
  "rustc": "1.95.0",
  "criterion_version": "0.8",
  "benches": {
    "apply_rules/ipv4-heavy": {
      "mean_ns": <CI-MEASURED>,
      "std_dev_ns": <CI-MEASURED>,
      "sample_count": 100
    },
    "apply_rules/mixed-syslog":   { "mean_ns": <CI-MEASURED>, "std_dev_ns": <CI-MEASURED>, "sample_count": 100 },
    "apply_rules/captures-heavy": { "mean_ns": <CI-MEASURED>, "std_dev_ns": <CI-MEASURED>, "sample_count": 100 },
    "passthrough/write_all":      { "mean_ns": <CI-MEASURED>, "std_dev_ns": <CI-MEASURED>, "sample_count": 100 }
  }
}
```

**İki dosya v0.4.0 için:**
- `benches/baselines/v0.4.0/ubuntu-latest.json`
- `benches/baselines/v0.4.0/macos-latest.json`

**`benches/baselines/latest/`** sembolik link veya copy: `latest/ubuntu-latest.json` + `latest/macos-latest.json`. CI workflow'u her zaman `latest/<runner-os>.json` okur.

**Init bootstrap problemi:** v0.4.0 baseline numbers şu an sadece M2 Pro local'da var (`benches/BASELINE.md`'deki numbers). CI baseline farklı — ubuntu/macos GHA shared hosts'lar farklı timing verir. v0.4.1 spec'i bunu çözer: **v0.4.1 implementation plan'ının ilk adımları:**

1. Geçici: `v0.4.0/<os>.json` dosyalarını **placeholder** olarak commit et (sample_count: 0, mean_ns: 0) — sırf CI workflow'un YAML schema'sını test etmek için.
2. CI workflow PR'da bench çalıştır → CI artifact'ından gerçek baseline numbers'ı çıkar.
3. Bu numbers'ı `v0.4.0/<os>.json`'a yaz (gerçek values), commit.
4. v0.4.1 ship sonrası, v0.4.1 release ceremony'sinde aynı şekilde `v0.4.1/<os>.json` baseline write commit.

Bu bootstrap detail'ı plan'da spelled out. Spec sadece final shape'i pin'liyor.

### 2.3 `.github/workflows/ci.yml` — `bench-regression` job

**Mevcut workflow** (`.github/workflows/ci.yml`): `test` (ubuntu + macos matrix) + `audit` (ubuntu only). Yeni `bench-regression` job aynı dosyaya eklenir (cohesion).

**Yeni job yapısı (rev2 — I-1, I-2, I-4, N-1, N-3 applied):**

```yaml
  bench-regression:
    name: Bench regression (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    needs: test  # only run if test passes (don't bench broken code)
    strategy:
      fail-fast: false  # ubuntu + macos independent
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      # jq is preinstalled on ubuntu-latest (1.7) and macos-latest (1.8.1)
      # per actions/runner-images. No install step needed (rev2 N-1).

      - name: Run benches
        run: cargo bench --bench throughput

      - name: Smoke test estimates.json + baseline JSON shape
        shell: bash
        run: |
          set -euo pipefail
          for bench in apply_rules/ipv4-heavy apply_rules/mixed-syslog apply_rules/captures-heavy passthrough/write_all; do
            file="target/criterion/${bench}/new/estimates.json"
            test -f "$file" || { echo "::error::missing $file"; exit 1; }
            jq -e '.mean.point_estimate' "$file" > /dev/null || {
              echo "::error::unexpected schema in $file (mean.point_estimate missing)"; exit 1
            }
          done

          # rev2 N-3: also smoke-test baseline JSON shape when it exists
          baseline="benches/baselines/latest/${{ matrix.os }}.json"
          if [[ -f "$baseline" ]]; then
            for bench in apply_rules/ipv4-heavy apply_rules/mixed-syslog apply_rules/captures-heavy passthrough/write_all; do
              jq -e --arg b "$bench" '.benches[$b].mean_ns' "$baseline" > /dev/null || {
                echo "::error::baseline $baseline missing benches[\"$bench\"].mean_ns"; exit 1
              }
            done
          fi

      - name: Compare against baseline
        id: compare
        shell: bash
        env:
          LC_NUMERIC: C  # rev2 I-2: pin numeric locale to avoid TR-style decimal separator
          STRICT_MODE: ${{ contains(github.event.pull_request.labels.*.name, 'bench-ci-strict') }}
        run: |
          set -euo pipefail
          baseline="benches/baselines/latest/${{ matrix.os }}.json"
          if [[ ! -f "$baseline" ]]; then
            echo "::warning::no baseline at $baseline; skipping comparison"
            exit 0
          fi

          breach_found=0
          for bench in apply_rules/ipv4-heavy apply_rules/mixed-syslog apply_rules/captures-heavy passthrough/write_all; do
            # rev2 I-1: jq -e fails loudly if field missing or null
            new_mean=$(jq -e '.mean.point_estimate' "target/criterion/${bench}/new/estimates.json") || {
              echo "::error::failed to read mean.point_estimate from ${bench}'s estimates.json"; exit 1
            }
            base_mean=$(jq -e --arg b "$bench" '.benches[$b].mean_ns' "$baseline") || {
              echo "::error::baseline $baseline missing benches[\"$bench\"].mean_ns"; exit 1
            }

            # Bootstrap baseline-zero guard: skip comparison if baseline is placeholder
            if [[ "$base_mean" == "0" ]]; then
              echo "::warning::baseline for $bench is placeholder (mean_ns=0); skipping comparison"
              continue
            fi

            # Threshold: apply_rules/* > 20% slower; passthrough/write_all > 30% (sub-µs jitter band)
            if [[ "$bench" == "passthrough/write_all" ]]; then
              threshold_pct=30
            else
              threshold_pct=20
            fi

            # rev2 I-2: single-pass awk for delta computation + breach decision
            # (eliminates bc — locale, scale=2 precision boundary, ubuntu-minimal availability).
            # awk uses raw f64; output: delta_pct + breach flag space-separated.
            read -r delta_pct breach < <(awk -v new="$new_mean" -v base="$base_mean" -v t="$threshold_pct" '
              BEGIN {
                d = (new - base) / base * 100
                printf "%.2f %d\n", d, (d > t) ? 1 : 0
              }')

            if [[ "$breach" == "1" ]]; then
              breach_found=1
              if [[ "$STRICT_MODE" == "true" ]]; then
                echo "::error::${bench} regressed +${delta_pct}% vs baseline (threshold: ${threshold_pct}%)"
              else
                echo "::warning::${bench} regressed +${delta_pct}% vs baseline (threshold: ${threshold_pct}%) — strict mode opt-in via [bench-ci-strict] label"
              fi
            fi
          done

          # Exit with failure only if strict + breach
          if [[ "$breach_found" == "1" && "$STRICT_MODE" == "true" ]]; then
            exit 1
          fi

      # rev2 I-4: upload criterion artifacts so reviewer can extract real
      # baseline numbers via jq script (eliminates manual ms/µs → ns
      # human-error in bootstrap). Retention 14 days = enough for the
      # bootstrap commit-back step in §6.2.
      - name: Upload criterion artifacts (for baseline extraction)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: criterion-${{ matrix.os }}
          path: target/criterion/
          retention-days: 14
```

**Trigger:** mevcut workflow'la aynı (`push: branches: [main]` + `pull_request: branches: [main]`). Bench job push + PR'da çalışır; main push'larında baseline write için gözlem amaçlı, PR'larda regression detection için canonical.

**Why `needs: test`:** broken code'da bench çalıştırmak zaman israfı. Test geçmeden bench çalışma.

**Runtime:** ~3-5 dk per OS × 2 OS paralel × ~3-5 dk gate = ~4-5 dk total wall clock. Mevcut CI ~1-2 dk; bench ekleyince ~5-7 dk. Kabul edilebilir.

### 2.4 Threshold contract

- `apply_rules/*`: **>%20 slower → annotation** (warning). Strict mode: **error → workflow auto-fail**.
- `passthrough/write_all`: **>%30 slower → annotation** (warning, sub-µs jitter band'in üstünde). Strict mode: **error → workflow auto-fail**.

Threshold'lar spec §6.3 per-group floor'undan **farklı**:
- Spec §6.3 floor: human-judgment review-gate (apply_rules/* >%5, passthrough >%25). Release-blocking thresholds (apply_rules/* >%20, passthrough yok).
- CI threshold: **CI's coarse single-number annotation rule** — sub-µs jitter koruması için release-block thresholds'a hizalı (apply_rules/* %20, passthrough %30 = release-block + jitter overhead).

İki katman ayrı policy:
- **CI annotation:** operasyonel uyarı, false-positive-tolerant (sub-µs jitter).
- **Spec §6.3 floor:** ship-time human judgment, finer-grained.

Umbrella §4.2 explicit'ti: "İki katman ayrı politika" — bu spec normative yapar.

### 2.5 Strict mode label opt-in

`[bench-ci-strict]` PR label'i yapıştırılırsa:
- Annotation severity warning → error.
- Workflow `exit 1` → CI red → PR auto-fail.

Yapıştırılmazsa: annotation warning, PR yeşil geçer (sadece reviewer dikkat çeker).

**Label name:** `bench-ci-strict` (kebab-case GHA convention). Mevcut tayf labelları yok; bu ilk label. README'de dokümante edilir mi? **HAYIR** — internal CI mechanism, kullanıcı-yüzü dokümantasyon değil. `.github/labels.yml` veya benzeri label-config dosyası **YOK** (premature; ilk label'da config gereksiz).

**Label creation (rev2 N-2):** v0.4.1 release ceremony §9 öncesi GitHub UI'dan manuel oluşturulur (repo > Settings > Labels > New label: `bench-ci-strict`, herhangi bir renk). İlk PR'da label yokken `contains(github.event.pull_request.labels.*.name, 'bench-ci-strict')` boş array'e karşı `false` döner — workflow patlamaz, opt-in implicit-off davranışı doğru.

### 2.6 `estimates.json` shape smoke test

`bench-regression` job'ın `Smoke test estimates.json shape` step'i (§2.3 YAML içinde). Criterion'un internal artifact path/schema'sı stabil ama undocumented; minor version bump'ta değişebilir. Smoke loud-fail garanti eder:
- Dosya yoksa → `::error` + exit.
- `mean.point_estimate` field yoksa → `::error` + exit.

Detection workflow level'da; downstream "compare" step zaten field'ı kullanır, fail oraya da yansır.

### 2.7 Release ceremony update — pre-tag baseline write

v0.4.0 release ceremony (memory `project_release_workflow`):
1. Pre-tag commits → push main → CI yeşil bekle.
2. Annotated tag push.
3. Post-tag CHANGELOG date follow-up + umbrella shipped marker + final cross-cutting review.

**v0.4.1 ile ekleme:** pre-tag commit zincirine bir yeni adım girer — **`bench: record vX.Y.Z/<os>.json baseline numbers`** commit (BASELINE.md update'iyle paralel, BASELINE.md ile aynı commit veya ayrı atomic). CI workflow latest baseline'ı bu commit sonrası okur.

Specifically:
- Pre-tag commit zincirinin BASELINE.md update commit'inin AYNISI ya da hemen ardından: `benches/baselines/<new-version>/<os>.json` yaz. `benches/baselines/latest/<os>.json` → `<new-version>/<os>.json` link/copy update.
- Tag push sonrası: `latest/` zaten yeni baseline'ı işaret ediyor, sonraki PR'lar artık ona göre compare ediyor.

**Bootstrap:** v0.4.1 implementation plan ilk PR'larında CI baseline yok (v0.4.0 baseline rakamları sadece M2 Pro local'da). Plan'da:
- Phase 1 step: skeleton CI job + placeholder `v0.4.0/<os>.json` (sıfır rakamlarla, smoke için).
- Phase 1 ilk PR push'unda CI bench gerçek rakamları üretir → artifact upload veya log'dan oku → manual commit `v0.4.0/<os>.json`'a real values + `latest/` link.
- Sonraki PR'lar canonical baseline'a karşı compare eder.

Bu bootstrap plan-time detail; spec sadece "release ceremony'ye baseline write step ekleniyor" kontratını pin'ler.

### 2.8 Memory entry — `feedback_review_calibration_en_tr`

**Yeni memory dosyası:** `/Users/bera/.claude/projects/-Users-bera-tayf/memory/feedback_review_calibration_en_tr.md`. Content:

```markdown
---
name: feedback-review-calibration-en-tr
description: EN/TR mismatch in code (not specs/conversation) = 🟡 minimum, never 🟢
metadata:
  type: feedback
---

When reviewing code, any English-vs-Turkish mismatch in identifiers, code comments, doc-comments, error messages, log strings, CLI help text, or test messages is **🟡 minimum severity, not 🟢**.

**Why:** v0.4.0 Task 3 reviewer flagged Turkish phrasing in a code comment (`src/pipeline.rs:65-67`) as 🟢 nice-to-have. Per CLAUDE.md §1 "catch and fix on sight", this required a separate `style:` cleanup commit (`d94b03b`). A 🟡 flag from the per-task reviewer would have folded the fix into the original commit, no follow-up needed.

**How to apply:** In every combined spec + code-quality review, flag any non-English text in code (NOT in spec/plan narrative or conversation) at 🟡 minimum. 🔴 if it ships in user-facing UX (error message, --help text, CLI output).
```

Update `MEMORY.md` index:
```
- [Reviewer calibration: EN/TR mismatch = 🟡](feedback_review_calibration_en_tr.md) — code-level (not spec) language mismatch never 🟢; CLAUDE.md §1 "catch and fix on sight" mandate
```

Bu memory v0.4.1 implementation plan'ın sonunda (ship sonrası) yazılır — code change değil process change.

---

## 3. CHANGELOG entry

```markdown
## [0.4.1] — TBD

### Added
- CI now runs `cargo bench --bench throughput` on every PR (ubuntu-
  latest + macos-latest matrix), compares results against canonical
  baseline JSON at `benches/baselines/latest/<os>.json`, and emits a
  workflow annotation when any of the four hot-path benches regresses
  past its threshold (`apply_rules/*` > +20%; `passthrough/write_all`
  > +30%, accounting for sub-µs jitter). Opt in by labeling the PR
  with `bench-ci-strict` to upgrade annotations to errors that fail
  the workflow. Baseline JSON is committed to the repo per release
  as part of the standard ceremony.

### Changed
- The two bare `unreachable!()` arms in
  `rules.rs::resolve_group_styles_for_rule` (the `RuleSource::Builtin`
  branches of the KeyMalformed and OutOfRange checks) now carry
  explanatory reason strings, per CLAUDE.md §2's "unreachable!()
  reason explanation" mandate. Behavior unchanged — these arms are
  constructor-guaranteed unreachable; the strings only surface if a
  future refactor breaks the invariant.
```

Date placeholder TBD; tag push sonrası ayrı `docs(changelog): v0.4.1 release date` commit'i.

---

## 4. Hedef olmayan

- **PR bot for comment posting** — workflow annotations zaten PR UI'da inline görünür, ekstra bot setup (GitHub token + write permission + comment dedup logic) gereksiz. Native annotation = UX yeter, no v0.5 carryover.
- **Baseline auto-regeneration on every commit** — release ceremony manual baseline write yeter; her commit'te baseline re-write CI tarihçesini "her commit kendi baseline'ı" haline getirir, regression detection anlamını kaybeder. Manual = kontrat.
- **Strict mode'un commit-level granularity'si** — sadece PR label opt-in yeter; commit message prefix veya per-file label gibi finer granularity YAGNI.
- **`cargo-criterion` install** — umbrella §3.3'te (a) opsiyonu olarak değerlendirildi, **iptal** edildi. `estimates.json` parsing (b) zaten tool-install gerektirmez, GHA `jq` yeter; (a) opsiyonu version pinning + cargo install cache pollution + JSON message schema dependency getirir, bizim için fazla.
- **`cargo bench` stdout parsing (c)** — umbrella §3.3'te zaten reddedildi.
- **macOS-specific bench fixture** — aynı `benches/throughput.rs` her iki OS'ta çalışır; OS-specific bench varyasyonu YAGNI.
- **Baseline diff format'ı için JSON schema dosyası** — `benches/baselines/<version>/<os>.json` shape spec'te tanımlı; ayrı JSON Schema dosyası YAGNI.
- **Bench history visualization / dashboard** — out of scope. Future v1.0+ if real demand.
- **Yeni Cargo dep** — `regex` / `criterion` / `jq` (sistem aracı) zaten mevcut. Memory `feedback_dependency_minimalism` + CLAUDE.md §3.

---

## 5. Test stratejisi

### 5.1 Zero-regression invariant

Hiçbir production kod değişimi yok (sadece §2.1 `unreachable!` mesajları + §2.3 CI workflow + §2.2 JSON dosyaları). `tests/integration_capture_groups.rs` + `cargo test --lib` byte-identical pass. Lib test count v0.4.0'dan değişmez (**410** baseline).

### 5.2 CI workflow self-testing

CI'ın kendisi test edilmesi gerekir — first PR'da workflow doğru çalışıyor mu? Bootstrap plan'ında:
1. v0.4.1 ilk PR push'unda workflow ilk kez tetiklenir. Sadece `test` job zaten geçmiş olmalı; `bench-regression` job ilk-fire'ı log inceleme.
2. Doğru bench output'u alıyor mu (`target/criterion/.../new/estimates.json` var mı, jq parse ediyor mu).
3. Compare step: baseline placeholder (sıfır rakamlar) ile karşılaştırma; sıfır rakamlara karşı her şey "regression" çıkar (% formula `(new - 0) / 0` → inf veya undefined). Workflow şu durumda annotation üretmemeli — baseline-zero guard ekle:

```bash
if [[ "$base_mean" == "0" || "$base_mean" == "null" ]]; then
  echo "::warning::baseline for $bench is placeholder (mean_ns=0); skipping comparison"
  continue
fi
```

4. Real baseline numbers `v0.4.0/<os>.json`'a yazılınca next PR canonical compare yapar.

Bootstrap plan'ında bu adımlar enumerate edilecek. Spec sadece "bootstrap edge case'i handle edilir" kontratını yazıyor.

### 5.3 Acceptance-gate noise

`integration_smoke::input_thread_joins_promptly_after_child_exit` v0.3.6 + v0.3.7 + v0.4.0'da deterministik M2 Pro local-only fail. v0.4.1 runtime/PTY/signals/line_buffer/tty_guard'a sıfır dokunma → memory `feedback_flaky_watch_test` disposition aynen geçerli. CI'a güven; `gh run rerun --failed` standart.

### 5.4 Pre-commit gate

CLAUDE.md komitmenti: `cargo fmt --all -- --check && cargo clippy --all-targets -- -D warnings && cargo test`. Subagent task'larının her birinde combined spec + code-quality review.

---

## 6. CI threshold + bootstrap kontratı

### 6.1 Threshold matrix (final)

| Bench | Warning (annotation) | Error (strict mode) | Spec §6.3 floor (info) |
|---|---|---|---|
| `apply_rules/ipv4-heavy` | >+20% | >+20% with strict | review-gate >+5%; release-block >+20% |
| `apply_rules/mixed-syslog` | >+20% | >+20% with strict | review-gate >+5%; release-block >+20% |
| `apply_rules/captures-heavy` | >+20% | >+20% with strict | review-gate >+5%; release-block >+20% |
| `passthrough/write_all` | >+30% | >+30% with strict | review-gate >+25%; release-block yok |

CI threshold'ı release-block'a hizalı (jitter overhead). Spec §6.3 floor finer-grained ship-time review.

### 6.2 Baseline bootstrap timeline

1. **v0.4.1 implementation Phase 1:** `benches/baselines/v0.4.0/ubuntu-latest.json` ve `macos-latest.json` placeholder olarak commit edilir (her bench: `"mean_ns": 0, "std_dev_ns": 0, "sample_count": 0`). `benches/baselines/latest/` → `v0.4.0/` link/copy.
2. **v0.4.1 PR ilk push'unda:** `bench-regression` job ilk kez çalışır, baseline-zero guard tetiklenir → annotation "baseline placeholder; skipping comparison". CI yeşil geçer. Artifact upload step (`actions/upload-artifact@v4`, §2.3 YAML son step) `target/criterion/` directory'sini `criterion-<os>` adıyla 14 gün retention'la upload eder.
3. **CI artifact'tan real numbers'ı çıkar (rev2 I-4 — manual ms/µs → ns conversion YASAK):**
   ```bash
   # Reviewer makinesinde:
   gh run download <run-id> -n criterion-ubuntu-latest -D /tmp/criterion-ubuntu
   gh run download <run-id> -n criterion-macos-latest -D /tmp/criterion-macos

   # For each OS, extract baseline JSON via jq (raw nanosecond f64, zero unit ambiguity):
   for os in ubuntu macos; do
     out="benches/baselines/v0.4.0/${os}-latest.json"
     jq -n --arg ver "v0.4.0" --arg tag "v0.4.0" --arg commit "c0eb945" \
       --arg date "2026-05-25" --arg os "${os}-latest" \
       --argjson ipv4_mean "$(jq '.mean.point_estimate' /tmp/criterion-${os}/apply_rules/ipv4-heavy/new/estimates.json)" \
       --argjson ipv4_sd "$(jq '.std_dev.point_estimate' /tmp/criterion-${os}/apply_rules/ipv4-heavy/new/estimates.json)" \
       --argjson syslog_mean "$(jq '.mean.point_estimate' /tmp/criterion-${os}/apply_rules/mixed-syslog/new/estimates.json)" \
       --argjson syslog_sd "$(jq '.std_dev.point_estimate' /tmp/criterion-${os}/apply_rules/mixed-syslog/new/estimates.json)" \
       --argjson cap_mean "$(jq '.mean.point_estimate' /tmp/criterion-${os}/apply_rules/captures-heavy/new/estimates.json)" \
       --argjson cap_sd "$(jq '.std_dev.point_estimate' /tmp/criterion-${os}/apply_rules/captures-heavy/new/estimates.json)" \
       --argjson pt_mean "$(jq '.mean.point_estimate' /tmp/criterion-${os}/passthrough/write_all/new/estimates.json)" \
       --argjson pt_sd "$(jq '.std_dev.point_estimate' /tmp/criterion-${os}/passthrough/write_all/new/estimates.json)" \
       '{
         version: $ver, tag: $tag, commit: $commit, date: $date, host: $os,
         runner_class: $os, rustc: "1.95.0", criterion_version: "0.8",
         benches: {
           "apply_rules/ipv4-heavy":     { mean_ns: $ipv4_mean,   std_dev_ns: $ipv4_sd,   sample_count: 100 },
           "apply_rules/mixed-syslog":   { mean_ns: $syslog_mean, std_dev_ns: $syslog_sd, sample_count: 100 },
           "apply_rules/captures-heavy": { mean_ns: $cap_mean,    std_dev_ns: $cap_sd,    sample_count: 100 },
           "passthrough/write_all":      { mean_ns: $pt_mean,     std_dev_ns: $pt_sd,     sample_count: 100 }
         }
       }' > "$out"
   done
   # Commit + push the two real-numbers JSON files.
   ```
   Manuel ms/µs → ns conversion **YASAK** — jq script raw nanosecond f64 değerlerini criterion `estimates.json`'dan direkt okur.
4. **Sonraki PR push'unda:** canonical baseline aktif. Her bench için real %delta hesaplanır.
5. **v0.4.1 release ceremony:** pre-tag baseline write commit zincirinin parçası — `bench: record v0.4.1/<os>.json baseline numbers from CI` (aynı jq script kullanılır, sadece `$ver/$tag/$commit/$date` argumanları update). Tag push sonrası `latest/` yeni baseline.

Plan bu bootstrap'ı 6-7 step olarak enumerate eder.

### 6.3 Threshold breach disposition (CI'da)

Workflow annotation aşağıdaki shape:
- Non-strict: `::warning::apply_rules/captures-heavy regressed +28.4% vs baseline (threshold: 20%) — strict mode opt-in via [bench-ci-strict] label`
- Strict: `::error::apply_rules/captures-heavy regressed +28.4% vs baseline (threshold: 20%)` + workflow `exit 1`

PR reviewer iki şekilde yanıt verir:
1. **Annotation seen, kabul edilebilir tradeoff:** PR merge; eğer regression intentional ise release ceremony'de baseline güncellenir.
2. **Annotation seen, tradeoff kabul edilemez:** PR fix until threshold under. Veya `[bench-ci-strict]` label ile gate hard fail.

`[bench-ci-strict]` label tipik use case: kod patch'i bench-critical bir path'a dokunuyorsa label'la, reviewer regression kaçırmasın.

---

## 7. Risk değerlendirmesi

| Risk | Olasılık | Etki | Azaltma |
|---|---|---|---|
| `estimates.json` shape criterion minor bump'ta değişir | Düşük (regression detection bu shape'i monitör eder) | Orta (CI workflow silently green) | §2.6 smoke test loud-fail; criterion version pinning Cargo.lock'ta zaten |
| CI bench runtime PR feedback delay'i yaratır (~5 dk eklenir) | Orta | Düşük (acceptable trade-off for regression coverage) | `needs: test` ile bench job test-pass sonrası çalışır; broken code'da bench bypass'lanır |
| ubuntu-latest ile macos-latest GHA shared host noise farkı false positive üretir | Orta | Düşük (annotation only; auto-fail yok) | Threshold genişliği (apply_rules %20, passthrough %30) jitter band'inin üstünde; strict mode opt-in |
| Bootstrap baseline placeholder zero'lar prod baseline'la karıştırılır | Düşük | Düşük (workflow baseline-zero guard skip eder) | §5.2 bootstrap edge case; first PR sonrası real numbers commit'lenir |
| Strict mode label yapıştırılınca beklenmedik false positive PR'ı engeller | Orta | Düşük (kullanıcı kendi yapıştırdı, opt-in conscious choice) | Annotation message strict-mode'u opt-out yöntemini söyler |
| macOS GHA shared host'ta absolute timing ubuntu'dan tamamen farklı → baseline tutarsız | Yüksek (gerçek) | Düşük (per-OS baseline files zaten ayrı) | §2.2 `<os>.json` per-OS storage; like-for-like compare |
| C-1 cleanup behavior'ı değiştirir | Çok düşük | Yok | `unreachable!` arm hot path'a girmiyor, sadece message string değişir |
| Cross-cutting review'da kaçırılan başka latent bug | Orta | Düşük | v0.4.1 final cross-cutting opus 4.7 review zorunlu (memory `feedback_cross_cutting_review_value` + `feedback_consume_prior_review`) |

---

## 8. Acceptance criteria

v0.4.1 ship için:

1. `cargo fmt --all -- --check` clean.
2. `cargo clippy --all-targets -- -D warnings` clean.
3. `cargo test --lib` tüm test'ler yeşil; total count v0.4.0'dan değişmez (**410**; C-1 fix behavior'ı değiştirmez, yeni test yok).
4. `cargo test --test integration_capture_groups` 100% pass, byte-identical to v0.4.0.
5. `cargo audit && cargo deny check` clean.
6. `.github/workflows/ci.yml` `bench-regression` job exists; valid YAML; matrix ubuntu-latest + macos-latest.
7a. `benches/baselines/v0.4.0/ubuntu-latest.json` ve `macos-latest.json` exist; ya placeholder (her bench için `mean_ns: 0`, `std_dev_ns: 0`, `sample_count: 0`) ya real (CI-measured non-zero); schema §2.2 ile uyumlu. **Placeholder OK during implementation Phase 1 (bootstrap edge case).**
7b. **Pre-tag release ceremony tamamlandıktan sonra (yani §9.3 tag push'tan önce), her iki dosya real (non-placeholder, `mean_ns > 0`) values içerir. Bootstrap commit-back (§6.2 step 3) tag push öncesi gerçekleşmiş olmalı.**
8. `benches/baselines/latest/ubuntu-latest.json` ve `macos-latest.json` exist as `v0.4.0/` copy (placeholder veya real, AC 7a/7b ile aynı durumda).
9. Ilk PR'da `bench-regression` job ubuntu-latest **ve** macos-latest paralel run, ikisi de yeşil (no real regression v0.4.1 değişimlerinden).
10. **Mekanik regression guard:** `grep -nP '^\s*RuleSource::Builtin =>\s*unreachable!\(\),' src/rules.rs` → empty (her iki arm artık reason taşıyor).
11. CHANGELOG.md `## [0.4.1] — TBD` entry'si "Added" + "Changed" sections ile mevcut.
12. `Cargo.toml` version bump `0.4.0` → `0.4.1`; `Cargo.lock` tayf entry'si paralel.

---

## 9. Release ceremony

Memory `tayf release workflow` + v0.4.0 precedent + v0.4.1 yeni ekleme:

1. **Pre-tag commits** (main'e, sırayla):
   - **Commit 2.** `refactor(rules): document Builtin unreachable! arms in resolve_group_styles_for_rule` — Fix C (§2.1). Atomic warm-up.
   - **Commit 3.** `ci: bench-regression workflow + benches/baselines scaffolding` — §2.3 workflow job + §2.2 schema + bootstrap placeholders.
   - **Commit 4.** (CI bootstrap; first PR sonrası, real numbers obtain edilince) `bench: record v0.4.0 CI baseline numbers (ubuntu-latest + macos-latest)` — gerçek bootstrap commit.
   - **Commit 5.** `docs(changelog): v0.4.1 entry — TBD` — CHANGELOG entry.
   - **Commit 6.** `chore(release): bump version to 0.4.1` — version bump.

2. **Push main** → CI yeşil bekle (yeni `bench-regression` job da yeşil olmalı; bootstrap edge case'i handle edilmiş). macOS GHA flake olursa `gh run rerun --failed`.

3. **Annotated tag push:** `git tag -a v0.4.1 -m "tayf v0.4.1 — bench-CI + regression detection"`, `git push origin v0.4.1`.

4. **Pre-tag baseline write** (yeni adım, v0.4.1 ile başlar): `bench: record v0.4.1 baseline numbers (ubuntu-latest + macos-latest)` commit pre-tag chain'ine eklenir (Commit 4'ün ardı, ama isim/sırada place flexibility). `benches/baselines/v0.4.1/<os>.json` + `latest/<os>.json` update.

5. **CHANGELOG date follow-up commit:** `docs(changelog): v0.4.1 release date`.

6. **Umbrella vision shipped marker:** `docs/superpowers/specs/2026-05-24-tayf-v0.3.6-v0.4-vision.md` §2 tablosundaki v0.4.1 row'unu ✅ shipped'e çevir + v0.4 minor için "fully shipped, v0.5 next" notu.

7. **Memory entry write:** `feedback_review_calibration_en_tr.md` — §2.8.

8. **Final cross-cutting opus 4.7 review** (zorunlu, memory `feedback_cross_cutting_review_value` + `feedback_consume_prior_review`): `docs/superpowers/reviews/2026-MM-DD-rust-senior-v0.4.1-final-cross-cutting-review.md`. CLEAN_SHIP target (no carryover; v0.5 temiz başlasın diye).

---

## 10. Açık sorular

Yok. Brainstorm + prior-review-consumption mandatesi sonrası tüm kararlar finalize:

- **Fold-or-defer disposition (v0.4.0 cross-cutting review):** §1 motivasyon + §2.1 (C-1) + §2.5 (strict mode label) + §2.8 (memory entry); HONORED tüm 5 madde.
- **Storage shape:** hand-curated `benches/baselines/<version>/<os>.json` (umbrella §3.3 (b) `estimates.json` parsing pattern).
- **Criterion artifact subdir (rev2 — review I-3):** umbrella §3.3 (b) `target/criterion/<bench>/<name>/change/estimates.json` der; bu spec git-canonical baseline tutar (criterion `--save-baseline` çağırmaz), dolayısıyla `change/` dir bench-run'da üretilmiyor — spec `target/criterion/<bench>/<name>/new/estimates.json` okur (absolute estimate, delta'yı kendisi hesaplar). Umbrella §7 "spec wins on divergence, must explicitly note" gereği bu bullet explicit.
- **Threshold matrix:** spec §6.1 final.
- **Strict mode opt-in:** `[bench-ci-strict]` PR label, no commit-level granularity.
- **Bootstrap:** placeholder zero + baseline-zero guard + first-CI real-numbers extract + commit-back (plan-time detail).
- **macOS bench:** included (per-OS baseline files, like-for-like compare; ubuntu-only seçeneği reddedildi — CI vs local M2 Pro mismatch'i ortadan kaldırır).
- **PR comment bot:** OUT OF SCOPE (annotation = UX yeter, no v0.5 carryover).
- **v0.5 hazırlığı:** v0.4.1 ship sonrası v0.5 brainstorm carryover-free başlar (capture-group naming + profile system + `tayf config` TUI, umbrella §1.2 deferred items).
