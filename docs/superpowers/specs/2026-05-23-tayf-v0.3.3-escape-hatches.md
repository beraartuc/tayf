# tayf v0.3.3 — Escape Hatches Tasarım Dokümanı

**Tarih:** 2026-05-23
**Kapsam:** v0.3.3 üç ortogonal "escape hatch" iş kalemini tek release'te ship eder. **(F1)** `--bypass` CLI flag'i + `TAYF_DISABLE` env var — whole-binary bypass; PTY wrap ve signal forwarding korunur, ama `Pipeline::apply_rules` çalışmaz ve `bg_detect::resolve()` skip edilir. Kullanıcının `.zshrc`'sinde `[[ -n "$TAYF_DISABLE" ]] || exec tayf` pattern'i için. **(F2)** `--no-hot-reload` CLI flag'i — file watcher + reload orchestrator thread'leri spawn edilmez; bağlı olarak v0.2.1'den beri yatan SIGHUP "silent sink" davranışı düzeltilir: `SIGHUP` her durumda (hot reload aktif olsun ya da olmasın) child process group'a forward edilir. **(F3)** Opt-in reload banner — yeni `[general] show_reload_banner = bool` (default `false`); true olduğunda başarılı `reload_once` sonrası `/dev/tty`'a tek satırlık dim metin (`tayf: config reloaded`) yazılır. Üç iş de additive, public API değişikliği yok (yeni CLI flag + yeni opsiyonel config field), tek davranış değişikliği SIGHUP'ta — explicit CHANGELOG notu.

**Prereqs:** v0.3.2 yayınlanmış (✅ shipped, commit `167806c`). `cargo audit && cargo deny check` yeşil. v0.3.1 hot-reload mimarisi (`src/reload.rs`, `src/watch.rs`, `src/signals.rs::spawn_handler`'ın `reload_tx` parametresi) yerinde. v0.3.2'de eklenen `bg_detect::env_truthy(name)` helper'ı (`src/bg_detect.rs:41-45`) F1 env-var parsing için yeniden kullanılabilir biçimde duruyor.

**v0.3 umbrella referansı:** `docs/superpowers/specs/2026-05-23-tayf-v0.3-vision.md` §3.3 (v0.3.3 scope satırı), §3.5 (umbrella §"v0.3.3 — Escape hatches" üç flag tanımı), §4 (cross-cutting kararlar — backward compat, perf, security, dep policy, test stratejisi, release workflow). Umbrella'da F3 default'unun (opt-in vs opt-out) ve F2'nin SIGHUP davranışının (yutulur mu forward mi) "sub-version brainstorming'de" denildiği yerlerin kararı bu spec'te pinleniyor (§2 Karar 5 + §2 Karar 8).

**Revizyon notu (rev1 — initial):** İlk taslak; opus 4.7 senior Rust reviewer turundan sonra rev2 düzenlenecek.

---

## 1. Vizyon

### 1.1 Kapsam F1 — `--bypass` CLI + `TAYF_DISABLE` env

Kullanıcılar tayf'ı PATH'in başına geçici olarak alıyor, sonra spesifik komutlar / spesifik shell session'ları için baypas etmek istiyor. Üç birincil kullanım hikayesi:

1. **`.zshrc` koşullu wrap:** `[[ -n "$TAYF_DISABLE" ]] || exec tayf` veya benzeri. Kullanıcı env var'ı set ederek (örn. CI session'unda, ya da debug için bir shell'de) tayf'ı baypas eder; başka shell'leri etkilemez.
2. **Tek komutluk override:** `TAYF_DISABLE=1 my-tool` — özellikle çıktısı tayf'ın pattern'lerinden biriyle çakışan bir alet için.
3. **Sorun raporlama:** Kullanıcı bir bug yaşadığında "tayf'ı geçici devre dışı bırak, davranış aynı mı?" diyebilmek için tek CLI flag.

Kritik davranış kontratı: **bypass aktifken tayf hâlâ PTY wrap ediyor olmalı.** Yani:

- Shell child process tayf'ın spawn ettiği PTY altında çalışır (terminal size, controlling terminal davranışı korunur).
- `signals.rs` thread'i `SIGWINCH`/`SIGINT`/`SIGTERM`/`SIGHUP` forwarding'ini yapmaya devam eder (yoksa child process group orphan kalır).
- `tty_guard::TtyGuard` raw mode + Drop restore davranışı korunur (yoksa kullanıcının terminal'i bypass mode'a girip exit ederken bozuk kalır).
- ANSI state machine (`src/ansi.rs::Parser`) çalışmaya devam eder — TUI mode detection + OSC string handling correctness için. Bypass mode'da pattern uygulama olmayacak ama `Pipeline.feed` ANSI sequence boundary'lerini yine de doğru tanımalı; aksi takdirde bypass mode "kontrolsüz dump" olur ve future feature'ları (örn. cross-line SGR tracking) bypass'ta görünmez olur.

**Bypass aktifken YOK olan:**
- `Pipeline::apply_rules` regex scan'i ve SGR enjeksiyonu. `runtime::run`'ın mevcut `apply_colors == false` fast path'i (raw passthrough; `src/lib.rs:173`) zaten Pipeline'ı tamamen baypas ediyor — bypass mode bu fast path'i tetikler.
- `bg_detect::resolve()` çağrısı. Startup latency'yi (en kötü 100 ms OSC 11 timeout) sıfırlar; bypass kullanıcısının amacı zaten "tayf gözükmesin" — terminal background'una göre theme seçmek de "gözükmek" sayılır.
- Config file load ve rule compilation. `Compiled` struct'a hiç ihtiyaç yok; bypass mode'da `Compiled` instance constructor edilmez.
- File watcher + reload orchestrator thread'leri. Hot reload de gözüksün/gözükmesin diye `--no-hot-reload`'a delege edilmez — bypass implicitly bunu da kapatır.

**Bypass aktivasyonu, precedence:**

```
CLI --bypass (bool flag, default false)
  > TAYF_DISABLE env var (truthy: "1"/"true"/"yes" case-insensitive)
  > default: bypass = false
```

CLI explicit'liği env var implicit'liğini yener (klasik POSIX precedence). Negatif yön YOK — CLI `--bypass=false` veya `--no-bypass` tanımlanmaz (YAGNI; env var'ı unset etmek aynı işi yapar).

**Tek read garantisi:** `TAYF_DISABLE` env var'ı `Tayf::run`'ın başında **tek sefer** okunur; runtime'da yeniden okunmaz. Mid-session davranış değişikliği yok — bu signal handler thread'inde race olmamasını garanti eder ve mental model'i basit tutar (bir tayf process bypass ya tüm session boyunca aktif ya değil).

### 1.2 Kapsam F2 — `--no-hot-reload` + SIGHUP behavior fix

İki bağlı değişiklik:

**(F2-a) `--no-hot-reload` CLI flag.** Default `false`. True olduğunda:
- `watch::ConfigWatcher::spawn` çağrılmaz (file watcher thread + debounce thread spawn edilmez).
- `reload::ReloadOrchestrator::spawn` çağrılmaz (reload thread spawn edilmez).
- `reload_tx` mpsc channel oluşturulur ama dağıtılmaz — signal handler'a `None` geçer (mevcut Option<Sender> şekli korunur).

Motivasyon: düşük-kaynak ortamlar (busybox-like container, CI smoke test), file system aktivitesi watcher event flood'una yol açıyor ortamlar, ve "config'i değiştirmeyeceğim, kesinlikle yeni thread istemiyorum" demek isteyen kullanıcılar.

**(F2-b) SIGHUP davranış düzeltmesi.** Mevcut `signals.rs:84-90` davranışı:

```rust
SIGHUP => {
    if let Some(tx) = &reload_tx {
        let _ = tx.send(ReloadRequest::SignalHup);
    }
}
```

`reload_tx` None ise (yani config dosyası yokken ya da v0.3.3'te `--no-hot-reload` ile) SIGHUP **silently dropped** — child process group'a forward edilmez. Bu v0.2.1 hot-reload tasarımının istemeden gelen yan etkisi: v0.2.0'da SIGHUP `signal_hook::Signals::new`'in listesinde yoktu (sadece WINCH/INT/TERM), default disposition (process termination) ya da inherited disposition geçerliydi; v0.2.1 SIGHUP'ı reload trigger olarak intercept etti ama "intercept edilmeyen" durumda forward fallback'i koymadı.

Operasyonel sonuç: `tmux detach`, terminal emulator kapanması, `kill -HUP` ile graceful shutdown beklentisi — bunların hiçbiri child shell'e ulaşmıyor. Detach sonrası shell ve onun altındaki foreground process (vim, less, ssh, ...) orphan kalıyor; tayf zombie parent olarak takılı kalıyor.

v0.3.3 fix: **SIGHUP her durumda child process group'a forward edilir.** Pseudo-code:

```rust
SIGHUP => {
    // Forward to child process group regardless of hot-reload state.
    // Mirrors SIGINT/SIGTERM handling.
    if let Some(pid) = child_pid {
        if let Ok(pid_i32) = i32::try_from(pid) {
            forward_to_pgid(pid_i32, SIGHUP);
        }
    }
    // Additionally trigger reload when hot-reload is wired.
    if let Some(tx) = &reload_tx {
        let _ = tx.send(ReloadRequest::SignalHup);
    }
}
```

İki action sırası (forward önce, sonra reload-notify): forward `killpg` syscall'u kernel'e gider, hızlıdır ve operasyonel doğruluk için kritik. Reload notification mpsc channel send — orchestrator thread'in çekme süresine bağlı. Forward önce ki kullanıcının `kill -HUP` intent'i (graceful shutdown) önce iletilir.

**Backward compatibility:** v0.2.1+ kullanıcısı `kill -HUP <tayf-pid>` ile config reload tetikliyor olabilir. Yeni davranışta aynı `kill -HUP` hâlâ reload tetikler (`reload_tx.send` korundu) **ama aynı sinyal child process group'a da gider**. Eğer child shell SIGHUP'ı default disposition (terminate) ile alıyorsa, kullanıcının session'ı sonlanır. **Bu davranış değişikliği prominent CHANGELOG entry alır** (Changed bölümünde, "BEHAVIOR CHANGE" işaretiyle).

Mitigation: shell'ler (`bash`, `zsh`, `fish`) interactive olduklarında genelde `trap '' HUP` veya equivalent ile SIGHUP'ı yutarlar; non-interactive shell'de risk var ama tayf use case'i interactive shell. Foreground program (vim, less) için SIGHUP genelde "save & exit" anlamına gelir — kullanıcı buna alışkın olmalı çünkü tmux/screen detach yapınca alacağı davranış zaten bu.

**Alternatif değerlendirildi: opt-in flag** (`--forward-sighup` veya `[general] forward_sighup = true`). Reddedildi:
- Default-correct davranış = forward. Yutmak v0.2.1 regression'u idi; opt-in mode yapmak regression'u "feature" olarak donduruyor.
- Bir flag daha → mental model karmaşası. tayf'ın signal handling kontratı "WINCH/INT/TERM/HUP'ı child process group'a forward et" şeklinde tutarlı olur.

### 1.3 Kapsam F3 — Reload banner (opt-in)

Hot reload başarılı olduğunda kullanıcının çıktısına minimal bir visual confirmation. Üç motivasyon:

1. **Discoverability:** Kullanıcı config dosyasını editör'de kaydediyor; tayf 200 ms debounce + reload yapıyor; ama kullanıcının görsel feedback'i yok. "Bir şey oldu mu?" sorusu.
2. **Debugging:** Config'in valid olduğunu ve gerçekten yeniden yüklendiğini anlamak için kullanıcının `TAYF_LOG=info` set etmesi gerekiyor (mevcut info_msg!). Bu çok intrusive (her reload bir stderr satırı + tüm tayf info log'ları).
3. **CI/headless monitoring:** `tayf` arkada uzun çalışan bir oturum; admin SIGHUP gönderdi; banner ekrana tek satırda düşüyor → görsel confirmation.

**Davranış:**
- Yeni config field: `[general] show_reload_banner: bool`, default `false` (**opt-in**). Mevcut `GeneralSection`'a additive field.
- True olduğunda: `reload::reload_once`'un success path'i (yani `ArcSwap::store` sonrası) `/dev/tty`'a şu byte sequence'i yazar:
  ```
  \r\x1b[K\x1b[2mtayf: config reloaded\x1b[0m\n
  ```
  Anlamı: `\r` → satır başına dön; `\x1b[K` → satırın geri kalanını sil (kullanıcının yarım promptu üzerine yazmamak için); `\x1b[2m` → dim attribute; metin; `\x1b[0m` → reset; `\n` → yeni satıra geç.
- Reload **FAILURE** durumunda banner yazılmaz; mevcut `warn_msg!` (stderr'e) yeterli. Failure ekranı kirletmemeli (kullanıcı zaten config dosyasında hata yaptığını editor'de görüyor).
- `show_reload_banner = false` (default) ise banner write hiç yapılmaz; mevcut `info_msg!` davranışı korunur.

**Hot reload reload event source agnostic:** Banner hem `ReloadRequest::FileChanged` (file watcher) hem `ReloadRequest::SignalHup` (kill -HUP) için yazılır — kullanıcı hangi yolla tetiklediğini banner içinde ayırt etmeye ihtiyaç duymaz (`TAYF_LOG=info` zaten ayırt eder).

**Pipeline bypass:** Banner string'i kendi içinde balanced SGR (`\x1b[2m` open, `\x1b[0m` close). Banner write `/dev/tty`'a doğrudan yapılır — `runtime`'ın stdout pipe'ından geçmez, dolayısıyla `Pipeline::apply_rules` banner byte'larını hiç görmez (yani regex match riski yok, banner metni "config" "reloaded" gibi kelimelerde tayf rule'larıyla çakışmaz).

**OSC 133 prompt marker wrap YOK.** OSC 133 `\x1b]133;A\x1b\\` (prompt start), `\x1b]133;B\x1b\\` (prompt end) gibi semantic prompt marker'ları modern terminal'lerin "scroll to prompt", "copy command" özellikleri için var. Banner'ı OSC 133 ile wrap edersek terminal banner satırını "user input" olarak algılar ve copy/scroll davranışını bozar. v0.4'te (alt-screen-aware banner queuing ile birlikte) tekrar değerlendirilir; v0.3.3 scope'unda **explicit non-goal** (§1.7).

**TUI mode awareness YOK.** v0.3.0 ANSI SM `tui_active` durumunu tutuyor (`src/ansi.rs::Parser` alt-screen state). Banner write TUI mode'u kontrol etmez; opt-in default olduğu için kullanıcı banner'ı aktif ederken vim/less içindeyken görmeyi kabul etmiş sayılır. Alt-screen "üzerine bant geçme" davranışı yine ekrana basar (alt-screen buffer'da görünür → main screen restore olunca silinir, dolayısıyla "kaybolur" — annoying ama not catastrophic). v0.4'te alt-screen-aware queueing (banner'ı main screen'e dönünce yaz) yapılır. README'de explicit not.

**`/dev/tty` open failure:** `OpenOptions::new().write(true).open("/dev/tty")` başarısız olursa (örn. `tayf < /dev/null > /dev/null` gibi tuhaf invocation, ya da `/dev/tty` mevcut değil) banner write **silently dropped** (warn log yok). tayf'ın main I/O path'i `/dev/tty`'a değil PTY master fd'sine yazıyor; banner side-channel, bozulması session'u etkilemez. Reload başarılı oldu, sadece visual confirmation gitmedi.

### 1.4 Hedefler

- F1: `--bypass` flag ve `TAYF_DISABLE` env var resolved at startup; bypass aktivasyonu PTY/signal/tty_guard'ı koruyup Pipeline regex + bg_detect + config load + rule compile + hot reload'u skip eder. CLI > env precedence. Tek read garantisi.
- F2: `--no-hot-reload` flag file watcher + reload orchestrator thread'lerini spawn etmez. SIGHUP **her durumda** (`reload_tx` Some/None bağımsız) child process group'a forward edilir; reload da Some ise reload trigger eder. CHANGELOG'da BEHAVIOR CHANGE notu.
- F3: `[general] show_reload_banner: bool` config field (default false). True iken success reload sonrası `/dev/tty`'a tek satırlık dim metin yazılır; FAILURE'da yazılmaz. `/dev/tty` open failure silently swallowed.
- Yeni direct dep YOK. Yeni dev-dep YOK.
- Public API değişmez (yeni CLI flag = additive; yeni opt-in config field = additive; SIGHUP davranış değişikliği = minor-version-acceptable çünkü v0.2.1 davranışı dokümante edilmemiş accidental sink'ti).
- v0.3.2 config dosyaları (ve daha eski) shimless okunur.

### 1.5 Hedef olmayan (sonraki sub-version'lara veya v0.4+'a)

- **Alt-screen-aware banner queuing.** vim/less içindeyken banner suppress + main screen'e dönünce flush. ANSI SM `tui_active` state'i ile yapılır ama complexity v0.3.3 scope'unda fazla; v0.4.
- **OSC 133 semantic prompt marker wrap.** Banner'ı `\x1b]133;A\x1b\\` ve `\x1b]133;B\x1b\\` ile wrap etmek. Modern terminal'lerin "copy command" davranışını bozar; alt-screen-aware queuing ile birlikte v0.4'te değerlendirilir.
- **Banner format / template / color customization.** YAGNI; default `tayf: config reloaded` dim metin yeterli. Eğer kullanıcı talebi olursa v0.4+ (custom `[general] reload_banner_text = "..."` veya benzeri).
- **`TAYF_DISABLE_HOT_RELOAD` env var.** `--no-hot-reload` CLI-only ship olur. Env var ihtiyacı pratik feedback'ten sonra eklenir (YAGNI); pattern `TAYF_DISABLE`'ın verb namespace'i ile uyumlu olur ama v0.3.3'te `--no-hot-reload` yeterli.
- **`TAYF_BYPASS` env var (TAYF_DISABLE'a alternatif isim).** İki isim aynı verb namespace'inde çelişir; sadece `TAYF_DISABLE` ship olur. Kullanıcı `--bypass` flag ya da `TAYF_DISABLE=1`'in ikisinden birini seçer.
- **`--no-bg-detect` CLI flag.** Bypass mode zaten bg_detect'i kapatır; non-bypass mode'da bg_detect'i devre dışı bırakmak için `--theme dark` veya `--theme light` zaten var. `TAYF_DISABLE_BG_DETECT` test-only escape hatch (v0.3.2 §3.3) production için uygun değil. YAGNI.
- **Bypass mode'da partial passthrough** (örn. "rules off ama theme color yine de inject et"). YAGNI ve mental model'i bozar — bypass binary toggle.
- **Bypass mode runtime toggle.** SIGUSR1 ile mid-session bypass açıp kapama gibi. YAGNI; v0.4+ "operational" işi.
- **Reload banner için diğer eventler** (config load on startup, theme resolution sonucu, vb.). Banner reload event'lerine özel; başlangıçta gözükmesi UX'i kalabalık eder.
- **Banner için OSC 8 hyperlink** (config dosyası path'ı clickable). YAGNI; kullanıcı path'ı zaten biliyor.

---

## 2. Kararlar (özet)

| # | Karar | Gerekçe |
|---|---|---|
| 1 | F1: CLI flag adı `--bypass`, env var adı `TAYF_DISABLE` | `--bypass` net intent ("tayf'ı baypas et"); `TAYF_DISABLE` umbrella'da §3.4'te belirtildi ve `TAYF_DISABLE_BG_DETECT` namespace'i ile uyumlu. |
| 2 | F1: env var truthy değerleri `{1, true, yes}` case-insensitive | `bg_detect::env_truthy` (v0.3.2 ship'lendi) ile aynı semantik; tutarlılık. Helper `lib.rs`'ye `crate::env_truthy` olarak taşınır (veya `bg_detect::env_truthy` `pub(crate)` yapılır), duplicate parse logic kabul edilmez. |
| 3 | F1: precedence CLI `--bypass` > env `TAYF_DISABLE` > default false | POSIX convention. Negatif yön yok (`--no-bypass` tanımlanmaz). |
| 4 | F1: env var tek read at `Tayf::run` entry; runtime'da yeniden okunmaz | Race-free; mental model basit. |
| 5 | F1: bypass aktifken PTY + signals + tty_guard + ANSI SM korunur; Pipeline + bg_detect + config load + rule compile + hot reload SKIP | Bypass kontratı "tayf görünmez ama PTY yönetimi devam" — orphan process / terminal corruption riski yok. |
| 6 | F2: `--no-hot-reload` CLI flag, default false | CLI-only; env var v0.3.3'te yok (YAGNI). |
| 7 | F2: SIGHUP child process group'a HER DURUMDA forward, ek olarak reload trigger Some ise | v0.2.1 silent-sink regression fix. Default-correct davranış; opt-in flag yapılması regression'u feature'a çevirir. |
| 8 | F2: SIGHUP davranış değişikliği CHANGELOG'da "BEHAVIOR CHANGE" işaretiyle prominent | Minor version'da kabul edilebilir kırılım (v0.2.1 davranışı dokümante edilmemişti, accidental sink'ti, forward yön default-safe shell convention). |
| 9 | F3: yeni `[general] show_reload_banner: bool`, default `false` (opt-in) | Default-off — banner ekran kirletmesin (vim/less awareness yok); opt-in kullanıcı trade-off'u kabul eder. |
| 10 | F3: banner string `\r\x1b[K\x1b[2mtayf: config reloaded\x1b[0m\n`, balanced SGR | `\r\x1b[K` yarım prompt'u temizler; dim attribute display-quiet; reset + newline disciplined. Pipeline bypass'lı (doğrudan `/dev/tty` write). |
| 11 | F3: banner sadece SUCCESS reload; failure'da yazılmaz | Failure mevcut `warn_msg!` (stderr) ile bildirilir; ekranı kirletmeyelim. |
| 12 | F3: banner `/dev/tty` open failure silently swallow | Banner side-channel; bozulması session'u etkilemez. Warn log eklemek "warn flood" riskini açar. |
| 13 | F3: OSC 133 wrap YOK; alt-screen awareness YOK | v0.4 territory; v0.3.3'te opt-in default ile kullanıcı sorumluluğu. |
| 14 | F1+F2 birlikte aktif olabilir (independent flags) | F1 implicit olarak hot reload'u da kapatır (bypass = no config load = no watcher); ama `--bypass --no-hot-reload` çelişki değil (ikinci flag noop). Mental model basit: her flag kendi domain'ini kapatır. |
| 15 | Performans: F1/F2 hot path'ı azaltır (regression imkansız); F3 banner write reload event'inde tek shot (microsecond cost). `BASELINE.md` bench rerun zorunlu | v0.3.2 baseline'ı bozmamak için bench-on-release. |
| 16 | Yeni dep YOK | CLAUDE.md §3 + memory dep-minimalism. |

---

## 3. Mimari etki ve dosya bazlı değişiklikler

### 3.1 `src/cli.rs`

İki yeni CLI flag eklenir. `Args` struct'a additive field'lar; mevcut field'ların order ve doc-comment'leri korunur.

```rust
/// Disable all of tayf's colorization, pattern matching, and background
/// detection — passthrough the shell's output byte-for-byte while still
/// wrapping the PTY and forwarding signals. Equivalent to `TAYF_DISABLE=1`
/// (CLI flag wins on precedence).
#[arg(long, default_value_t = false)]
pub bypass: bool,

/// Disable hot config reloading. The file watcher and reload orchestrator
/// threads are not spawned. `SIGHUP` is still forwarded to the child
/// process group (a behavior change from v0.2.1 — see CHANGELOG).
#[arg(long, default_value_t = false)]
pub no_hot_reload: bool,
```

**Notlar:**
- Field adları snake_case: `bypass`, `no_hot_reload`. clap derive `--bypass` ve `--no-hot-reload` flag'leri üretir.
- `default_value_t = false` explicit (mevcut `--login` ve `--no-color` pattern'i ile aynı).
- Doc-comment'ler `--help` çıktısında görünür; F2'nin SIGHUP davranış değişikliği parantez içinde not.

Test mod'una (`src/cli.rs::tests`) iki yeni test:

```rust
#[test]
fn parses_bypass_flag() {
    let args = Args::try_parse_from(["tayf", "--bypass"]).unwrap();
    assert!(args.bypass);
    assert!(!args.no_hot_reload);
}

#[test]
fn parses_no_hot_reload_flag() {
    let args = Args::try_parse_from(["tayf", "--no-hot-reload"]).unwrap();
    assert!(args.no_hot_reload);
    assert!(!args.bypass);
}

#[test]
fn bypass_and_no_hot_reload_default_to_false() {
    let args = Args::try_parse_from(["tayf"]).unwrap();
    assert!(!args.bypass);
    assert!(!args.no_hot_reload);
}

#[test]
fn parses_combined_bypass_and_no_hot_reload() {
    let args = Args::try_parse_from(["tayf", "--bypass", "--no-hot-reload"]).unwrap();
    assert!(args.bypass);
    assert!(args.no_hot_reload);
}
```

Mevcut `parses_all_flags` test güncellenir (yeni flag'leri içerecek şekilde).

### 3.2 `src/bg_detect.rs` (env_truthy helper'ın taşınması)

`bg_detect::env_truthy` fonksiyonu (`src/bg_detect.rs:41-45`) modül-private. F1'in `TAYF_DISABLE` parse'ı için aynı semantik gerekli — duplicate fonksiyon yazmak CLAUDE.md §3 standartlarına aykırı. İki seçenek:

**Seçenek A:** `bg_detect::env_truthy`'yi `pub(crate)` yap, `lib.rs`'den `crate::bg_detect::env_truthy` olarak çağır. Minimum diff; helper'ı mevcut yerinde tut.

**Seçenek B:** `env_truthy` ayrı modüle (`src/util.rs` veya doğrudan `src/lib.rs::env_truthy`) taşı; `bg_detect` ondan import etsin.

**Karar: Seçenek A.** Tek satır görünürlük değişikliği (`fn env_truthy` → `pub(crate) fn env_truthy`), helper-only modül kurmak YAGNI (tayf hâlâ tek bir küçük binary). Doc-comment hafifçe güncellenir: "test-only" cümlesi kalır ama "Used by `TAYF_DISABLE_BG_DETECT` and `TAYF_DISABLE` (v0.3.3)" eklemesi.

```rust
/// Truthy env-var values: "1", "true", "yes" (case-insensitive). Used by
/// the `TAYF_DISABLE_BG_DETECT` test-only bypass (v0.3.2) and the
/// `TAYF_DISABLE` whole-binary bypass (v0.3.3).
pub(crate) fn env_truthy(name: &str) -> bool {
    std::env::var(name)
        .ok()
        .is_some_and(|v| matches!(v.to_ascii_lowercase().as_str(), "1" | "true" | "yes"))
}
```

### 3.3 `src/lib.rs` (`Tayf::run` bypass branching)

`Tayf::run`'a bypass resolution eklenir; bypass aktifse erken bir alternatif kod yolu çalıştırılır (config load + bg_detect + rule compile + hot reload skip; sadece PTY + signal + I/O loop).

**Bypass resolution (CLI > env > default false):**

```rust
let bypass = args.bypass || bg_detect::env_truthy("TAYF_DISABLE");
```

Tek satır, race-free (env var bir kez okunur, runtime'da yeniden okunmaz).

**Bypass aktif kod yolu:**

```rust
if bypass {
    // Bypass mode: PTY wrap + signal forwarding + tty_guard, but no
    // config load, no bg_detect, no rule compile, no hot reload.
    // runtime::run consumes apply_colors == false and bypasses
    // Pipeline entirely (raw passthrough fast path).
    let spec = shell::discover(args.shell.as_deref(), args.login)?;
    let guard = tty_guard::TtyGuard::engage()?;
    let session = pty::PtySession::spawn(&spec)?;
    let (reader, writer, resizer, child) = session.into_parts()?;
    let child_pid = child.pid();
    let signal_guard = signals::spawn_handler(resizer, child_pid, None)?;

    // Construct a minimal empty Compiled to satisfy runtime::run's signature.
    // apply_colors == false ensures the Pipeline is never actually constructed,
    // so the rules handle is structurally unreachable past startup.
    let empty_rules: Arc<ArcSwap<rules::Compiled>> =
        Arc::new(ArcSwap::from_pointee(rules::Compiled::empty()));

    let exit_code = runtime::run(reader, writer, child, Arc::clone(&empty_rules), false)?;

    drop(signal_guard);
    drop(guard);

    #[allow(clippy::cast_possible_truncation, clippy::cast_sign_loss)]
    let code = (exit_code & 0xff) as u8;
    return Ok(ExitCode::from(code));
}
```

**`rules::Compiled::empty()` yeni constructor.** `apply_colors == false` ile `runtime::run` Pipeline'ı hiç construct etmez, ama `Arc<ArcSwap<Compiled>>` signature gerektiriyor. Boş bir `Compiled` (no patterns, no styles) construct etmek için minimal constructor:

```rust
// In src/rules.rs:
impl Compiled {
    /// Empty rule set — no patterns, no styles. Used by the bypass mode
    /// (`--bypass` / `TAYF_DISABLE`) to satisfy `runtime::run`'s Arc<ArcSwap<Compiled>>
    /// signature without actually compiling any rules. The Pipeline is never
    /// constructed in bypass mode (apply_colors == false), so this struct
    /// is structurally unreachable past startup.
    pub(crate) fn empty() -> Self {
        Self {
            patterns: regex::bytes::RegexSet::empty(),
            // ... all other fields zero-default ...
        }
    }
}
```

**Hot reload off branching (bypass değil, `--no-hot-reload` true):**

Mevcut `lib.rs:147-150` ve `lib.rs:157-163` blokları conditional:

```rust
// Spawn the file watcher only when (a) a config file was loaded AND
// (b) hot-reload is not disabled. Absent watcher == absent reload
// thread; reload_tx clone'ları sender-end'leri çekilmez.
let hot_reload_enabled = !args.no_hot_reload;

let watcher = if hot_reload_enabled {
    loaded
        .as_ref()
        .map(|(_, p)| watch::ConfigWatcher::spawn(p, reload_tx.clone()))
        .transpose()?
} else {
    None
};

let _orchestrator = if hot_reload_enabled {
    Some(reload::ReloadOrchestrator::spawn(
        Arc::clone(&rules),
        loaded.as_ref().map(|(_, p)| p.clone()),
        effective_theme.clone(),
        effective_depth,
        reload_rx,
        // F3: pass show_reload_banner from config (false if no config)
        config_ref.is_some_and(|c| c.general.show_reload_banner),
    ))
} else {
    drop(reload_rx);  // explicit; receiver never spawned
    None
};
```

**Signal handler `reload_tx`:** `--no-hot-reload` true iken signal handler'a `None` geçer (SIGHUP forward davranışı `signals.rs` içinde reload_tx'den bağımsız). Bu, F2-b SIGHUP fix'ini desteklemenin temeli — signals.rs `reload_tx`'i sadece "reload trigger var mı" sorusu için kullanır, "SIGHUP'ı forward et mi" kararı için değil.

```rust
let signal_reload_tx = if hot_reload_enabled { Some(reload_tx.clone()) } else { None };
let signal_guard = signals::spawn_handler(resizer, child_pid, signal_reload_tx)?;
```

Mevcut `drop(reload_tx)` çağrısı korunur (orchestrator spawn olmadığı için reload_tx'in tek owner'ı bu noktada local; drop trivial).

### 3.4 `src/signals.rs` (SIGHUP behavior fix)

Mevcut `SIGHUP` arm değiştirilir. `forward_to_pgid` helper'ı zaten mevcut (`src/signals.rs:100-106`); SIGHUP arm onu çağırır.

**Eski (mevcut, `src/signals.rs:84-90`):**

```rust
SIGHUP => {
    // Best-effort: orchestrator may have exited
    // (channel closed). Silent drop in that case.
    if let Some(tx) = &reload_tx {
        let _ = tx.send(ReloadRequest::SignalHup);
    }
}
```

**Yeni (v0.3.3):**

```rust
SIGHUP => {
    // v0.3.3: SIGHUP is forwarded to the child process group
    // unconditionally, mirroring SIGINT/SIGTERM. This fixes a v0.2.1
    // regression where SIGHUP was silently dropped when hot-reload was
    // not wired (no config file present, or --no-hot-reload set),
    // leaving the child shell unaware of tmux detach / terminal close
    // and orphaning its foreground processes.
    if let Some(pid) = child_pid {
        if let Ok(pid_i32) = i32::try_from(pid) {
            forward_to_pgid(pid_i32, sig);
        }
    }
    // Additionally trigger a config reload when the hot-reload
    // pipeline is wired. Best-effort: orchestrator may have exited
    // (channel closed).
    if let Some(tx) = &reload_tx {
        let _ = tx.send(ReloadRequest::SignalHup);
    }
}
```

Order matters: forward önce (kullanıcının "kill -HUP" intent'i için en hızlı kernel path), reload-notify sonra (mpsc channel send).

**`spawn_handler` signature değişmez.** `child_pid` ve `reload_tx` parametreleri zaten mevcut. Yalnızca handler body güncellenir.

**Module-level doc-comment güncellemesi:** "forwarding `SIGWINCH` ... and `SIGINT`/`SIGTERM`/`SIGHUP` to the child's process group" şeklinde HUP'ı listelemeye eklenir (mevcut doc HUP'tan bahsetmiyordu).

**Test eklemesi (`src/signals.rs::tests`)**: `tests` modülü mevcut değil (`src/signals.rs:106` itibariyle yok). Spawn etmenin sinyal yarış koşulları nedeniyle unit test'i zor; davranışı integration test (`tests/integration_signal_hup.rs`, §4.1) ile pinleriz. Unit-level kapsam yok.

### 3.5 `src/config.rs` (`show_reload_banner` field)

`GeneralSection`'a additive field:

```rust
pub(crate) struct GeneralSection {
    #[serde(default = "default_true")]
    pub(crate) respect_existing_colors: bool,

    #[serde(default)]
    pub(crate) theme: Option<String>,

    /// v0.3.3: when `true`, the reload orchestrator writes a one-line dim
    /// banner (`tayf: config reloaded`) to `/dev/tty` after each successful
    /// hot reload (file change or `SIGHUP`). Default `false` (opt-in).
    /// Failures during reload do not write the banner; they continue to
    /// surface via the existing `warn_msg!` log path on stderr.
    #[serde(default)]
    pub(crate) show_reload_banner: bool,
}

impl Default for GeneralSection {
    fn default() -> Self {
        Self {
            respect_existing_colors: true,
            theme: None,
            show_reload_banner: false,
        }
    }
}
```

`deny_unknown_fields` korunur — typo'lar reddedilir. `#[serde(default)]` field-level: missing field = `bool::default()` = `false`, yani opt-in.

Mevcut config dosyaları (v0.3.2 ve öncesi) `show_reload_banner` field'ı içermez; serde default'u (`false`) ile parse eder. Backward compatible.

Test eklemeleri:

```rust
#[test]
fn show_reload_banner_defaults_to_false_when_omitted() {
    let cfg = parse("test", "[general]\n").unwrap();
    assert!(!cfg.general.show_reload_banner);
}

#[test]
fn show_reload_banner_defaults_to_false_when_general_section_missing() {
    let cfg = parse("test", "").unwrap();
    assert!(!cfg.general.show_reload_banner);
}

#[test]
fn show_reload_banner_parses_true() {
    let cfg = parse("test", "[general]\nshow_reload_banner = true\n").unwrap();
    assert!(cfg.general.show_reload_banner);
}

#[test]
fn show_reload_banner_parses_false_explicit() {
    let cfg = parse("test", "[general]\nshow_reload_banner = false\n").unwrap();
    assert!(!cfg.general.show_reload_banner);
}

#[test]
fn show_reload_banner_unknown_typo_rejected() {
    let err = parse("test", "[general]\nreload_banner = true\n").unwrap_err();
    assert!(matches!(err, crate::error::Error::Config { .. }));
}
```

### 3.6 `src/reload.rs` (banner write)

`ReloadOrchestrator::spawn` signature'a yeni parametre `show_reload_banner: bool` eklenir. Thread closure'ı parametreyi capture eder ve success branch'te banner yazar.

**Yeni `reload_once` helper veya orchestrator body:**

```rust
pub(crate) struct ReloadOrchestrator {
    handle: Option<JoinHandle<()>>,
}

impl ReloadOrchestrator {
    pub(crate) fn spawn(
        rules_handle: Arc<ArcSwap<Compiled>>,
        config_path: Option<PathBuf>,
        theme: Option<String>,
        depth: ColorDepth,
        rx: Receiver<ReloadRequest>,
        show_reload_banner: bool,
    ) -> Self {
        let handle = thread::Builder::new()
            .name("tayf-reload".into())
            .spawn(move || {
                while let Ok(req) = rx.recv() {
                    match reload_once(&rules_handle, config_path.as_deref(), theme.as_deref(), depth) {
                        Ok(()) => {
                            crate::log::info_msg!("config reloaded ({req:?})");
                            if show_reload_banner {
                                write_reload_banner();
                            }
                        }
                        Err(e) => {
                            crate::log::warn_msg!(
                                "config reload failed ({req:?}): {e}; keeping previous rule set"
                            );
                            // Failure path: no banner (intentional — see spec §1.3).
                        }
                    }
                }
            })
            .expect("reload thread must spawn");
        ReloadOrchestrator { handle: Some(handle) }
    }
}

/// Write the v0.3.3 reload banner to `/dev/tty`. Best-effort: failures
/// (e.g. `/dev/tty` unavailable in an exotic invocation environment) are
/// silently swallowed because the banner is a side-channel — its absence
/// does not affect session correctness.
///
/// Banner bytes: `\r\x1b[K\x1b[2mtayf: config reloaded\x1b[0m\n`
/// - `\r\x1b[K` returns to column 0 and erases the rest of the line, so
///   a half-typed prompt is not overwritten in place.
/// - `\x1b[2m` enables dim attribute; `\x1b[0m` resets it.
/// - `\n` advances one line so subsequent prompt redraw is clean.
fn write_reload_banner() {
    use std::io::Write;
    if let Ok(mut tty) = std::fs::OpenOptions::new().write(true).open("/dev/tty") {
        // Best-effort: ignore write errors. The banner is a side-channel.
        let _ = tty.write_all(b"\r\x1b[K\x1b[2mtayf: config reloaded\x1b[0m\n");
        let _ = tty.flush();
    }
}
```

**Notlar:**
- `std::fs::OpenOptions::new().write(true).open("/dev/tty")` — `bg_detect.rs`'in TTY açışındaki gibi raw mode'a girmek YOK (banner write read yapmıyor; termios snapshot'a ihtiyaç yok). Trivial open + write.
- Write error swallow edilir (banner side-channel).
- `flush()` zorunlu — kernel write buffer'ı drain edilmezse banner gecikebilir; `/dev/tty` line-buffered olabilir.
- Banner bytes raw — `regex` veya `style.rs` SGR builder'ı kullanılmaz; constant bytes en hızlı + en az ekstra surface.

**Mevcut `reload_once` fonksiyonu DEĞİŞMEZ.** Banner write orchestrator'ın success branch'inde, `reload_once` saf re-load + re-compile + `ArcSwap::store` semantiğini korur. Bu, mevcut `reload_once_swaps_on_valid_config` ve `reload_once_preserves_old_arc_on_parse_error` unit test'lerinin etkilenmemesini sağlar.

**Mevcut test'lere ekleme (`reload_once`'a değil, orchestrator-level test):**

```rust
#[test]
fn orchestrator_writes_banner_on_success_when_enabled() {
    // /dev/tty mock zor (test ortamında /dev/tty interactive değil veya yok).
    // Bu davranış integration test'te (tests/integration_reload_banner.rs)
    // portable-pty subprocess'inde pinlenir; orchestrator-unit-level test
    // sadece "show_reload_banner = true geçilmesi panic etmiyor"u kontrol eder.
    // ... existing setup ...
    let _orchestrator = ReloadOrchestrator::spawn(
        Arc::clone(&handle),
        Some(path),
        None,
        ColorDepth::Truecolor,
        rx,
        true,  // show_reload_banner
    );
    // ... existing assertion (Arc swapped) ...
}

#[test]
fn orchestrator_no_banner_when_disabled() {
    // Same setup, show_reload_banner = false, orchestrator çalışır, swap
    // yapar, banner yazmaz. Test ortamında /dev/tty olmadığı için zaten
    // yazsa da no-op olur — bu test sadece compile gate.
    // ...
}
```

Mevcut `orchestrator_swaps_on_file_changed_event` ve `orchestrator_preserves_old_arc_on_bad_config` test'lerinin `ReloadOrchestrator::spawn` çağrıları `false` parametresiyle güncellenir (backward-compat semantik korunur).

### 3.7 `CHANGELOG.md` ve `Cargo.toml`

- `Cargo.toml`: `version = "0.3.2"` → `"0.3.3"`.
- `CHANGELOG.md`: `[0.3.3]` entry, Keep a Changelog format. Tam taslak:

```markdown
## [0.3.3] — TBD

### Added

- `--bypass` CLI flag and `TAYF_DISABLE` environment variable. When either is set
  (CLI takes precedence; env truthy values: `1`, `true`, `yes`, case-insensitive),
  tayf still wraps the PTY, forwards signals, and protects the terminal via its
  raw-mode RAII guard, but skips all rule matching, SGR injection, automatic
  background detection, and hot config reloading. Equivalent to running the shell
  directly except for PTY ownership and signal plumbing. Intended for the
  `[[ -n "$TAYF_DISABLE" ]] || exec tayf`-style conditional wrap in shell
  rc files, and for one-shot overrides like `TAYF_DISABLE=1 my-tool`.
- `--no-hot-reload` CLI flag. When set, the file watcher and reload orchestrator
  threads are not spawned. Config still loads at startup as usual; only the
  *re*-load pipeline is off.
- `[general] show_reload_banner` config field (default `false` — opt-in). When
  set to `true`, a one-line dim banner (`tayf: config reloaded`) is written
  directly to `/dev/tty` after each successful hot reload (file change or
  `SIGHUP`). Reload *failures* do not write the banner — they continue to
  surface via the existing `warn_msg!` stderr log path. The banner is naive
  about TUI / alt-screen state: when an opt-in user is inside vim / less, the
  banner will be drawn into the alt-screen buffer and vanish when the program
  exits. Alt-screen-aware queuing is deferred to v0.4.

### Changed

- **BEHAVIOR CHANGE — SIGHUP forwarding.** `SIGHUP` is now forwarded to the
  child process group in all configurations, mirroring `SIGINT` and `SIGTERM`.
  Previously (v0.2.1 through v0.3.2), `SIGHUP` was forwarded *only* when the
  hot-reload pipeline was wired — i.e. when a config file existed AND the
  reload orchestrator had been spawned. In every other case (no config,
  or — with v0.3.3 — `--no-hot-reload`), `SIGHUP` was silently dropped by
  the signal thread, leaving the child shell unaware of tmux detach,
  terminal-emulator close, or `kill -HUP` and orphaning its foreground
  processes. v0.3.3 fixes this: the child process group always receives the
  signal, AND (when hot-reload is wired) the orchestrator additionally
  receives the reload trigger. Users who relied on the v0.2.1 silent-drop
  behavior (rare — it was undocumented and arose from the v0.2.1 hot-reload
  design accidentally intercepting `SIGHUP` without a forwarding fallback)
  should expect the child shell to terminate or behave per its own `SIGHUP`
  trap on detach / `kill -HUP`. Most interactive shells (`bash`, `zsh`,
  `fish`) install a `SIGHUP` trap and tolerate the signal cleanly.

### Notes

- No new dependencies. v0.3.2 (and earlier) config files remain shimless
  backward-compatible — the new `show_reload_banner` field defaults to
  `false` when absent.
- The `bg_detect::env_truthy` helper (v0.3.2) was promoted from module-
  private to `pub(crate)` so the `TAYF_DISABLE` parse path can reuse it.
  Parsing semantics are identical (`1` / `true` / `yes`, case-insensitive).
- No new public API surface beyond the additive CLI flags and the additive
  config field. `Tayf::run` signature unchanged. Hot-reload pipeline shape
  unchanged when active; the orchestrator gains a `show_reload_banner: bool`
  parameter, but `ReloadOrchestrator` is `pub(crate)` so this is not a
  public API change.

[0.3.3]: https://github.com/beraartuc/tayf/releases/tag/v0.3.3
```

### 3.8 `README.md`

İki ufak ek (Configuration ve Operational notes bölümlerine):

- `--bypass` + `TAYF_DISABLE` belirgin biçimde dokümante (örnek `.zshrc` snippet'i; "tayf still wraps PTY + forwards signals" davranış kontratı; "skips all colorization + bg_detect + hot reload" listesi).
- `--no-hot-reload` ve SIGHUP davranış değişikliği prominent notu (BEHAVIOR CHANGE: SIGHUP forwarding) — kullanıcı v0.2.1 davranışına alışmışsa şaşırmasın.
- `[general] show_reload_banner = true` opt-in örneği + alt-screen caveat'i (vim/less içindeyken banner alt-screen buffer'a düşer ve program çıkışında kaybolur).
- `TAYF_DISABLE_BG_DETECT`'in `TAYF_DISABLE`'dan **farklı** olduğunu açıkla — biri test-only OSC 11 bypass (production'da kullanılmamalı), öteki kullanıcı-facing whole-binary bypass.

README satır sayısı artışı ~30-40 satır. Mevcut sections korunur.

---

## 4. Implementasyon detayları

### 4.1 Integration test `tests/integration_signal_hup.rs`

SIGHUP forward davranışını binary-level pinler. Portable-pty subprocess'inde tayf spawn et, child shell olarak `sh -c 'trap "echo HUP_RECEIVED; exit 0" HUP; sleep 5'` çalıştır, parent'tan tayf process'e `kill -HUP` gönder, child output stream'inde `HUP_RECEIVED` string'ini bekle.

```rust
//! Integration test: SIGHUP forwarding to child process group.
//!
//! Pins the v0.3.3 behavior change: SIGHUP is always forwarded to the
//! child process group, regardless of hot-reload state. Spawns tayf
//! under portable-pty (no config file → reload_tx is None at the
//! signal thread level), sends SIGHUP to the tayf process, and verifies
//! the shell's HUP trap fires.
//!
//! Pre-v0.3.3 behavior: this test would time out — SIGHUP was silently
//! dropped by the signal thread when reload_tx was None.

#[cfg(unix)]
#[test]
fn sighup_is_forwarded_to_child_process_group_without_hot_reload() {
    use std::time::Duration;
    // 1. Spawn `tayf` under portable-pty with TAYF_DISABLE_BG_DETECT=1
    //    (avoid OSC 11 hang) and no --config flag (reload_tx None at
    //    signal thread).
    // 2. Send `trap 'echo HUP_RECEIVED; exit 0' HUP; sleep 5\n` to stdin.
    // 3. Wait 500 ms for shell to install trap.
    // 4. Send SIGHUP to tayf process via nix::sys::signal::kill.
    // 5. Read stdout with a 2 s budget; assert "HUP_RECEIVED" appears.
    // 6. Wait for tayf to exit; assert exit_code (shell exit 0 via trap).
    // ...
}

#[cfg(unix)]
#[test]
fn sighup_is_forwarded_to_child_process_group_with_bypass() {
    // Same as above but spawn with --bypass. Bypass mode also has
    // reload_tx None; SIGHUP forwarding must still work.
    // ...
}
```

Test budgets: shell trap install için 500 ms; "HUP_RECEIVED" çıktısı için 2 s. macOS portable-pty constraints (v0.3.2 §3.1 D'de gözlendi: pty timing flaky); budget üst sınır 3 s. Failure mode: timeout → assertion fail (regression sinyali).

Test'in `TAYF_DISABLE_BG_DETECT=1` env'i set etmesi gerekiyor (v0.3.2 pattern; aksi takdirde macOS hang riski).

### 4.2 Integration test `tests/integration_bypass.rs`

Bypass mode'un davranışını binary-level pinler. İki kontrolü pinler:

```rust
//! Integration test: --bypass and TAYF_DISABLE bypass mode.
//!
//! Pins:
//! 1. With --bypass, tayf wraps the shell but does NOT inject SGR
//!    sequences into the output (output bytes are identical to raw shell).
//! 2. With TAYF_DISABLE=1 (env), same as #1 (env var path).
//! 3. CLI --bypass wins over TAYF_DISABLE=0 (precedence).

#[cfg(unix)]
#[test]
fn bypass_flag_skips_pattern_matching() {
    // 1. Spawn `tayf --bypass` under portable-pty.
    // 2. Send `echo error: failed\n` to stdin.
    // 3. Read stdout; assert "error: failed" appears WITHOUT any
    //    ANSI SGR bytes wrapping "error".
    // 4. Send `exit\n`; assert tayf exits cleanly.
}

#[cfg(unix)]
#[test]
fn tayf_disable_env_skips_pattern_matching() {
    // Same as above but TAYF_DISABLE=1 in env, no --bypass flag.
}

#[cfg(unix)]
#[test]
fn cli_bypass_overrides_env_disable_zero() {
    // Spawn with --bypass and TAYF_DISABLE=0 in env. CLI wins;
    // bypass active; no SGR injection.
}
```

`bg_detect` bypass yapıldığı için `TAYF_DISABLE_BG_DETECT=1` env'e gerek olmamalı (bypass mode zaten bg_detect çağırmıyor). Yine de defansif: portable-pty subprocess'inde test fixture default olarak `TAYF_DISABLE_BG_DETECT=1` set eder (v0.3.2 pattern).

### 4.3 Integration test `tests/integration_no_hot_reload.rs`

`--no-hot-reload` davranışı: file watcher thread spawn edilmediğini doğrula.

```rust
#[cfg(unix)]
#[test]
fn no_hot_reload_does_not_spawn_watcher() {
    // 1. Write a minimal config to a temp file.
    // 2. Spawn `tayf --config <path> --no-hot-reload` under portable-pty.
    // 3. Wait for tayf to fully start (poll for prompt or send echo).
    // 4. Modify the config file (write different bytes).
    // 5. Wait 500 ms (longer than DEBOUNCE_WINDOW).
    // 6. Send `echo error: still_old_rules\n`.
    // 7. Assert "error" is colored per the ORIGINAL config — not the
    //    edited one (reload didn't happen).
    // 8. Send SIGHUP to tayf; verify it's forwarded (shell trap fires)
    //    but config still doesn't reload (--no-hot-reload).
    // 9. Send `exit\n`; verify clean shutdown.
}
```

### 4.4 Test edilmeyen davranışlar (justification)

- **F3 banner'ın /dev/tty'a yazıldığı:** portable-pty subprocess'inin /dev/tty'ı stdout'tan farklı olabilir (process'in controlling terminal'i). Banner'ı stdout'ta görmek garanti değil. Banner write davranışı **manual smoke test** ile doğrulanır (release ceremony'de bir tester `echo > ~/.config/tayf/config.toml` ile reload tetikler ve banner'ı görür). Unit/integration test sadece "show_reload_banner = true geçilmesi panic etmiyor" + "banner kodu /dev/tty open failure'da panic etmiyor" düzeyinde kapanır.

- **Bypass mode'da config dosyası MEVCUTSA bile compile edilmediği:** integration test "SGR injection yok" kontrolü ile dolaylı pinlenir; explicit "Compiled::new çağrılmadı" assert'i mümkün değil (private state).

- **F2'nin SIGHUP'ı zaten hot-reload aktifken DE forward ettiği:** `integration_signal_hup` test'i hot-reload OFF case'i pinler. Hot-reload ON case için ayrı bir test gerekmez — `signals.rs` handler body single-branch SIGHUP arm'ı her iki path'ı da çalıştırıyor. Spec §3.4 pseudo-kodu reviewer için doğrulanabilir; bench/test surface şişirmenin değer/maliyet oranı düşük.

---

## 5. Cross-cutting kararlar (v0.3 umbrella mandate'leri)

### 5.1 Backward compatibility

- v0.3.2 config dosyaları shimless okunur. `[general]` block'a yeni `show_reload_banner` field eklendi ama `#[serde(default)]` — missing field = `false`. `deny_unknown_fields` typo'ları reddeder (mevcut davranış korunur).
- Mevcut CLI flag'leri (`--config`, `--no-color`, `--shell`, `--login`, `--theme`) signature aynı kalır. Yeni `--bypass` ve `--no-hot-reload` flag'leri additive.
- `Error` enum `#[non_exhaustive]`; yeni variant eklenmez.
- **SIGHUP davranış değişikliği** semver minor-kabul edilebilir: v0.2.1 davranışı dokümante edilmemiş accidental sink; v0.3.3 forward yön default-safe shell convention'una uyar (SIGINT/SIGTERM zaten forward'lanıyor). Prominent CHANGELOG notu.
- `Tayf::run` public signature değişmez. `Args` struct'a iki field eklendi — `Args` `#[non_exhaustive]` DEĞİL ama struct field eklenmesi clap derive'ın convention'ı ile uyumlu (kullanıcı `Args` instance'larını direkt construct etmiyor; `Args::try_parse_from_env()` yoluyla parse'lıyor).
- `pub(crate)` API'lar: `ReloadOrchestrator::spawn` signature'a `show_reload_banner: bool` parametresi eklendi. Bu `pub(crate)`, public API değil; iç refactor.

### 5.2 Performans bütçesi

`benches/throughput.rs` v0.3.2 baseline'ı (`benches/BASELINE.md`) korunur. v0.3.3 sonrası throughput **%20'den fazla düşmemeli**.

- F1 (bypass): hot path'tan **çıkış**. `apply_colors == false` path zaten mevcut; bypass mode bu path'ı tetikler. Bypass kullanıcısı için throughput **artar** (regex skip).
- F2 (`--no-hot-reload`): hot path etkilenmez. File watcher thread CPU/RAM tasarrufu — pure win.
- F2 (SIGHUP forward): signal thread'de tek `killpg` syscall eklenir. Microsecond cost, event-driven (signal delivery rate). Hot path değil.
- F3 (reload banner): reload event'inde tek `/dev/tty` open + write + flush. Mikrosaniye cost; reload event'leri saniye-skalalı (file save), hot path değil.

Bench mandate'i §5.6'da; F1/F2/F3 hiçbiri throughput regression yapamaz ama disciplined check zorunlu.

### 5.3 Güvenlik (CLAUDE.md §3 mandate'leri)

- **Bypass mode adversarial input:** bypass mode'da Pipeline yok, raw passthrough. Adversarial PTY output (escape sequence injection, OSC injection) terminal'e doğrudan ulaşır. Bu kullanıcının açık tercihi (bypass = "tayf'ı geç"); tayf hâlâ ANSI SM çalıştırmıyor olduğundan, kullanıcı kabul ediyor. README'de prominent not.
- **F1 env var read:** `bg_detect::env_truthy` `std::env::var` + ASCII lowercase + literal match. Allocation bounded (env string copy), no parser, no regex. ReDoS / memory exhaustion riski yok.
- **F2 SIGHUP forward:** `killpg(SIGHUP)` mevcut `forward_to_pgid` yolundan geçer; `signal_hook` SIGINT/SIGTERM ile aynı kod path'ı. Yeni surface yok.
- **F3 banner write:** `/dev/tty` open + 33-byte constant write + flush. Open failure swallow (sessiz fail). Banner bytes SGR-balanced; sequence escape injection riski yok (constant bytes, user input girmez). Adversarial config dosyası `show_reload_banner = true` set edebilir — bu zaten kullanıcı'nın kendi config'i; kullanıcı kendi kendine "saldırı" yapmıyor.
- **`/dev/tty` race:** Banner write reload thread'inden yapılır; main I/O thread `stdout`'a yazıyor (`runtime::run`). İki farklı fd; race yok. `/dev/tty` per-process resource; concurrent open OK.
- `cargo audit` ve `cargo deny` mevcut CI gate'leri korunur; yeni dep yok, advisory veya license sürprizi yok.

### 5.4 Dependency policy

Yeni direct dep YOK. Yeni dev-dep YOK.

`portable-pty` zaten `[dev-dependencies]` listesinde (v0.3.2 integration test'leri için). F1/F2/F3 integration test'leri aynı crate'i kullanır. `nix` zaten direct dep; `nix::sys::signal::kill` (integration test'lerde tayf'a SIGHUP göndermek için) mevcut feature flag'lerinde mevcut (`"signal"`).

### 5.5 Test stratejisi

- `src/cli.rs::tests`: yeni `--bypass` ve `--no-hot-reload` flag parse test'leri (§3.1).
- `src/config.rs::tests`: yeni `show_reload_banner` field parse test'leri (§3.5).
- `src/reload.rs::tests`: mevcut orchestrator test'lerine `show_reload_banner` parameter güncellemesi (false geçen); yeni `orchestrator_writes_banner_on_success_when_enabled` ve `orchestrator_no_banner_when_disabled` test'leri (gerçek banner emit'i test ortamında /dev/tty olmadığı için no-op; compile gate). Mevcut `reload_once`'a değişiklik yok, mevcut testler korunur.
- `src/bg_detect.rs::tests`: `env_truthy`'nin görünürlüğü değişti (`pub(crate)`), test surface aynı. Mevcut test'ler değişmez.
- `src/signals.rs`: tests modülü yok; SIGHUP davranış değişikliği integration test'inde pinlenir (§4.1).
- `tests/integration_signal_hup.rs`: yeni; SIGHUP forwarding pinler (no-config case + bypass case).
- `tests/integration_bypass.rs`: yeni; bypass mode davranış kontratını pinler.
- `tests/integration_no_hot_reload.rs`: yeni; file watcher spawn olmaması + reload olmaması.
- Mevcut integration test'ler etkilenmez; `tests/integration_bg_detect.rs` (v0.3.2) ve `tests/integration_smoke.rs` (v0.1) signature aynı kalır.
- CI matrix değişmez (Linux + macOS + audit/deny).

### 5.6 Release workflow

Memory'deki `tayf release workflow` + v0.3.2'deki bench mandate:

1. v0.3.3 commit'leri (CLI flag, config field, signals fix, reload banner, integration tests) main'e atomik push (her flag/feature ayrı commit).
2. CHANGELOG release-date placeholder (`[0.3.3] — TBD`) ile başla; release günü gerçek tarihiyle follow-up commit.
3. `cargo fmt && cargo clippy --all-targets -- -D warnings && cargo test` lokal pre-commit her commit'te.
3a. **`cargo bench --bench throughput`** developer makinesinde. `benches/BASELINE.md` (v0.3.2 baseline) ile karşılaştır. Herhangi bir grup %20'den fazla regresyon → release block; F1/F2/F3 hot path'a dokunmuyor, regression beklenmiyor ama disciplined check zorunlu. Temiz run sonrası `BASELINE.md` yeni rakamlarla güncellenir.
4. Push main → CI yeşil bekle. macOS runner'da `watch::drop_stops_debounce_thread` flake'i v0.3.2'de fix edildi; yine de prudent — `gh run rerun --failed` ile bir kez deneme bütçesi tut, ikincide gerçek defect olarak flag.
5. Manual smoke test on developer macOS machine: `tayf --bypass` ile shell session aç, raw passthrough doğrula; `tayf --no-hot-reload` + edit config doğrula; `tayf` + `show_reload_banner = true` + `kill -HUP` ile banner gör. (Banner /dev/tty'a yazıldığı için CI'de görsel doğrulama yok.)
6. Annotated tag `v0.3.3` push, mesaj: `tayf v0.3.3 — Escape hatches`.
7. CHANGELOG date bump commit.

### 5.7 Done tanımı

Release block-clear için zorunlu kontrol listesi:

- [ ] `cargo fmt --check` ✓
- [ ] `cargo clippy --all-targets -- -D warnings` ✓
- [ ] `cargo test` ✓ (lokal + CI; macOS dahil)
- [ ] `cargo bench --bench throughput` çalıştırıldı; tüm gruplarda regression < %20; `BASELINE.md` güncellendi.
- [ ] F1: `tests/integration_bypass.rs` (3 test) yeşil.
- [ ] F2: `tests/integration_signal_hup.rs` (2 test) yeşil.
- [ ] F2: `tests/integration_no_hot_reload.rs` (1 test) yeşil.
- [ ] F3: orchestrator unit test'leri yeni `show_reload_banner: bool` parametresiyle güncellenmiş ve yeşil.
- [ ] Manual smoke test (developer macOS): bypass + no-hot-reload + reload banner görsel doğrulandı.
- [ ] CHANGELOG `[0.3.3]` entry yazıldı (Added + Changed + Notes split, BEHAVIOR CHANGE prominent).
- [ ] README güncellendi (`--bypass`, `--no-hot-reload`, `show_reload_banner` dokümante + SIGHUP davranış değişikliği notu + `TAYF_DISABLE` vs `TAYF_DISABLE_BG_DETECT` ayrımı).
- [ ] `Cargo.toml` version bump (0.3.2 → 0.3.3).
- [ ] `cargo audit && cargo deny check` ✓.
- [ ] Senior Rust final code review (opus 4.7) approved.

---

## 6. Açık sorular

Yok. Tüm kararlar §2'de pinlenmiş; F1/F2/F3 mimari etki §3'te dosya bazlı detaylı; test stratejisi §4 + §5.5'te açık. Spec rev1 → opus 4.7 senior reviewer turunda yeni gözlem gelirse rev2'de adreslenir.

---

## 7. Bu doküman değiştirildiğinde

Spec v0.3.3 sub-version locked; içeriği bir implementation task açıldıktan sonra **rev2 / rev3** ile değiştirilebilir (review feedback → spec güncelleme normal döngü). Implementation tamamlandıktan sonra spec snapshot olarak kalır — gelecek bir release o sub-version'ın davranışını değiştirirse yeni bir spec dosyası açar, mevcut v0.3.3 spec'i değiştirilmez.
