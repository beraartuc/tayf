# tayf v0.10.0 — Public Flip & Distribution (Tasarım / Spec)

- **Tarih:** 2026-05-31
- **Sürüm hedefi:** `0.10.0` (ilk public sürüm; 0.x devam — semver-stability v1.0'a ertelendi)
- **Karakter:** BIG cycle. Çalışma-zamanı kodu (`src/`) **neredeyse** sabit; tek dar dokunuş `pty.rs` `TAYF_SESSION` marker. İşin gövdesi: CI runner sertleştirmesi + canlı imzalı-release infra + crates.io/Homebrew dağıtımı + public docs + tek-yönlü release seremonisi.
- **Geri-dönülebilirlik:** Bu cycle **tek-yönlü/zor-geri-dönüş** adımlar içerir (repo public toggle, crates.io publish). Her outward adım açık kullanıcı onayına bağlı (§11).

---

## §0 — Baseline & sequencing (v0.9.1 SHIPPED — 2026-05-31)

**v0.10.0, v0.9.1 üzerine kurulur.** v0.9.1 **shipped** (paralel session artık kapalı; tek session bu): **24-bit "Neon" default palette** — built-in rule'lar `Color::Rgb`'ye geçti (log_level hot-coral **+bold**, ipv4 azure **bold-OFF**, ipv6 indigo, url blue **tek underlined span**, duration amber, …); yeni **`classic` theme** (eski named-ANSI, terminal-adaptive, opt-in); light theme Neon'a recast (capture-group sub-color'lar dahil); docker/aws/k8s profile renkleri coral/rose/emerald'e harmonize. **Runtime logic DEĞİŞMEDİ** — yalnız style değerleri + theme/profile preset'leri + testleri. Baseline: `main @ 74e5ccb`, tag `v0.9.1`.

Baseline sonuçları:
- **Version bump (§10):** `0.9.1 → 0.10.0`.
- **§1:** v0.9.0 **ve v0.9.1** consume (aşağıda).
- **README (§9 F1) STALE:** v0.9.1 README'ye DOKUNMADI → built-in renk tablosu hâlâ eski named-ANSI'yi (`ipv4` "bold yellow" vb.) gösteriyor. Rewrite **Neon default'larını + 3 theme'i (dark/light/classic) + ipv4-bold-off + url-single-span**'i yansıtacak.
- **`pty.rs` v0.9.1'de DOKUNULMADI** → `TAYF_SESSION` marker çakışmaz. ✓ (teyit edildi)
- **Yeni tracked `docs/design/color-sets-preview.html`** (v0.9.1 commit'i, scan-clean) — A2'de değerlendir: public design-reference olarak **TUT** (öneri; küçük) ya da çıkar.
- **Yeni `assets/themes/classic.toml`** — Cargo.toml `include` glob'u (`assets/themes/*.toml`) otomatik kapsar (packaging OK).
- **Çakışma yok:** v0.9.1 src dokunuşları (`rules`/`themes`/`profiles`/`pipeline`/`reload`/`cli`) v0.10.0'ın tek src dokunuşu (`pty.rs` marker) ile ayrık.

---

## §1 — Önceki cycle'ı tüket (v0.9 ship + audit) — fold-or-defer

[[feedback-consume-prior-review]] mandate'i. v0.9.0 ship (`62f4dfc`) ve `docs/superpowers/reviews/2026-05-31-v0.9-systematic-security-audit.md` (verdict **CLEAN**, 5 🔵 informational) açık item'ları:

| Kaynak | Item | Disposition (v0.10.0) |
|---|---|---|
| v0.9 carryover | crates.io publish | **FOLD** → §7 |
| v0.9 carryover | Homebrew tap | **FOLD** → §7 |
| v0.9 carryover | GitHub repo public toggle | **FOLD** → §11 |
| v0.9 carryover | GH-release-create + signed binaries | **FOLD** → §6 |
| v0.9 carryover | Sigstore'u Public-Good instance ile re-verify | **FOLD** → §6 |
| v0.9 carryover | fuzz-smoke yeşil → `continue-on-error` kaldır (hard gate) | **FOLD** → §10 |
| audit 🔵 #1 | signal teardown reap→join `killpg` penceresi (ACKNOWLEDGED, kod değişmez) | **FOLD (doc-only)** → SECURITY.md notu §9 |
| audit 🔵 #2 | stale `fancy-regex` lock orphan | **FOLD** → §10 (version-bump lock rewrite'ında prune) |
| audit 🔵 #3 | `__bench__` `#[cfg]`-gated değil | **DEFER** → §13 (deliberate pattern; consumer'larını da gate'lemek scope-creep — audit'in kendi dispoziyonu) |
| audit 🔵 #4 | `integration_signals.rs` SIGWINCH testi spawn env'inde `TAYF_DISABLE_BG_DETECT` yok | **FOLD** → §10 |
| audit 🔵 #5 | pre-release version state | **FOLD** → §10 (version bump 0.10.0) |

Tek `DEFER`: `__bench__` cfg-gating (Karar 5 ile tutarlı). Diğer hepsi bu cycle'a foldlandı.

### v0.9.1 (Neon palette) consume
Küçük cycle (default colors); ayrı final cross-cutting review dokümanı yok. CHANGELOG `[0.9.1]` + plan `docs/superpowers/plans/2026-05-31-tayf-v0.9.1-neon-default-palette.md` consume edildi:
- **FOLD → §9 F1:** README built-in renk tablosu Neon'a güncellenir (stale; v0.9.1 README'ye dokunmadı).
- **FOLD → §9 F1 + install docs:** 3 theme (dark/light/classic) yansır; `classic` = named-ANSI opt-in.
- **Bilgi (README not'u):** Neon 24-bit SGR daha uzun → ağır-eşleşen stream'lerde per-line overhead artışı (yalnız byte-count, fonksiyonel değil); `--theme classic` hafif named-ANSI'ye döndürür.
- **Çakışma kontrolü:** v0.9.1 src dokunuşları (`rules`/`themes`/`profiles`/`pipeline`/`reload`/`cli`) v0.10.0'ın tek src dokunuşu `pty.rs` marker ile ayrık → çakışma yok.

**v0.9 audit'in metodolojik dersi (bu cycle'a taşınan):** "release infra'yı public-flip'ten ÖNCE build + dry-run et" → v0.9 `include_str!` packaging bug'ını ve gerçek MSRV floor'unu (1.88) bu sayede yakaladı ([[feedback-msrv-floor-from-ci-not-local]]). v0.10.0 bu dry-run'ı CANLI'ya çevirirken aynı disiplin: her şey CI'da yeşil + dry-run doğrulanmadan tag atılmaz ([[tayf-release-workflow]]).

---

## §2 — Kapsam & Non-Goals

### Kapsam
1. **CI runner sertleştirmesi** (`ci.yml`) — public-repo fork-PR RCE riski kapatma. **Güvenlik-kritik, en yüksek öncelik.**
2. **`release.yml` dry-run → canlı** — tag-trigger, imzalı binary'ler, SHA256SUMS, SBOM, GH Release, idempotent crates.io publish, Sigstore Public-Good verify.
3. **Dağıtım kanalları** — crates.io (ilk publish manuel, sonrası CI) + Homebrew tap + GH-release binary'leri. Üçü de bu sürümde.
4. **Always-on UX** — `TAYF_SESSION=1` child-env marker + güvenli rc-exec snippet + per-terminal config docs.
5. **Public file surface** — Türkçe AI-süreç artefaktlarını + public-build'i kıran/iç-debug dosyalarını HEAD'den çıkar; İngilizce contributor docs ekle.
6. **Public docs** — README rewrite, ARCHITECTURE.md, CONTRIBUTING.md, CHANGELOG, SECURITY.md, asciinema demo.
7. **Version bump + carryover temizliği** — 0.10.0, fuzz hard-gate, fancy-regex prune, signals-env.
8. **Release seremonisi** — tek-yönlü, her outward adımda onaylı.

### Non-Goals (bu cycle DIŞI)
- v1.0 semver-stability beyanı (0.x devam; v1.0 API/feature seti oturunca).
- Windows desteği (post-v1.0, SECURITY.md non-goal).
- `git history` rewrite (süreç dökümanlarını history'den purge etmek — tüm v0.x tag'lerini kırar, lansman öncesi riskli; tarama BLOCKER bulamadı, gerek yok).
- `__bench__` cfg-gating (DEFER, §1).
- Çoklu-profil/HTTP-profile, kalan TUI item'ları, performans (ayrı roadmap).
- Otomatik cross-repo formula-bump (ilk sürümde tap güncellemesi seremonide manuel; otomasyon sonraki iyileştirme).

---

## §3 — Kararlar (locked — AskUserQuestion ile çözüldü)

| # | Karar | Seçim | Not |
|---|---|---|---|
| 1 | Sürüm | **0.10.0** | 0.x devam; v1.0 ertelendi |
| 2 | crates.io kimlik | **API token** | İlk publish **manuel** (publish-new, kısa-expiry, revoke); sonrası CI **crate-scoped + publish-update**, korumalı `release` environment |
| 3 | Runner stratejisi | **Kısıtlı self-hosted** | Değişmez kural: fork-PR kodu self-hosted'a asla dokunmaz |
| 3b | Fork-PR CI | **GitHub-hosted smoke** | fmt+clippy+test ubuntu-latest'te; self-hosted yalnız push/tag |
| 4 | Dağıtım kapsamı | **Üçü de bu sürümde** | crates.io + brew + GH-release |
| 5 | Fuzz/🔵 | **Hard-gate + lock-prune + signals-env** | `__bench__` cfg-gate DEFER |
| 6 | README/demo | **Full public README + asciinema demo** | Capture script bende; cast'i kullanıcı kaydeder veya sentetik |
| 7 | Always-on | **`TAYF_SESSION=1` marker ekle** | pty.rs tek satır + TDD + docs |
| 8 | TR docs public posture | **`docs/superpowers/` + `tayf-tasarim.md` HEAD'den çıkar** | History'de kalır; İngilizce ARCHITECTURE.md özeti |

---

## §4 — Phase A: Public file surface + flip-öncesi güvenlik

### A1 — Secret/PII tarama (TAMAMLANDI, brainstorm fazında)
4-angle read-only workflow (652 commit + history + tree): **`cleanForPublicFlip: true`, 0 blocker.** Hiçbir gerçek secret/key/token/sensitive-file/binary-blob yok. Benign-and-dismissed: test fixture'ları (passwd/SSH/syslog), NIST SHA-256 test vektörleri, placeholder AWS ARN. Tek kozmetik bulgu (memory-path refs) → A2 ile soğuruldu.

**Seremoni-zamanı re-run:** public toggle'dan hemen önce (§11.3) son-durum üzerinde tekrar koşulur (final diff'te yeni bir şey girmediğini doğrula).

### A2 — HEAD'den çıkar (git rm; history'de kalır)
| Path | Sebep |
|---|---|
| `docs/superpowers/` (118 md, 4.9M) | Türkçe AI-süreç artefaktları (specs/plans/reviews); Karar 8 |
| `tayf-tasarim.md` | Türkçe master design; Karar 8 |
| `examples/repro_osc11_hang.rs` | İç debug repro artefaktı (gerçek kullanım örneği değil) |
| `.cargo/config.toml` | Global mold/clang linker zorluyor → mold'suz contributor + **GH-hosted smoke** build'ini KIRAR. mold CONTRIBUTING'de opsiyonel-dev olarak belgelenir; self-hosted runner kendi (uncommitted) config'ini tutabilir |

> **Not (A2 → §1 memory-path 🔵):** Memory-path içeren 6 dosyanın hepsi `docs/superpowers/` altında (5 plan + 1 spec) → removal onları otomatik götürür. HEAD'de ayrı path-scrub gereksiz; başka yerde `/Users/bera/.claude` ref'i yok (grep doğrulandı).

### A3 — Ekle (İngilizce, contributor-facing)
- **`ARCHITECTURE.md`** — `tayf-tasarim.md` + key spec'lerden damıtılmış, İngilizce, özlü mimari/tasarım özeti (modül haritası, I/O loop, pipeline SM, threat-model özeti → SECURITY.md/CLAUDE.md'ye link). Kaldırılan tasarım dökümanının public-uygun halefi.
- **`CONTRIBUTING.md`** — build (`cargo build`, opsiyonel mold hızlandırması), test (`cargo test` + `cargo bench`), kod standartları (fmt + clippy `-D warnings` + pedantic), commit/PR konvansiyonu (EN code / imperative commits), güvenlik-bildirme (→ SECURITY.md), MSRV (1.88).

### A4 — Güncelle
- **`README.md`** — full rewrite (§9'da detay). "See also" (satır 455-456) kaldırılan dökümanlara ref vermeyecek → ARCHITECTURE.md/CONTRIBUTING.md'ye repoint.
- **`CLAUDE.md`** — **disposition spec-review'da bağlanacak (OPEN-1, §14).** Öneri: **KEEP + stale-ref fix + hafif public-framing** (İngilizce mühendislik standartları contributor'a değerli; AI-assistant guide'ları giderek normal). 5 stale ref (`tayf-tasarim`/`docs/superpowers`/`superpowers:`) + "Project Layout" ağacı güncellenir. Alternatif (review'da seçilebilir): içeriği CONTRIBUTING.md+ARCHITECTURE.md'ye taşı, CLAUDE.md'yi de HEAD'den çıkar.

---

## §5 — Phase B: CI runner sertleştirmesi (`ci.yml`) — GÜVENLİK-KRİTİK

### Mevcut durum (risk)
`ci.yml` 5 job'un HEPSİ `runs-on: [self-hosted, Linux, X64]`: `test`, `audit`, `bench-regression`, `msrv`, `fuzz-smoke`. Trigger: `push` (main) + `pull_request` (main). **Repo public olunca fork-PR'lar bu job'ları self-hosted runner'da çalıştırır = kullanıcının makinesinde RCE.** (macOS CI job'u yok; macOS yalnız release matrix'inde.)

### Değişmez güvenlik kuralı
**Fork-PR kodu self-hosted runner'a ASLA dispatch edilmez.**

### Mekanizma
1. **Self-hosted job'lara trusted-context `if`-gate** (her 5 job'a job-seviyesi):
   ```yaml
   if: >-
     github.event_name != 'pull_request' ||
     github.event.pull_request.head.repo.full_name == github.repository
   ```
   → push (main) + same-repo PR çalışır; **fork PR atlanır** (runner'a dispatch edilmez).
2. **Yeni `smoke` job (GitHub-hosted)** — fork-PR'lar için:
   ```yaml
   runs-on: ubuntu-latest
   if: >-
     github.event_name == 'pull_request' &&
     github.event.pull_request.head.repo.full_name != github.repository
   ```
   Adımlar: checkout + toolchain (stable, rustfmt+clippy) + `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` + `cargo test`. Env: `TAYF_DISABLE_BG_DETECT=1` (OSC 11 hang, environment-general) + `RUST_TEST_THREADS=1` (defensif; reload-orchestrator hang self-hosted-spesifik olabilir ama GH-hosted'da doğrulanana dek serileştir).
   - **mold yok** → A2'deki `.cargo/config.toml` removal bu job'ı unblock eder (default linker).
3. **Repo ayarı (defense-in-depth):** Actions → "Fork pull request workflows from outside collaborators" = **"Require approval for all outside collaborators"**. Kullanıcı toggle'lar ya da onayıyla `gh api` (§11.4). Bu, fork-PR workflow'unun (smoke dahil) maintainer onayı olmadan çalışmamasını sağlar (CI-abuse/crypto-mining koruması).

### Neden güvenli (doğrulanacak invariant'lar — §13)
- tayf `pull_request` kullanıyor (`pull_request_target` DEĞİL) → fork-PR, **base branch'in** workflow tanımını çalıştırır; fork `if`-gate'i değiştiremez. GITHUB_TOKEN read-only + secret'lar fork-PR'a expose edilmez (public-repo default).
- `if: false` olan job runner'a dispatch EDİLMEZ (skip), fork kodu self-hosted'ta execute olmaz.

### Doğrulama (CI round-trip beklenir)
[[feedback-msrv-floor-from-ci-not-local]] dersi: GH-hosted smoke'un ilk koşusu, self-hosted-spesifik test-exclude'ların (RUST_TEST_THREADS, bg-detect) GH-hosted'da gerekli/yeterli olup olmadığını ortaya çıkarır. Bir CI round-trip bütçele; ilk sonucu "discovery" say, regression değil.

---

## §6 — Phase C: `release.yml` dry-run → CANLI

### Mevcut (dry-run)
`workflow_dispatch`-only; build matrix (ubuntu-latest + macos-14 + macos-13) → per-target `.sha256` → `attest-build-provenance@v3` (SLSA L2) → `cargo publish --dry-run` + `cargo cyclonedx` SBOM → artifact upload. **Sıfır `secrets.*`, publish yok.**

### Canlı eklemeler
1. **Trigger:** `on: push: tags: ['v*']` eklenir (`workflow_dispatch` dry-run için kalır). Canlı/dry-run ayrımı: `IS_RELEASE: ${{ github.event_name == 'push' }}` → publish/release adımları yalnız tag-push'ta.
2. **Birleşik SHA256SUMS:** tüm target binary'leri için tek `SHA256SUMS` dosyası (per-target `.sha256` yerine/yanında), release'e eklenir.
3. **Sigstore Public-Good verify (in-workflow):** `attest-build-provenance@v3` (public-good Fulcio/Rekor'a yazar) sonrası bir **verify adımı** — `gh attestation verify <binary> --repo beraartuc/tayf` — provenance'ın **public transparency log**'da doğrulandığını teyit eder. (v0.9 dry-run'da bu re-verify yapılmamıştı; carryover.) **Exact komut + instance OPEN-2 (§14), paralel review ampirik doğrular.**
4. **crates.io publish (idempotent):**
   - Job `environment: release` (korumalı; **required reviewer** = manuel onay kapısı, Actions UI'da bekler; `CARGO_REGISTRY_TOKEN` secret'ı bu environment'a scope'lu → build-matrix job'larına sızmaz).
   - **Idempotency guard:** publish'ten önce `https://crates.io/api/v1/crates/tayf/<version>` sorgulanır; 200 (var) → publish SKIP (log); 404 → `cargo publish --locked`. Bu, manuel-ilk-publish (§7) ile otomatik-sonraki-publish'i çatışmasız bir arada tutar + kazara çift-publish'i engeller.
5. **`gh release create`:** GitHub Release oluştur — binary'ler + `SHA256SUMS` + SBOM + provenance ekli; release notes CHANGELOG `[0.10.0]`'dan. (Projenin eski tag-only konvansiyonundan [[tayf-release-workflow]] EVRİM: GH-release-create + imzalı-binary artık seremoninin parçası.)
6. **Permissions:** minimal job-bazında (`contents: write` yalnız release-create job'ında; `id-token: write` + `attestations: write` attest job'ında; publish job'ı yalnız `release` environment secret'ı).

### Güvenlik
- Tag-trigger → yalnız tag push edebilen (maintainer) tetikler; fork-PR tag push edemez.
- `release` environment + required reviewer → tag push'tan SONRA bile publish/release manuel onay bekler (ikinci kapı, "her outward adımdan önce onay" §11 ile hizalı).
- Token: crate-scoped + publish-update + expiry; environment-scoped; loglanmaz.

---

## §7 — Phase D: Dağıtım kanalları

### D1 — crates.io
- **Cargo.toml hazır** (v0.9'da `include_str!` packaging fix → `assets/profiles/*.toml` include'da). Metadata tam: name/version/description/license(MIT OR Apache-2.0)/repository/homepage/authors/keywords/categories/readme/include. `tayf` adı **müsait** (404 doğrulandı).
- **İlk publish (manuel, seremoni §11.7):** kullanıcı makinesinden, kısa-ömürlü `publish-new`-scope'lu token ile `cargo publish --locked` → ismi rezerve eder + crate'i oluşturur; sonra token revoke. **En az ayrıcalık** (crate-scoped token, crate var olmadan oluşturulamaz). 
- **Sonraki publish'ler (CI):** `release.yml` idempotent publish (crate-scoped + publish-update token, `release` environment). v0.10.0 tag'inde idempotency-guard "zaten var" → SKIP; v0.10.1+ canlı publish.

### D2 — Homebrew tap
- **`beraartuc/homebrew-tayf`** public repo oluştur (henüz yok — doğrulandı; `gh repo create`, seremoni §11.9, onaylı).
- **`Formula/tayf.rb`** — release binary-indirme formülü: `on_macos`/`on_arm`/`on_intel` başına `url` (GH release asset) + `sha256` (SHA256SUMS'tan). Source-build fallback (`depends_on "rust" => :build`) opsiyonel.
- **Güncelleme akışı:** release artifact'ları yayınlandıktan SONRA (sha256 o an bilinir). İlk sürümde cross-repo otomasyon-token'ı saklamamak için tap güncellemesi **seremonide manuel** (gh repo-scope mevcut): SHA256SUMS oku → formula yaz → tap'e commit. `brew install beraartuc/tayf/tayf` ile test.
- Otomatik formula-bump (release.yml'den tap'e PR) → sonraki iyileştirme (Non-Goal).

---

## §8 — Phase E: Always-on UX (`TAYF_SESSION` marker)

### E1 — Marker (src — tek dar dokunuş)
`src/pty.rs:80-85` spawn path'inde, `spawn_command` öncesi:
```rust
cmd.env("TAYF_SESSION", "1");
```
Sabit değer; kullanıcı girdisi yok. Child shell + altındaki her şey miras alır. **Güvenlik:** benign — exec semantiğini (no-sh-c, direct argv) değiştirmez; audit'in spawn-path bulgusunu etkilemez (§13'te re-review).

### E2 — TDD
- **Test (RED→GREEN):** integration testi — `tayf` ile spawn edilen shell `echo $TAYF_SESSION` → `1` döndürür. (Mevcut `tests/common/` helper'ları + PTY-substring-SGR fragmentation farkındalığı [[feedback-pty-substring-sgr-fragmentation]]: marker-scan, has_sgr-helper değil.)
- Nested-tayf farkındalığı testi (opsiyonel): tayf içinde `TAYF_SESSION=1` zaten set → rc-guard'ın nasıl davranacağı dokümante.

### E3 — Docs (README "Always-on" bölümü)
İki yol:
- **(A) Terminal-emülatör başlangıç komutu** (önerilen): iTerm2/Terminal.app/Alacritty/kitty/GNOME Terminal/Konsole per-terminal `command = tayf`. Rekürsiyon yok (rc temiz).
- **(B) rc-exec (power-user), güvenli guard:**
  ```sh
  # ~/.zshrc / ~/.bashrc
  [[ $- == *i* && -t 1 && -z $TAYF_SESSION ]] && exec tayf
  ```
  `TAYF_SESSION` guard'ı rekürsiyonu keser (marker olmadan sonsuz döngü olurdu). Interactive + TTY check'leri non-interactive/SSH/scp'yi korur.

---

## §9 — Phase F: Public docs

### F1 — README.md (full rewrite)
- **Stale "v0.1 skeleton" banner kaldır** (artık 0.10.0).
- **Badge'ler:** CI status, crates.io version, license, MSRV (1.88).
- Tek-cümle pitch + **asciinema/GIF demo** (F4).
- **Install:** (1) `brew install beraartuc/tayf/tayf`; (2) `cargo install tayf`; (3) pre-built signed binary (GH release, SHA256SUMS + `gh attestation verify` ile doğrulama). **Her yöntemde PATH netliği** (brew otomatik; cargo `~/.cargo/bin`; binary `/usr/local/bin`).
- **Always-on** bölümü (§8 E3).
- Quickstart, özellikler (built-in pattern tablosu **Neon default'larıyla güncellenir** — `ipv4` azure/bold-off, `url` tek underlined span, `log_level` coral+bold; eski named-ANSI renkleri DEĞİL), themes (**dark/light/classic** — `classic` = named-ANSI opt-in, terminal-adaptive), profiles, escape hatches, TUI-compat, known-limitations.
- **Security posture** → SECURITY.md linki (CLAUDE.md §3 referansı CLAUDE.md disposition'a göre güncellenir).
- **Contributing** → CONTRIBUTING.md; **See also** → ARCHITECTURE.md (kaldırılan tasarım dökümanlarına ref YOK).
- License (MIT OR Apache-2.0).

### F2 — CHANGELOG.md
`## [0.10.0] - <release-date>` (Keep a Changelog). Bölümler: Added (TAYF_SESSION, public dağıtım, ARCHITECTURE/CONTRIBUTING), Changed (CI runner stratejisi, release.yml canlı, version), Removed (TR süreç-dökümanları HEAD'den, .cargo/config.toml, repro example), Security (fork-PR runner sertleştirme, token-scoping, signal-window ACKNOWLEDGED notu). Tarih [[tayf-release-workflow]] gereği tag SONRASI gerçek release gününe bump'lanır.

### F3 — SECURITY.md
- "Supported Versions" tablosu: `0.10.x ✅ / < 0.10 ❌`.
- Reporting: e-posta korunur; public repo için "open an issue with `security` label" alternatifi gözden geçirilir (private disclosure tercih).
- **Signal-teardown-window ACKNOWLEDGED notu** (audit 🔵 #1) — threat-model bölümüne tek paragraf: reap→join arası `killpg` penceresi, `ESRCH`-benign, PTY-stream'den attacker-controlled değil, tmux/script/sudo'nun da kabul ettiği standart yarış; `pidfd` ile kapanır.

### F4 — asciinema demo
- **Capture script** (bende): tayf'ın colorize'ını gösteren komut dizisi (ör. `tail` log → renkli IP/level/timestamp; `ls -l` → permission/filename).
- **Cast:** kullanıcı `asciinema rec` ile kaydeder VEYA sentetik `.cast` üretirim. README'ye SVG (svg-term) veya asciinema.org linki. **demo recording kullanıcı-driven olabilir** (gerçek interaktif oturum) — README full şimdi, cast follow-up'a kayabilir (kullanıcı kararı, ama "demo sonra" değil "full" seçildi → en azından sentetik/scripted cast hedeflenir).

---

## §10 — Phase G: Version bump + carryover temizliği

- **Version:** `Cargo.toml` `0.9.1 → 0.10.0` (baseline §0: v0.9.1 üzerine); `Cargo.lock` (tayf entry) + SECURITY.md/README badge ref'leri.
- **fuzz-smoke hard-gate:** `ci.yml:191` `continue-on-error: true` KALDIR + `ci.yml:187-190` TODO yorumunu güncelle/sil ([[feedback-stale-dead-code-reason-drift]]: stale "until X lands" temizliği). fuzz-smoke `if`-gate'i §5 ile uyumlu (self-hosted, push/tag-only). (Runner'da yeşil geçtiği v0.9'da doğrulandı.)
- **fancy-regex lock prune:** version-bump lock-rewrite'ında orphan'ı (`Cargo.lock:558` package + `:1840` edge) düşür. **Doğrula:** `cargo tree -i fancy-regex` boş (zaten öyle) + lock'tan kalktı + `cargo deny check` + `cargo audit` yeşil. Doğal düşmezse implementer araştırır (audit: "next natural lock-rewriting op").
- **signals-env:** `tests/integration_signals.rs` (SIGWINCH testi) — `common::spawn_for_interaction` çağrısının spawn env'ine `TAYF_DISABLE_BG_DETECT=1` ekle (CI global env'e bağımlılığı kaldır; local-flake hygiene). Helper imzası gerekiyorsa `tests/common/mod.rs` genişletilir.
- **`__bench__` cfg-gating:** DEFER (§1, §13).

---

## §11 — Release seremonisi (tek-yönlü, her outward adımda ONAY)

Sıra (🔒 = açık kullanıcı onayı şart):

1. Tüm v0.10.0 işi `main`'e iner (feature branch → FF veya self-PR; subagent-driven impl).
2. Push `main` → **CI green BEKLE** (self-hosted full matrix; fork-smoke push'ta tetiklenmez). [[tayf-release-workflow]]. İlk koşu Linux/env sürprizi çıkarabilir → fix-commit bütçele.
3. 🔒 **[ONAY] repo public toggle ÖNCESİ:** son-durum secret-scan re-run (§A1) → kullanıcı onayı → **repo'yu public yap** (kullanıcı flip'ler veya onayıyla `gh repo edit --visibility public`). Runner-fix zaten merged → fork riski kapalı.
4. 🔒 **[ONAY] Repo Actions fork-onay ayarı:** "require approval for all outside collaborators" (kullanıcı veya onaylı `gh api`).
5. 🔒 **[ONAY] crates.io ilk publish (manuel, §7 D1):** kullanıcı makinesinden kısa-expiry `publish-new` token → `cargo publish --locked` → doğrula (crates.io sayfası) → token revoke.
6. 🔒 **[ONAY] tag `v0.10.0` push** → `release.yml` tetiklenir.
7. Build/sign/SBOM çalışır; **`release` environment'ta DURUR** (required reviewer).
8. 🔒 **[ONAY] environment approve** → crates.io publish (idempotent: v0.10.0 zaten var → SKIP) + `gh release create` (binary'ler + SHA256SUMS + SBOM + provenance).
9. **Doğrula:** crates.io sayfası, `gh attestation verify` (Public-Good log), GH release sayfası, SHA256SUMS. [[feedback-transcribe-measured-numbers]]: checksum/digest doğrulamaları tee→Read→verify.
10. 🔒 **[ONAY] Homebrew tap (§7 D2):** `beraartuc/homebrew-tayf` create → `Formula/tayf.rb` (released sha256) → commit → `brew install beraartuc/tayf/tayf` test.
11. CHANGELOG tarih-bump follow-up commit ([[tayf-release-workflow]]).
12. Memory: `project-v0-10-0-shipped` yaz.

> **Sıralama notu:** public-flip (3) tag/release'den (6-8) ÖNCE → repository/homepage linkleri ve GH release public görünür. crates.io ilk-publish (5) repo-visibility'den bağımsız ama crate'in repository linki resolve etsin diye public-flip sonrası. Runner-fix (2'de merged) public-flip'ten önce → fork RCE penceresi hiç açılmaz.

---

## §12 — Test stratejisi

- **TDD (test-first) — testlenebilir mantık:** `TAYF_SESSION` marker (integration test, §8 E2). Bu cycle'ın tek src-davranış değişikliği; RED→GREEN→commit.
- **CI-validated infra:** `ci.yml` / `release.yml` değişiklikleri — yerel `act`/dry-run + ilk gerçek CI koşusu doğrular ([[feedback-msrv-floor-from-ci-not-local]] dersi: CI authoritative). release.yml canlı-publish path'i tag'e dek "armed-not-fired"; idempotency-guard + environment-gate ile güvenli.
- **Mevcut test suite byte-korunur:** marker dışında src dokunulmadığı için lib+integration testleri değişmez (signals-env fix hariç — o testin env'ini sağlamlaştırır, assertion'ı değil). [[feedback-enumerate-tests-for-invariant-claims]]: "değişmez" iddiası testleri adıyla listele + her assertion'ı kontrol et (impl fazında).
- **Formula testi:** `brew install` smoke (seremoni §11.10).
- **Demo:** scripted/sentetik cast üretimi doğrulanır (render).

---

## §13 — Güvenlik değerlendirmesi (public-flip threat model)

Bu cycle'a özgü, security-review skill + paralel review + final cross-cutting doğrulayacak:

1. **Fork-PR RCE (self-hosted):** §5 mekanizması. Doğrulanacak invariant'lar: (a) `pull_request` (not `_target`) → fork base-workflow çalıştırır; (b) `if`-gate'li job runner'a dispatch edilmez; (c) GITHUB_TOKEN read-only + fork-PR'a secret yok; (d) repo-onay-ayarı smoke'u da gate'ler.
2. **crates.io token:** crate-scoped + publish-update + expiry; `release` environment-scoped (build-matrix'e sızmaz); loglanmaz; ilk-publish token kısa-expiry + revoke. Doğrulanacak: token hiçbir log/echo/artifact'a düşmez.
3. **TAYF_SESSION marker:** sabit env-injection; exec semantiği değişmez; nested-tayf benign (double-colorize yok çünkü rc-guard veya kullanıcı bilir). Re-review spawn-path.
4. **release.yml supply-chain:** GHA SHA-pin korunur; yeni adımlar (gh release create, attestation verify) script-injection yüzeyi açmaz (matrix sabit, `github.event.*` yalnız boolean'a). cargo tools version-pinned. SBOM graph-based (fancy-regex prune sonrası tutarlı).
5. **Public-repo açılışı:** secret-scan CLEAN (A1); `.cargo/config.toml`/repro removal contributor-build'i bozmaz; CLAUDE.md/README stale-ref temizliği dangling-link bırakmaz.
6. **DEFER — `__bench__` cfg-gating:** publish build'inde `#[doc(hidden)]` thin-wrapper kalır (v0.5.3 deliberate pattern; behavior-neutral; adversarial-input-unreachable). §1 dispositionu; bu cycle'da risk değişmez.

---

## §14 — Açık sorular / divergences (paralel review çözer)

- **OPEN-1 — CLAUDE.md disposition:** keep+fix (öneri) vs migrate-to-CONTRIBUTING+remove. Spec-review'da bağla. (§4 A4)
- **OPEN-2 — Sigstore Public-Good verify exact mekanizma:** `gh attestation verify` vs `cosign verify-attestation`; hangi log/instance; in-workflow verify + seremoni manuel re-verify komutları. Release-eng/security senior ampirik doğrular. (§6.3)
- **OPEN-3 — İlk-publish token scoping:** crate var-olmadan crate-scoped token oluşturulamaz → ilk publish account-scoped publish-new + kısa-expiry + revoke (öneri: manuel). Alternatif (CI ilk-publish, geniş token) reddedildi. Release-eng senior token-scope mekaniğini + crates.io ilk-publish akışını doğrular. (§7 D1)
- **OPEN-4 — `if`-gate ifadesi doğruluğu:** `github.event.pull_request.head.repo.full_name == github.repository` fork-detection + push'ta short-circuit. CI/release-eng senior GHA semantiğini + `pull_request` vs `_target` invariant'ını doğrular. (§5)
- **OPEN-5 — fancy-regex prune mekaniği:** version-bump lock-rewrite'ında doğal düşer mi yoksa explicit `cargo update` mi gerekir; deny/audit yeşil kalır mı. (§10)
- **OPEN-6 — GH-hosted smoke test-exclude'ları:** RUST_TEST_THREADS/bg-detect GH-hosted'da gerekli/yeterli mi (ilk CI koşusu discovery). (§5)

---

## §15 — Forward-pointers (post-0.10.0)

- Otomatik cross-repo Homebrew formula-bump (release.yml → tap PR, fine-grained token).
- crates.io Trusted Publishing'e (OIDC) geçiş (token saklamayı bırakmak) — bu cycle token seçildi; gelecekte değerlendirilir.
- `__bench__` cfg-gating (consumer'larıyla birlikte) — DEFER.
- v1.0 semver-stability beyanı (API/feature seti oturunca; Windows büyük gelecek-break ondan önce/sonra).
- Sentetik demo → gerçek asciinema cast (kullanıcı kaydı).

---

## §16 — Süreç (tayf ceremony — BIG cycle)

1. ✅ Brainstorm (preconditions + 8 karar + secret-scan).
2. ✅ Spec (bu doküman); §1 v0.9 consume.
3. **Paralel spec review** ([[feedback-spec-phase-parallel-review]]): iki opus senior paralel — **Rust senior** (marker/TDD, lock-prune, test-invariant, CLAUDE.md/README ref-tutarlılık) + **release-eng/security senior** (OPEN-2/3/4 ampirik: Sigstore Public-Good, token-scoping, `if`-gate GHA semantiği, fork-PR `pull_request` invariant'ı, repo-onay-ayarı). Bulgular spec rev1'e foldlanır.
4. Plan (`superpowers:writing-plans`).
5. Subagent-driven TDD impl (logical-group dispatch: A-file-surface / B-ci / C-release / D-dist / E-marker / F-docs / G-version-carryover).
6. `security-review` skill (public-flip diff) + final cross-cutting review ([[feedback-cross-cutting-review-value]]).
7. Release seremonisi (§11) — fiili, gated.

EN code / TR convo. Yeni dep minimal + justify ([[feedback-dependency-minimalism]]). Checksum/digest tee→Read→verify ([[feedback-transcribe-measured-numbers]]).
