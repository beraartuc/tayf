# tayf v0.1 — Tasarım ve Karar Dokümanı

**Tarih:** 2026-05-21
**Kapsam:** v0.1 "Çalışan İskelet" (master design `tayf-tasarim.md` §11)
**Onay durumu:** Brainstorming sonucu, kullanıcı + bağımsız senior Rust engineer review onaylı.

Bu dokümanın amacı: master tasarım dokümanı (`tayf-tasarim.md`) yüksek seviye kararları çoktan vermiş. v0.1'i implemente etmek için açık kalan noktaları kapatmak, brainstorming sırasında alınan kararları kayıt altına almak, senior review'dan gelen mimari düzeltmeleri uygulamak.

Master tasarım hâlâ otoritedir. Çatışma olursa, master daha sonra revize edilir; bu doc her zaman v0.1 için kanonik.

---

## 1. Brainstorming Kararları

Sırayla netleştirildi; her satır master tasarımdaki bir açık soru ya da boşluğu kapatıyor.

| # | Karar | Master doc referansı |
|---|---|---|
| 1 | Master `tayf-tasarim.md` korunur. v0.1 için bu ek doc yazılır. | — |
| 2 | Hedef platformlar: Linux + Unix + macOS, day 1. | §1 (Hedefler) |
| 3 | Shell discovery: `$SHELL` → `/etc/passwd` (`getpwuid`) → `/bin/sh`. Non-login default; `--shell <path>` ve `--login` flag'leri override. | §12 Q4, Q5 |
| 4 | v0.1 hardcoded pattern seti (7 adet): IPv4, IPv6, MAC, log seviyesi, HTTP status, FQDN, süre metriği (ör. `20.291 ms`). TOML config v0.2. | §11 v0.1, §12 Q6 |
| 5 | Lisans: dual MIT + Apache-2.0. | §12 (yeni karar) |
| 6 | Terminal mode kontrolü: `nix::sys::termios` (crossterm değil). | §4 |
| 7 | Cargo: tek crate, `lib + bin`. Workspace YAGNI. | §4 (yeni karar) |
| 8 | CI: post-v0.1. Lokal `cargo fmt && clippy && test` ile başla, olgunlaşınca GitHub Actions. | §9 (yeni karar) |
| 9 | Test stratejisi: pure logic için TDD (test önce), PTY/signal/raw-mode için integration smoke. `proptest` opsiyonel v0.1'de. | §11 v1.0 (öne çekildi) |
| 10 | v0.1'e **minimal alt-screen detection** eklenir (`\x1b[?1049h`/`l` ve eski varyantları). Bu modda pipeline passthrough. Tam VTE entegrasyonu hâlâ v0.3. | §6.3 (öne çekildi — vim/htop'un v0.1'de çalışması için zorunlu) |

---

## 2. v0.1 Scope

### Kapsam İçi

- PTY açma, child shell spawn, iki yönlü byte kopyalama (`portable-pty`)
- Stdin raw mode + RAII Drop guard + panic hook fallback
- `SIGWINCH` → PTY resize
- `SIGINT` / `SIGTERM` → child process group forwarding
- Child exit code propagation
- 7 hardcoded pattern (yukarıdaki karar 4) ile satır-bazlı renklendirme
- Shell discovery cascade (karar 3)
- CLI flag'leri: `--shell <path>`, `--login`, `--no-color`, `--help`, `--version`
- `isatty(stdout)` kontrolü → pipe edildiyse rules engine tamamen bypass, byte-for-byte forward (renk byte'ı eklenmez, mevcut renkler de strip edilmez)
- Minimal alt-screen detection: detect → pipeline'ı passthrough'a çevir
- `TAYF_LOG` env var ile `tracing` (stderr only, default off)
- `--version` çıktısında build SHA ve `rustc` versiyonu

### Kapsam Dışı (v0.2+ için)

- TOML config dosyası ve hot reload
- Tam `vte`-tabanlı VTE parser entegrasyonu
- Mevcut SGR state preservation (master §6.2)
- OSC sequence handling (OSC 7, 8, 133 dahil — master §6.7)
- Bracketed paste mode özel handling (master §6.8)
- Mouse tracking özel handling (master §6.9)
- Capture group bazlı renklendirme (master §6.13)
- Çakışan match önceliği (master §6.14) — v0.1'de "ilk eşleşme kazanır"
- RegexSet/Aho-Corasick performans optimizasyonu (master §6.10)
- Profile system
- Windows desteği

---

## 3. Mimari

Master §3 yüksek seviye veri akışını veriyor. Bu bölüm v0.1 modül sınırlarını, senior review'dan gelen düzeltmelerle birlikte tanımlar.

### 3.1 Modül Listesi

```
src/
├── main.rs          # CLI entry, ExitCode mapping
├── lib.rs           # public facade: Tayf::run(args) -> Result<ExitCode, Error>
├── cli.rs           # clap argümanları
├── error.rs         # tek tayf::Error enum (thiserror-derived)
├── shell.rs         # ShellSpec discovery + --login resolution
├── pty.rs           # PtySession (+ into_parts decomposition)
├── tty_guard.rs     # TtyGuard: termios snapshot + Drop + panic hook
├── signals.rs       # spawn_handler(resizer, child_pgid) -> SignalGuard
├── runtime.rs       # iki thread orchestration + shutdown protocol
├── pipeline.rs      # output stream: alt-screen state machine → buffer → rules
├── line_buffer.rs   # UTF-8 safe accumulator, timeout flush, 64KB hard cap
├── rules.rs         # Compiled { set: RegexSet, individuals, styles } + builtin patterns
├── style.rs         # Style + to_sgr() (audit-gated: only SGR allowed)
├── terminfo.rs      # color depth detection + isatty checks
├── logging.rs       # tracing init from TAYF_LOG
└── version.rs       # --version build info
```

```
tests/
├── common/mod.rs    # spawn_in_pty fixture, helpers
└── integration_smoke.rs

benches/
└── throughput.rs    # criterion baseline (perf §7 targets)
```

### 3.2 Public API Surface (sadece `lib.rs` aracılığıyla)

```rust
// lib.rs
pub use cli::Args;
pub use error::Error;

pub struct Tayf;
impl Tayf {
    /// Top-level entry. Owns the session, the TTY guard, signal installation,
    /// and joins the runtime threads.
    pub fn run(args: Args) -> Result<std::process::ExitCode, Error>;
}
```

Diğer her şey `pub(crate)`. Public surface bilinçli olarak küçük: tayf bir kütüphane değil, integration test'lerin lib API üzerinden çalışabilmesi için bu kadarı yeterli.

### 3.3 `PtySession::into_parts` Dekompozisyonu

`Session` overloaded olmasın diye: `PtySession::into_parts(self) -> (Reader, Writer, Resizer, ChildHandle)`.

- `Reader` — output thread'in tek owner'ı, master read tarafını owned olarak tutar.
- `Writer` — input thread'in owner'ı, master write tarafını owned olarak tutar. Ayrı bir fd (dup'lanmış) olduğu için `shutdown()` Reader tarafını etkilemez; sadece input thread'i sonlandırmak için kullanılır.
- `Resizer` — signal thread'in owner'ı; içinde master fd'nin ayrı bir dup'u var, `ioctl(TIOCSWINSZ)` çağırır. Thread-safe; signal thread'inden güvenle çağrılabilir.
- `ChildHandle` — main thread'in owner'ı; `wait()`, `exit_code()`, ve child PGID accessor'ı sağlar.

Hepsi `'static` ownership ile move edilir; `Arc`/`Mutex` yok.

Bu split, herhangi bir `Arc<Mutex<PtySession>>` ihtiyacını ortadan kaldırır.

### 3.4 Shutdown Protokolü (v0.1'de en kritik tasarım kararı)

Numaralı sıra:

1. **Child exit eder** (normal exit, signal, ne sebeple olursa).
2. **Output thread** master.read() çağrısı:
   - Linux'ta `EIO` döner.
   - macOS'ta `Ok(0)` döner.
   - Her ikisi "child kapandı, drain et" olarak yorumlanır.
3. Output thread `line_buffer.flush_partial()` çağırır (regex uygulanmadan, partial line as-is); stdout'a yazar.
4. Output thread temiz exit eder. Main thread'e bunun bilgisi yok (heuristic gerek yok).
5. **Main thread** `child.wait()` üzerinden child reap'i tetikler ve exit kodunu alır.
6. Main thread `master_writer.shutdown()` çağırır — master write fd kapanır.
7. **Input thread** bir sonraki `master.write_all(...)` çağrısında `EPIPE` alır. Loop'tan çıkar.
   - **v0.1 limit:** input thread şu anda `stdin.read()`'te bloke ise, kullanıcı bir karakter yazana kadar bekler. Bu pratikte sorun değil çünkü:
     - Child exit'te terminal zaten kullanıcıya geri döner.
     - Main thread input thread'in `JoinHandle`'ını `join` etmek yerine `drop` eder; thread `main` return ettiğinde process exit ile birlikte OS tarafından sonlandırılır (Rust thread'leri default'ta `join`'lenmediği takdirde process exit'inde sonlanır).
   - v0.2+ planı: `nix::poll::poll` ile stdin + self-pipe (eventfd ya da pipe pair) wakeup mekanizması; ana thread shutdown sinyali bu pipe'a yazar, poll uyanır, input thread temiz exit eder.
8. **Signal thread** `SignalGuard::drop` üzerinden `signal_hook::Signals::halt()` ile durdurulur ve `join`'lenir.
9. **`TtyGuard::drop`** termios'u restore eder.
10. `main` `ExitCode`'u döner.

Exit code mapping:
- Child exit kodu varsa, onu kullan.
- CLI parse hatası: `64` (`EX_USAGE`).
- PTY/signal/internal: `71` (`EX_OSERR`).
- Logic error (unreachable!): `70` (`EX_SOFTWARE`).
- Crash: `134` (SIGABRT) veya signal+128 — `main` dönmeden gelir, kernel set eder.

### 3.5 Alt-Screen Parser (State Machine)

`pipeline.rs` içinde 5-state machine:

```
GROUND   ──ESC (0x1b)──► ESC_SEEN   [hold ESC]
ESC_SEEN ──'['─────────► CSI         [hold "ESC ["]
ESC_SEEN ──other───────► GROUND      [emit held + char]
CSI      ──'?'─────────► QUESTION    [hold "ESC [ ?"]
CSI      ──other───────► GROUND      [emit held + char]
QUESTION ──digit───────► DIGITS      [accumulate digit]
QUESTION ──other───────► GROUND      [emit held + char]
DIGITS   ──digit───────► DIGITS      [accumulate digit]
DIGITS   ──'h'─────────► GROUND      [num ∈ {1049, 1047, 47} → enter alt-screen; emit held + 'h']
DIGITS   ──'l'─────────► GROUND      [same nums → exit alt-screen; emit held + 'l']
DIGITS   ──other───────► GROUND      [emit held + char]
```

**Önemli incelikler:**
- "Hold" = state machine matching halinde olan byte'ları biriktirir; transition'da ya tüketir ya da olduğu gibi emit eder. State machine **byte yutmaz**; alt-screen mode toggle'ı dışında byte stream üzerinde hiçbir değişiklik yapmaz.
- State chunk'lar arasında korunur. Bir `\x1b[?1049h` sequence'i iki ayrı `read()` chunk'ı arasında bölünebilir — state machine bunu doğal olarak handle eder.
- 7-byte trailing window saklama gerektirmez; state machine zaten incremental.
- Alt-screen aktifken `pipeline.rs` byte'ları rules engine'e göndermez, doğrudan stdout'a yazar. State machine yine de çalışmaya devam eder ki `\x1b[?1049l` (exit) görebilsin.
- Çıkış varyantı (eski `\x1b[?47l` ile birlikte): `DIGITS('h')` ve `DIGITS('l')` üzerine ek olarak num=`1047` ve `47`'yi de aynı şekilde kabul eder (modern xterm kullanan tüm terminaller `1049`'u tercih eder ama eski kod yolu hâlâ destekleniyor olmalı).

### 3.6 Rules Engine — Compiled Shape

```rust
pub(crate) struct Compiled {
    pub(crate) set: regex::bytes::RegexSet,         // for fast match-any check (unused in v0.1, populated for v0.4)
    pub(crate) individuals: Vec<regex::bytes::Regex>, // for span retrieval
    pub(crate) styles: Vec<style::Style>,            // parallel to individuals
}
```

`Compiled` `pub(crate)`; dış API'ye sızmaz. v0.1 sadece `individuals` üzerinde iterasyon yapar. `set` field'ı v0.1'de populate edilir ama kullanılmaz — v0.4'te RegexSet-bazlı fast-path devreye alındığında veri yapısı zaten doğru şekilde mevcut olsun diye. `#[allow(dead_code)]` ile clippy susturulur; commit mesajında "reserved for v0.4 RegexSet fast-path" notu.

Match algoritması v0.1 için: ilk eşleşen kazanır (config tanımlama sırası). Çakışma resolution v0.5'te.

### 3.7 Style → SGR Audit Gate

`Style::to_sgr(&self) -> String` sadece `\x1b[…m` ve `\x1b[0m` üretir. Test:

```rust
#[test]
fn to_sgr_only_emits_sgr_sequences() {
    let re = regex::Regex::new(r"^\x1b\[[0-9;]+m$|^\x1b\[0m$").unwrap();
    for style in all_test_styles() {
        let sgr = style.to_sgr();
        assert!(re.is_match(&sgr), "style {style:?} emitted non-SGR: {sgr:?}");
    }
}
```

Bu, escape injection saldırı yüzeyini bir fonksiyona daraltır.

---

## 4. Hata Modeli

Tek `tayf::Error` enum, `thiserror` ile:

```rust
#[derive(Debug, thiserror::Error)]
pub enum Error {
    #[error("could not determine shell: {0}")]
    ShellDiscovery(String),
    #[error("PTY operation failed: {0}")]
    Pty(#[from] std::io::Error),
    #[error("terminal control failed: {0}")]
    Tty(#[from] nix::errno::Errno),
    #[error("regex compilation failed: {0}")]
    RegexCompile(#[from] regex::Error),
    #[error("signal installation failed: {0}")]
    Signal(std::io::Error),
    #[error("line buffer exceeded {cap} bytes; flushing as-is")]
    BufferOverflow { cap: usize },  // log only, not fatal
}
```

`Error` üzerinden çıkan her `Display` cümlesi: (a) ne başarısız, (b) neden, (c) kullanıcı ne yapmalı. `main.rs` bunları stderr'e yazar, exit code'a çevirir.

Built-in pattern'lerin compile etmemesi imkansız (test ile garanti edilir). `rules::load_builtins()` içinde:

```rust
.unwrap_or_else(|e| {
    unreachable!("builtin pattern must compile: {e}")
})
```

CLAUDE.md §2 "proven-unreachable paths" izniyle.

---

## 5. Test Stratejisi

### Unit testler (TDD ile yazılır)

- `style::Style::to_sgr` — audit gate testi (yukarıda)
- `line_buffer::LineBuffer` — chunk birleşimi, UTF-8 sınırı, 64KB cap davranışı, timeout flush
- `rules` — her pattern için match/no-match/edge case
- `shell::discover` — $SHELL set/unset, /etc/passwd fallback (mock), --shell override
- `pipeline::AltScreenStateMachine` — chunk boundary'lerde split edilmiş escape'ler

### Integration testler (smoke)

`tests/integration_smoke.rs`:

- `spawn_in_pty("echo 192.168.1.1")` → output IPv4 SGR'ı içermeli
- `spawn_in_pty` ile geçici dosyaya `vim`/`less` aç (alt-screen'e girer) → output'ta tayf SGR injection olmamalı; sadece vim/less'in kendi ANSI byte'ları görünmeli
- `spawn_in_pty("exit 42")` → tayf exit code 42 dönmeli
- `spawn_in_pty("false")` → exit code 1
- SIGWINCH simulation → master resize çağrıldı doğrulaması

### Manuel doğrulama (her milestone öncesi)

- `tayf` → interactive shell, `ls`, `ping 8.8.8.8`, `cat /etc/hosts`, `journalctl -n 50` çalıştır, renkler doğru mu
- `tayf` içinde `vim`, `htop`, `less /etc/services` → alt-screen modunda hiçbir renk leak'i yok
- `tayf` içinde `Ctrl-C` `ping` → forward doğru, terminal restore
- `tayf` exit → terminal usable (no broken termios)

### Benchmarks (perf §7 takibi)

`benches/throughput.rs`:

- `cat /usr/share/dict/words` baseline vs tayf ile fark — hedef <%20
- `journalctl -n 10000 --no-pager` — frame drop yok

---

## 6. Güvenlik Sınırları

CLAUDE.md §3 maddelerini v0.1 implementasyonuna bağlayan tablo:

| Tehdit | v0.1'de nasıl kapatılır |
|---|---|
| **ReDoS** | Built-in 7 pattern manuel olarak linear-time tasarlandı. Backtracking yok. `proptest` ile adversarial input testi v0.2'de kullanıcı pattern'leri eklendiğinde. |
| **Memory exhaustion** | `line_buffer` 64KB hard cap, aşılırsa partial flush + reset. `BufferOverflow` log atılır. |
| **Escape injection** | `Style::to_sgr` audit gate ile SGR dışında byte emit edilemez. Pattern eşleşmesinin **içine** zaten ANSI varsa, v0.1'de "ilk match kazanır" semantiği bunu naive korur ama tam koruma v0.3 (master §6.2). v0.1 limit. |
| **Terminal corruption** | `TtyGuard` Drop + `std::panic::set_hook` fallback. Exit yolları tek noktadan (`main.rs` → `ExitCode`) — `std::process::exit` kullanılmaz; dep'lerde `process::exit` çağrısı `cargo geiger` ile audit. |
| **SIGKILL / abort** | In-process saving yok. README'de "`tayf` aniden öldürülürse `reset` çalıştır" uyarısı. |
| **Credential/PII leak** | tracing default off; `TAYF_LOG` set edilmeden hiçbir log yazılmaz. Asla stdout'a debug yazılmaz. |
| **Config file traversal** | v0.1'de config dosyası yok. v0.2'de `canonicalize` + `~/.config/tayf/` whitelist. |
| **Shell injection** | `portable-pty` `CommandBuilder` `argv`-style. `sh -c` yok. `--shell` flag value'su `argv[0]` olarak gider, string interpolation yok. |
| **Signal forwarding** | SIGINT/SIGTERM child process group'a (`killpg(pgid, sig)`) forward. PID'e değil. |
| **Dependency surface** | v0.1 direct deps: `portable-pty`, `regex`, `nix`, `clap`, `thiserror`, `signal-hook`, `tracing`, `tracing-subscriber`. Her birinin son commit tarihi, license, `unsafe` line sayısı README'ye not edilir. `cargo audit` lokal'de çalıştırılır. |

---

## 7. Açık Kalan (v0.2'ye Devredilen) Sorular

Master §12'den v0.1 sırasında ele alınmayanlar:

1. Q1 — Crate ismi `tayf` rezervasyonu: kullanıcı manuel kontrol edecek (`cargo search tayf`).
2. Q2 — Default config storage hibrit (embed + override): v0.2'de TOML config gelince.
3. Q3 — Config validation friendly errors: v0.2.
4. Q7 — Perf regresyon eşiği (%10 mu %20 mı): v0.4 benchmark'lar yerleşince netleştirilir; v0.1 hedefi sadece <%20.
5. Q8 — `tayf --bypass` escape hatch: v0.2.
6. Q9 — Powerlevel10k instant prompt yarışı: v0.3 VTE/OSC 133 implementasyonu sırasında ölçülür.
7. Q10 — Tayf kendi UI mesajlarında OSC 133: v0.3'te shell prompt detection'ı ile birlikte.

---

## 8. v0.1 Bittiğinde "Done" Tanımı

- [ ] `cargo build --release` Linux ve macOS'ta uyarısız geçer.
- [ ] `cargo fmt --check && cargo clippy -- -D warnings` temiz.
- [ ] Tüm unit test'ler yeşil; integration smoke test'ler yeşil.
- [ ] Manuel doğrulama listesi (§5) tek tek geçti.
- [ ] `tayf-tasarim.md` §6.1, 6.5, 6.6, 6.10, 6.11, 6.16 edge case'leri kontrol edildi (kapanan/kapanmayan/v0.2 olarak işaretlendi).
- [ ] `README.md`'de kurulum + temel kullanım + bilinen v0.1 sınırları (alt-screen passthrough, capture group yok, vs.).
- [ ] `CHANGELOG.md`'de `[0.1.0]` entry.
- [ ] CLAUDE.md §3 güvenlik kontrolleri PR review'da geçildi (`security-review` skill).

---

_Bu doc, brainstorming aşamasının çıktısıdır. Implementation plan ayrı bir doc olarak `superpowers:writing-plans` skill ile üretilecektir._
