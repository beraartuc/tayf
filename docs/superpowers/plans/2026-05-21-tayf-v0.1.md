# tayf v0.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.1 "Çalışan İskelet" — PTY ile shell spawn eden, çıktıyı 7 hardcoded pattern'e göre renklendiren, alt-screen modlarda passthrough'a geçen, signal/exit-code/raw-mode garantileriyle tek binary Rust uygulaması.

**Architecture:** Tek crate (`lib + bin`), iki blocking thread + bir signal thread + bir main thread. Output pipeline = alt-screen state machine → line buffer → rules engine. RAII guard'lar termios restore'unu garantiler. Tüm yapısal kararlar spec'te: `docs/superpowers/specs/2026-05-21-tayf-v0.1-design.md`. Tüm kurallar: `CLAUDE.md`.

**Tech Stack:** Rust 1.74+, `portable-pty` 0.8, `regex` 1.10+, `nix` 0.27, `signal-hook` 0.3, `clap` 4 (derive), `thiserror` 1, `tracing` 0.1, `tracing-subscriber` 0.3, `built` 0.7. Test/dev: `tempfile` 3, `criterion` 0.5.

**Toplam görev sayısı:** 20.

---

## Pre-Implementation Context — Konuşulan Her Şeyin İz Sürülebilir Hali

Bu plan boşlukta yazılmadı. Aşağıdaki artefaktlar implementation sırasında her zaman okunabilir referanslardır:

### Master Tasarım
- **`tayf-tasarim.md`** (proje kökünde) — 700 satır, 13 bölüm, 16 edge case. Tüm yüksek seviye kararlar (PTY wrapper seçimi, teknoloji yığını, edge case katalogu, shell framework uyumluluk matrisi, yol haritası v0.1→v1.0). Plan task'larında `master §X.Y` referansları bu doc'a işaret eder.

### v0.1'e Özgü Spec
- **`docs/superpowers/specs/2026-05-21-tayf-v0.1-design.md`** — Brainstorming çıktısı. v0.1 scope (IN/OUT), modül sınırları, public API surface (`Tayf::run`), `PtySession::into_parts` dekompozisyonu, 10-adımlı shutdown protokolü (§3.4), 5-state alt-screen makinesi (§3.5), `Compiled { set, individuals, styles }` shape (§3.6), `Style::to_sgr` audit gate (§3.7), error modeli (§4), test stratejisi (§5), güvenlik tehdit tablosu (§6), v0.1 "Done" kontrol listesi (§8).

### Brainstorming Kararları (spec §1'de tablo halinde)
Aşağıdaki 10 karar konuşma sırasında alındı ve kilitlendi:

| # | Karar | Plan task |
|---|---|---|
| 1 | Master doc + v0.1 ek spec | (bu doc) |
| 2 | Linux + Unix + macOS day 1 | T1 (Cargo.toml) |
| 3 | Shell discovery: `$SHELL` → `/etc/passwd` → `/bin/sh`, non-login default, `--shell`/`--login` flag'leri | T7, T11 |
| 4 | 7 hardcoded pattern: IPv4, IPv6, MAC, log level, HTTP status, FQDN, duration | T4 |
| 5 | Dual MIT + Apache-2.0 lisans | T1 |
| 6 | `nix::sys::termios` (crossterm değil) | T12 |
| 7 | Tek crate, lib + bin (workspace değil) | T1 |
| 8 | CI olgunlaştıktan sonra (post-v0.1) | — |
| 9 | TDD-for-pure-logic + smoke-for-PTY | T2–T9, T18–T19 |
| 10 | v0.1'e minimal alt-screen detection eklendi (`\x1b[?1049h`/`l`); tam VTE v0.3'te | T6 |

### Senior Rust Engineer Architecture Review
- **`docs/superpowers/reviews/2026-05-21-rust-senior-architecture-review.md`** — Bağımsız subagent tarafından yapılan sert eleştiri ve verilen kararlar. 3 yanlış modül kesimi (alt_screen/colorize/Session), shutdown protokolünün eksikliği, Drop/panic safety, public API facade kararı, error handling enum stratejisi, 5 mimari güvenlik bulgusu, 7 naming düzeltmesi, 8 eksik modül.

Plan'daki her büyük tasarım kararının kaynağı yukarıdaki review'da. Implementation sırasında "bu neden böyle?" sorusu için ilk durak burası.

### Proje Kuralları (Non-Negotiable)
- **`CLAUDE.md`** — 4 kural:
  1. **English in code, Turkish in conversation.** Tüm identifier, doc-comment, commit, error message, log string İngilizce. Spec ve plan Türkçe.
  2. **Architecture and code standards first.** Rust naming (snake_case/UpperCamelCase/SCREAMING), `cargo fmt` + `clippy::pedantic -D warnings` zorunlu, lib kodunda `unwrap`/`expect` yasak (test ve `unreachable!` istisnası), public item'lar doc-comment'li, file-per-concept (~400 satır üstü split).
  3. **Security is first-class.** Adversarial input (ReDoS, memory exhaustion, escape injection, terminal state corruption), credential leak engelleme, file system traversal, process spawning safety, signal forwarding to PGID, dependency surface audit, release signing.
  4. **Zero technical debt tolerance.** TODO'lar issue-tracked, no dead code, no commented-out code, public API stability, test+doc with feature, CHANGELOG maintained, error messages = UX, deprecation policy.

Her görevin son adımındaki `cargo fmt && clippy -D warnings && test` komutu bu kuralları yerel olarak doğrular.

### Açık Kalan Sorular (v0.2'ye Devredildi — Spec §7)
1. `tayf` crate ismi crates.io rezervasyonu (kullanıcı manuel kontrol)
2. TOML config storage (v0.2'de gelecek)
3. Config validation friendly errors (v0.2)
4. Perf regresyon eşiği %10 vs %20 — v0.4 benchmark'ları yerleşince
5. `tayf --bypass` escape hatch (v0.2)
6. Powerlevel10k instant prompt yarış ölçümü (v0.3)
7. Tayf'ın kendi UI mesajlarında OSC 133 (v0.3)

---

## Genel Çalışma Disiplini (Her görev için)

Aşağıdaki kurallar her görev içinde varsayılır; tekrar yazılmaz:

- Her dosyanın en üstünde modül-seviye `//!` doc-comment olmalı (amaç, public API, invariantlar).
- Public her item için `///` doc-comment + (gerekiyorsa) `# Examples`.
- Kod yazılmadan önce test yazılır (saf logic modüllerinde). PTY/signal/threading modüllerinde test impl ile birlikte yazılır.
- Her görev sonunda: `cargo fmt && cargo clippy --all-targets -- -D warnings && cargo test` temiz olmadan commit yok.
- Commit mesajı: imperative present, <70 char title, body "neden"i anlatır.
- Tüm code/comments/identifier/error message/log ENGLISH (CLAUDE.md §1).
- `unwrap()`/`expect()` lib kodunda yasak — sadece test'lerde, `main.rs` top-level setup'ta, ve `unreachable!("reason")` ile kanıtlanmış yollarda.
- Yeni dependency eklerken commit mesajında "neden" + son commit tarihi + license + unsafe-line sayısı notu.

Pre-commit kontrol komutu (her commit öncesi koş):

```bash
cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test
```

---

## Task 1: Project Scaffold

**Files:**
- Create: `/Users/bera/tayf/Cargo.toml`
- Create: `/Users/bera/tayf/.gitignore`
- Create: `/Users/bera/tayf/LICENSE-MIT`
- Create: `/Users/bera/tayf/LICENSE-APACHE`
- Create: `/Users/bera/tayf/README.md` (skeleton)
- Create: `/Users/bera/tayf/CHANGELOG.md`
- Create: `/Users/bera/tayf/src/main.rs` (stub)
- Create: `/Users/bera/tayf/src/lib.rs` (stub)
- Create: `/Users/bera/tayf/rustfmt.toml`
- Create: `/Users/bera/tayf/clippy.toml`

- [ ] **Step 1: `.gitignore` oluştur**

```
/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
*.profraw
```

`Cargo.lock` commit edilir (binary için).

- [ ] **Step 2: `Cargo.toml` yaz**

```toml
[package]
name = "tayf"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
description = "Terminal-agnostic, PTY-based, regex-driven output colorizer."
license = "MIT OR Apache-2.0"
repository = "https://github.com/beraartuc/tayf"
homepage = "https://github.com/beraartuc/tayf"
authors = ["Bera Artuc <bera@korp.com.tr>"]
keywords = ["terminal", "tty", "pty", "colorize", "regex"]
categories = ["command-line-utilities"]
readme = "README.md"
include = [
    "src/**/*",
    "build.rs",
    "Cargo.toml",
    "LICENSE-MIT",
    "LICENSE-APACHE",
    "README.md",
    "CHANGELOG.md",
]

[[bin]]
name = "tayf"
path = "src/main.rs"

[lib]
name = "tayf"
path = "src/lib.rs"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
nix = { version = "0.27", features = ["term", "signal", "user", "fs", "poll"] }
portable-pty = "0.8"
regex = "1.10"
signal-hook = "0.3"
thiserror = "1.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[build-dependencies]
built = { version = "0.7", features = ["git2"] }

[dev-dependencies]
tempfile = "3.10"

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
opt-level = 3
panic = "abort"
```

Not: `panic = "abort"` release'te binary küçültür. Drop guard'lar yine de çalışır çünkü termios restore'u Drop değil panic-hook ile yapılır (Task 12'de).

- [ ] **Step 3: Lisans dosyalarını koy**

`LICENSE-MIT` ve `LICENSE-APACHE` standart SPDX metinleri. İçerikler için:

```bash
curl -fsSL https://raw.githubusercontent.com/dtolnay/cargo-license/master/LICENSE-MIT -o LICENSE-MIT
curl -fsSL https://raw.githubusercontent.com/dtolnay/cargo-license/master/LICENSE-APACHE -o LICENSE-APACHE
```

İndirilen dosyalarda `Copyright (c) ...` satırını `Copyright (c) 2026 Bera Artuc <bera@korp.com.tr>` ile değiştir.

- [ ] **Step 4: `rustfmt.toml`**

```toml
edition = "2021"
max_width = 100
use_small_heuristics = "Max"
imports_granularity = "Crate"
group_imports = "StdExternalCrate"
newline_style = "Unix"
```

- [ ] **Step 5: `clippy.toml`**

```toml
avoid-breaking-exported-api = true
disallowed-methods = [
    { path = "std::process::exit", reason = "Use ExitCode return from main to allow Drop." },
]
```

- [ ] **Step 6: `src/lib.rs` skeleton**

```rust
//! tayf — terminal-agnostic, PTY-based, regex-driven output colorizer.
//!
//! Public entry: [`Tayf::run`]. See `docs/superpowers/specs/2026-05-21-tayf-v0.1-design.md`
//! for full design.

#![deny(unsafe_code)]
#![warn(clippy::pedantic, clippy::cargo)]
#![allow(clippy::module_name_repetitions)]

// Modules are added in subsequent tasks.

/// Placeholder facade. Implemented in Task 17.
pub struct Tayf;

impl Tayf {
    /// Placeholder; real implementation in Task 17.
    #[must_use]
    pub fn placeholder() -> &'static str {
        "tayf"
    }
}
```

- [ ] **Step 7: `src/main.rs` skeleton**

```rust
//! tayf CLI entry. Real arg parsing and exit-code mapping arrive in Task 18.

fn main() {
    println!("tayf placeholder");
}
```

- [ ] **Step 8: `README.md` skeleton (İngilizce)**

```markdown
# tayf

Terminal-agnostic, PTY-based, regex-driven output colorizer.

> **Status:** v0.1 in development. Not yet ready for general use.

## Overview

`tayf` wraps your shell in a pseudo-terminal and applies regex rules to the
output stream, so common patterns (IP addresses, log levels, HTTP status codes,
duration metrics, etc.) appear colorized automatically — in any terminal
emulator, with any shell. Think iTerm2 Triggers as a standalone binary.

See [`tayf-tasarim.md`](./tayf-tasarim.md) for the full design (Turkish).

## License

Dual-licensed under either of:

- [Apache License, Version 2.0](./LICENSE-APACHE)
- [MIT license](./LICENSE-MIT)

at your option.
```

- [ ] **Step 9: `CHANGELOG.md` skeleton**

```markdown
# Changelog

All notable changes to this project are documented here. The format is based on
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this project
adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial repository scaffolding (Cargo manifest, license files, README).
```

- [ ] **Step 10: Build doğrulama**

```bash
cd /Users/bera/tayf
cargo build
cargo clippy --all-targets -- -D warnings
cargo fmt --check
```

Expected: Hepsi temiz geçer; `target/debug/tayf` mevcut.

- [ ] **Step 11: Commit**

```bash
git add Cargo.toml Cargo.lock .gitignore LICENSE-MIT LICENSE-APACHE \
       README.md CHANGELOG.md rustfmt.toml clippy.toml src/
git commit -m "chore: scaffold Rust project (Cargo manifest, licenses, lint config)

Adds the v0.1 baseline: Cargo manifest with pinned dependencies, dual
MIT/Apache-2.0 license files, README and CHANGELOG skeletons, rustfmt
and clippy configuration, and placeholder lib + bin entries.

See docs/superpowers/specs/2026-05-21-tayf-v0.1-design.md §3.1."
```

---

## Task 2: Error Module (`error.rs`)

Tek `tayf::Error` enum'unun tanımı. Sonraki her modül buna bağlanır.

**Files:**
- Create: `src/error.rs`
- Modify: `src/lib.rs` (modül declaration ekle)

- [ ] **Step 1: `src/error.rs` yaz**

```rust
//! Single top-level error type for the tayf crate.
//!
//! All public functions surface errors through this enum so callers (including
//! `main`) can map them to user-facing messages and exit codes in one place.
//! See spec §4.

use std::io;

/// All recoverable errors produced by tayf.
#[derive(Debug, thiserror::Error)]
pub enum Error {
    /// Could not determine which shell to launch.
    #[error("could not determine shell: {0}")]
    ShellDiscovery(String),

    /// PTY operation failed (open, spawn, read, write, resize).
    #[error("PTY operation failed: {0}")]
    Pty(#[from] io::Error),

    /// Terminal control failed (termios get/set, ioctl).
    #[error("terminal control failed: {0}")]
    Tty(#[from] nix::errno::Errno),

    /// Built-in or user regex failed to compile.
    #[error("regex compilation failed: {0}")]
    RegexCompile(#[from] regex::Error),

    /// Signal handler installation failed.
    #[error("signal installation failed: {0}")]
    Signal(io::Error),

    /// A line exceeded the buffer cap; flushed as-is without rule application.
    /// Non-fatal — logged via tracing, not returned to caller.
    #[error("line buffer exceeded {cap} bytes; flushing as-is")]
    BufferOverflow {
        /// The cap (in bytes) that was exceeded.
        cap: usize,
    },
}

/// Convenience alias.
pub type Result<T> = std::result::Result<T, Error>;
```

- [ ] **Step 2: `src/lib.rs`'e modül declaration ekle**

`src/lib.rs` içinde `// Modules are added in subsequent tasks.` yorumunu sil ve şunu ekle:

```rust
pub mod error;

pub use error::{Error, Result};
```

- [ ] **Step 3: Build + lint**

```bash
cargo build
cargo clippy --all-targets -- -D warnings
```

Expected: Temiz.

- [ ] **Step 4: Commit**

```bash
git add src/error.rs src/lib.rs
git commit -m "feat(error): define unified tayf::Error enum

Adds a single top-level error type derived via thiserror so every
fallible operation in the crate (PTY, terminal control, regex compile,
signal install, buffer overflow) flows through one place. The Display
impl produces user-facing messages; main maps variants to exit codes.

See spec §4."
```

---

## Task 3: Style Module (`style.rs`) + SGR Audit Gate

`Style` struct'ı ve onun `to_sgr()` çıktısını SGR-only olduğunu garantileyen audit testi.

**Files:**
- Create: `src/style.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Failing test yaz**

`src/style.rs` oluştur, ŞİMDİLİK sadece test bloğu ile:

```rust
//! Visual styling primitive and its safe rendering to ANSI SGR.
//!
//! `Style` describes a foreground/background/attribute combination.
//! `Style::to_sgr` is the *only* function in the crate that emits ANSI escape
//! bytes that surround pattern matches; it is restricted to SGR sequences
//! (`\x1b[…m` and `\x1b[0m`) and audited by a unit test. See spec §3.7.

#[cfg(test)]
mod tests {
    use super::*;

    fn sgr_pattern() -> regex::Regex {
        // Permits CSI parameter list (digits and semicolons) followed by 'm',
        // and the reset sequence \x1b[0m.
        regex::Regex::new(r"^(?:\x1b\[(?:\d+;)*\d+m|\x1b\[0m)$").unwrap()
    }

    #[test]
    fn to_sgr_emits_only_sgr_sequences() {
        let cases: Vec<Style> = vec![
            Style { fg: Some(Color::Red), bg: None, bold: false, italic: false, underline: false, dim: false },
            Style { fg: Some(Color::BrightYellow), bg: None, bold: true, italic: false, underline: false, dim: false },
            Style { fg: Some(Color::Indexed(178)), bg: Some(Color::Indexed(0)), bold: false, italic: false, underline: false, dim: false },
            Style { fg: Some(Color::Rgb(255, 136, 0)), bg: None, bold: false, italic: true, underline: true, dim: false },
            Style::default(),
        ];

        let re = sgr_pattern();
        for style in &cases {
            let sgr = style.to_sgr();
            assert!(re.is_match(&sgr), "style {style:?} emitted non-SGR: {sgr:?}");
        }
    }

    #[test]
    fn reset_emits_zero_m() {
        assert_eq!(Style::reset_sgr(), "\x1b[0m");
    }

    #[test]
    fn default_style_renders_empty_or_zero_m() {
        let s = Style::default().to_sgr();
        assert!(s == "\x1b[0m" || s.is_empty(), "default produced: {s:?}");
    }
}
```

- [ ] **Step 2: Test'in fail ettiğini doğrula**

```bash
cargo test --lib style
```

Expected: Compile error — `Style`, `Color` tanımlı değil.

- [ ] **Step 3: Minimal `Style` ve `Color` impl**

`src/style.rs` üstüne (test bloğunun üstüne) ekle:

```rust
/// 16-color ANSI base palette plus 256-indexed and 24-bit RGB.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum Color {
    Black,
    Red,
    Green,
    Yellow,
    Blue,
    Magenta,
    Cyan,
    White,
    BrightBlack,
    BrightRed,
    BrightGreen,
    BrightYellow,
    BrightBlue,
    BrightMagenta,
    BrightCyan,
    BrightWhite,
    /// 256-color palette index.
    Indexed(u8),
    /// 24-bit truecolor (r, g, b).
    Rgb(u8, u8, u8),
}

impl Color {
    /// Foreground SGR parameter list (e.g. `"31"`, `"38;5;178"`, `"38;2;255;136;0"`).
    fn fg_params(self) -> String {
        match self {
            Color::Black => "30".into(),
            Color::Red => "31".into(),
            Color::Green => "32".into(),
            Color::Yellow => "33".into(),
            Color::Blue => "34".into(),
            Color::Magenta => "35".into(),
            Color::Cyan => "36".into(),
            Color::White => "37".into(),
            Color::BrightBlack => "90".into(),
            Color::BrightRed => "91".into(),
            Color::BrightGreen => "92".into(),
            Color::BrightYellow => "93".into(),
            Color::BrightBlue => "94".into(),
            Color::BrightMagenta => "95".into(),
            Color::BrightCyan => "96".into(),
            Color::BrightWhite => "97".into(),
            Color::Indexed(n) => format!("38;5;{n}"),
            Color::Rgb(r, g, b) => format!("38;2;{r};{g};{b}"),
        }
    }

    /// Background SGR parameter list.
    fn bg_params(self) -> String {
        match self {
            Color::Black => "40".into(),
            Color::Red => "41".into(),
            Color::Green => "42".into(),
            Color::Yellow => "43".into(),
            Color::Blue => "44".into(),
            Color::Magenta => "45".into(),
            Color::Cyan => "46".into(),
            Color::White => "47".into(),
            Color::BrightBlack => "100".into(),
            Color::BrightRed => "101".into(),
            Color::BrightGreen => "102".into(),
            Color::BrightYellow => "103".into(),
            Color::BrightBlue => "104".into(),
            Color::BrightMagenta => "105".into(),
            Color::BrightCyan => "106".into(),
            Color::BrightWhite => "107".into(),
            Color::Indexed(n) => format!("48;5;{n}"),
            Color::Rgb(r, g, b) => format!("48;2;{r};{g};{b}"),
        }
    }
}

/// Visual styling for a pattern match.
#[derive(Debug, Clone, Copy, Default, PartialEq, Eq)]
pub struct Style {
    pub fg: Option<Color>,
    pub bg: Option<Color>,
    pub bold: bool,
    pub italic: bool,
    pub underline: bool,
    pub dim: bool,
}

impl Style {
    /// Render this style as an opening SGR escape sequence.
    ///
    /// Returns an empty string if the style would have no visible effect.
    /// Guaranteed to emit only `\x1b[…m` or `\x1b[0m` — see `tests::to_sgr_emits_only_sgr_sequences`.
    #[must_use]
    pub fn to_sgr(&self) -> String {
        let mut params: Vec<String> = Vec::new();

        if self.bold {
            params.push("1".into());
        }
        if self.dim {
            params.push("2".into());
        }
        if self.italic {
            params.push("3".into());
        }
        if self.underline {
            params.push("4".into());
        }
        if let Some(fg) = self.fg {
            params.push(fg.fg_params());
        }
        if let Some(bg) = self.bg {
            params.push(bg.bg_params());
        }

        if params.is_empty() {
            String::new()
        } else {
            format!("\x1b[{}m", params.join(";"))
        }
    }

    /// The single canonical SGR reset.
    #[must_use]
    pub fn reset_sgr() -> &'static str {
        "\x1b[0m"
    }
}
```

- [ ] **Step 4: `src/lib.rs`'e modül ekle**

```rust
pub mod error;
pub mod style;

pub use error::{Error, Result};
```

- [ ] **Step 5: Test'in geçtiğini doğrula**

```bash
cargo test --lib style
cargo clippy --all-targets -- -D warnings
```

Expected: 3 test pass.

- [ ] **Step 6: Commit**

```bash
git add src/style.rs src/lib.rs
git commit -m "feat(style): add Style type with SGR-only audit gate

Style describes fg/bg/attribute combinations. to_sgr is the single
choke point through which we emit ANSI sequences around matches;
a unit test asserts every output matches the SGR grammar, closing
off the escape-injection attack surface described in CLAUDE.md §3
and spec §3.7."
```

---

## Task 4: Rules Module (`rules.rs`) — Compiled Patterns

`Compiled` struct'ı ve 7 hardcoded builtin pattern. Her pattern için match/no-match testi.

**Files:**
- Create: `src/rules.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Failing test yaz**

`src/rules.rs` oluştur, test bloğu ile başla:

```rust
//! Compiled rule set and built-in patterns.
//!
//! `Compiled` holds a parallel collection of regexes and styles. v0.1 walks
//! `individuals` for every line; the `set` field is populated for the v0.4
//! RegexSet fast-path so that switching does not break the public shape.
//! See spec §3.6.

#[cfg(test)]
mod tests {
    use super::*;

    fn find_rule(name: &str) -> &'static str {
        BUILTIN_RULES.iter().find(|r| r.name == name).expect("rule must exist").pattern
    }

    fn matches(pattern_name: &str, input: &str) -> bool {
        let re = regex::bytes::Regex::new(find_rule(pattern_name)).unwrap();
        re.is_match(input.as_bytes())
    }

    #[test]
    fn ipv4_matches() {
        assert!(matches("ipv4", "connect 192.168.1.1"));
        assert!(matches("ipv4", "0.0.0.0"));
        assert!(matches("ipv4", "255.255.255.255"));
        assert!(!matches("ipv4", "999.999.999.999"));
        assert!(!matches("ipv4", "1.2.3"));
    }

    #[test]
    fn ipv6_matches() {
        assert!(matches("ipv6", "fe80::1"));
        assert!(matches("ipv6", "2001:0db8:85a3:0000:0000:8a2e:0370:7334"));
        assert!(matches("ipv6", "::1"));
        assert!(!matches("ipv6", "12345"));
    }

    #[test]
    fn mac_matches() {
        assert!(matches("mac", "aa:bb:cc:dd:ee:ff"));
        assert!(matches("mac", "AA-BB-CC-DD-EE-FF"));
        assert!(matches("mac", "00:11:22:33:44:55"));
        assert!(!matches("mac", "zz:bb:cc:dd:ee:ff"));
        assert!(!matches("mac", "aa:bb:cc"));
    }

    #[test]
    fn log_level_matches() {
        assert!(matches("log_level", "[ERROR] failed to connect"));
        assert!(matches("log_level", "WARN something"));
        assert!(matches("log_level", "INFO startup"));
        assert!(matches("log_level", "FATAL crash"));
        assert!(!matches("log_level", "errorlike"));
    }

    #[test]
    fn http_status_matches() {
        assert!(matches("http_status", "HTTP/1.1 404 Not Found"));
        assert!(matches("http_status", "status: 500"));
        assert!(matches("http_status", "200 OK"));
        assert!(!matches("http_status", "abc500def"));
    }

    #[test]
    fn fqdn_matches() {
        assert!(matches("fqdn", "https://example.com/"));
        assert!(matches("fqdn", "api.staging.internal.corp.net"));
        assert!(matches("fqdn", "host.local"));
        // Single labels are not FQDNs:
        assert!(!matches("fqdn", "localhost"));
    }

    #[test]
    fn duration_matches() {
        assert!(matches("duration", "took 20.291 ms"));
        assert!(matches("duration", "elapsed 1.5s"));
        assert!(matches("duration", "150ms"));
        assert!(matches("duration", "2.5 us"));
        assert!(matches("duration", "100 μs"));
        assert!(!matches("duration", "milliseconds"));
    }

    #[test]
    fn compiled_load_succeeds() {
        let c = Compiled::load_builtins().expect("builtin compile");
        assert_eq!(c.individuals.len(), BUILTIN_RULES.len());
        assert_eq!(c.styles.len(), BUILTIN_RULES.len());
        assert_eq!(c.set.len(), BUILTIN_RULES.len());
    }
}
```

- [ ] **Step 2: Test'in fail ettiğini doğrula**

```bash
cargo test --lib rules
```

Expected: Compile error — `BUILTIN_RULES`, `Compiled` tanımlı değil.

- [ ] **Step 3: `rules.rs` impl**

Test bloğunun üstüne ekle:

```rust
use regex::bytes::{Regex, RegexSet};

use crate::error::{Error, Result};
use crate::style::{Color, Style};

/// One built-in rule: a name (for diagnostics), a regex pattern source, and the
/// style applied to each match.
pub(crate) struct BuiltinRule {
    pub(crate) name: &'static str,
    pub(crate) pattern: &'static str,
    pub(crate) style: Style,
}

/// The seven built-in rules shipped with v0.1.
///
/// Each pattern is hand-tuned to be linear-time (no catastrophic backtracking)
/// per CLAUDE.md §3. New patterns must be reviewed for ReDoS.
pub(crate) static BUILTIN_RULES: &[BuiltinRule] = &[
    BuiltinRule {
        name: "ipv4",
        // 0-255.0-255.0-255.0-255 with word boundaries.
        pattern: r"\b(?:25[0-5]|2[0-4]\d|1\d{2}|[1-9]?\d)(?:\.(?:25[0-5]|2[0-4]\d|1\d{2}|[1-9]?\d)){3}\b",
        style: Style { fg: Some(Color::Yellow), bold: true, ..Style::DEFAULT },
    },
    BuiltinRule {
        name: "ipv6",
        // Minimal IPv6: full eight groups OR any string containing `::`.
        // Not fully RFC-correct but linear-time and covers the practical cases.
        pattern: r"(?:[0-9A-Fa-f]{1,4}:){7}[0-9A-Fa-f]{1,4}|(?:[0-9A-Fa-f]{1,4}:){1,6}:[0-9A-Fa-f]{0,4}|::[0-9A-Fa-f]{1,4}|::1",
        style: Style { fg: Some(Color::BrightYellow), ..Style::DEFAULT },
    },
    BuiltinRule {
        name: "mac",
        pattern: r"\b[0-9A-Fa-f]{2}(?:[:-][0-9A-Fa-f]{2}){5}\b",
        style: Style { fg: Some(Color::Cyan), ..Style::DEFAULT },
    },
    BuiltinRule {
        name: "log_level",
        pattern: r"\b(?:ERROR|FAIL|FATAL|CRITICAL|WARN|WARNING|INFO|DEBUG|TRACE)\b",
        style: Style { fg: Some(Color::BrightRed), bold: true, ..Style::DEFAULT },
    },
    BuiltinRule {
        name: "http_status",
        // Three-digit number after a space, slash, colon, or start-of-line.
        // Word boundaries alone are not enough because "abc500" should not match.
        pattern: r"(?:^|[\s/:])([1-5]\d{2})\b",
        style: Style { fg: Some(Color::Magenta), ..Style::DEFAULT },
    },
    BuiltinRule {
        name: "fqdn",
        // At least two dot-separated labels. ASCII-only for v0.1 (IDN later).
        pattern: r"\b(?:[A-Za-z0-9](?:[A-Za-z0-9-]{0,61}[A-Za-z0-9])?\.){1,}[A-Za-z]{2,24}\b",
        style: Style { fg: Some(Color::Blue), ..Style::DEFAULT },
    },
    BuiltinRule {
        name: "duration",
        // Decimal or integer followed by an optional space and a duration unit.
        pattern: r"\b\d+(?:\.\d+)?\s?(?:ns|us|μs|ms|s|m|h)\b",
        style: Style { fg: Some(Color::Green), ..Style::DEFAULT },
    },
];

/// Compiled rule set ready for application against output lines.
///
/// `individuals` and `styles` are parallel — index `i` of `individuals` carries
/// the regex; index `i` of `styles` carries the style to apply. `set` is the
/// equivalent `RegexSet` populated for v0.4's planned fast-path; v0.1 ignores
/// it but the storage shape stays stable.
pub(crate) struct Compiled {
    #[allow(dead_code)] // reserved for v0.4 RegexSet fast-path
    pub(crate) set: RegexSet,
    pub(crate) individuals: Vec<Regex>,
    pub(crate) styles: Vec<Style>,
}

impl Compiled {
    /// Compile the seven built-in rules.
    ///
    /// # Errors
    /// Returns `Error::RegexCompile` if any built-in pattern fails to compile.
    /// In practice this never happens — the patterns are tested.
    pub(crate) fn load_builtins() -> Result<Self> {
        let mut individuals = Vec::with_capacity(BUILTIN_RULES.len());
        let mut styles = Vec::with_capacity(BUILTIN_RULES.len());
        let mut sources = Vec::with_capacity(BUILTIN_RULES.len());

        for rule in BUILTIN_RULES {
            individuals.push(Regex::new(rule.pattern).map_err(Error::from)?);
            styles.push(rule.style);
            sources.push(rule.pattern);
        }

        let set = RegexSet::new(&sources).map_err(Error::from)?;

        Ok(Compiled { set, individuals, styles })
    }
}
```

`Style::DEFAULT` const'ı eksik — `style.rs`'e ekle:

```rust
impl Style {
    pub const DEFAULT: Style = Style {
        fg: None, bg: None, bold: false, italic: false, underline: false, dim: false,
    };
    // ... existing impl ...
}
```

- [ ] **Step 4: `src/lib.rs`'e modül ekle**

```rust
pub mod error;
pub mod style;
pub(crate) mod rules;

pub use error::{Error, Result};
```

- [ ] **Step 5: Test'lerin geçtiğini doğrula**

```bash
cargo test --lib rules
cargo clippy --all-targets -- -D warnings
```

Expected: 8 test pass (7 pattern + 1 compile).

- [ ] **Step 6: Commit**

```bash
git add src/rules.rs src/style.rs src/lib.rs
git commit -m "feat(rules): compile seven built-in patterns

Adds the v0.1 hardcoded ruleset (IPv4, IPv6, MAC, log level,
HTTP status, FQDN, duration) and the Compiled struct that holds
them. Each pattern is hand-tuned to be linear-time per CLAUDE.md §3.

The RegexSet field is populated but unused; v0.4 will switch the
hot path to it without breaking the public shape (spec §3.6)."
```

---

## Task 5: Line Buffer (`line_buffer.rs`)

UTF-8-safe accumulator, `\n` veya 50ms timeout ile flush, 64KB hard cap.

**Files:**
- Create: `src/line_buffer.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Failing test'leri yaz**

`src/line_buffer.rs`:

```rust
//! UTF-8-safe accumulator with timeout flush and a hard cap.
//!
//! Lines are emitted on `\n`, on a 50ms idle timeout, or when the buffer
//! reaches `MAX_BUFFER_BYTES` — in the last case the partial line is flushed
//! *without* rule application (spec §6, "Memory exhaustion").

use std::time::{Duration, Instant};

use crate::error::Error;

/// Hard cap on a single accumulated line. Above this, we flush and reset.
pub(crate) const MAX_BUFFER_BYTES: usize = 64 * 1024;

/// Idle timeout. If no newline arrives within this window, flush the partial
/// buffer (interactive prompts have no trailing `\n`).
pub(crate) const FLUSH_TIMEOUT: Duration = Duration::from_millis(50);

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn empty_feed_yields_nothing() {
        let mut buf = LineBuffer::new();
        let lines = buf.feed(b"");
        assert!(lines.is_empty());
    }

    #[test]
    fn single_line_with_newline_is_emitted() {
        let mut buf = LineBuffer::new();
        let lines = buf.feed(b"hello world\n");
        assert_eq!(lines, vec![b"hello world\n".to_vec()]);
    }

    #[test]
    fn two_lines_in_one_chunk_are_split() {
        let mut buf = LineBuffer::new();
        let lines = buf.feed(b"a\nb\n");
        assert_eq!(lines, vec![b"a\n".to_vec(), b"b\n".to_vec()]);
    }

    #[test]
    fn partial_line_is_held() {
        let mut buf = LineBuffer::new();
        let lines = buf.feed(b"part");
        assert!(lines.is_empty());
        let lines = buf.feed(b"ial\n");
        assert_eq!(lines, vec![b"partial\n".to_vec()]);
    }

    #[test]
    fn utf8_multibyte_split_across_chunks_is_safe() {
        let mut buf = LineBuffer::new();
        // "üç" = 0xC3 0xBC 0xC3 0xA7 ; split between bytes 1 and 2.
        let _ = buf.feed(&[0xC3]);
        let lines = buf.feed(&[0xBC, 0xC3, 0xA7, b'\n']);
        assert_eq!(lines, vec![vec![0xC3, 0xBC, 0xC3, 0xA7, b'\n']]);
    }

    #[test]
    fn timeout_flush_releases_partial() {
        let mut buf = LineBuffer::new();
        let _ = buf.feed(b"prompt> ");
        // Simulate clock advance by calling tick with a stale instant.
        let stale = Instant::now() - Duration::from_millis(100);
        let flushed = buf.flush_if_stale(stale);
        assert_eq!(flushed, Some(b"prompt> ".to_vec()));
    }

    #[test]
    fn overflow_flushes_as_is_and_returns_warning() {
        let mut buf = LineBuffer::new();
        let huge = vec![b'x'; MAX_BUFFER_BYTES + 100];
        let (lines, overflow) = buf.feed_with_overflow(&huge);
        assert!(matches!(overflow, Some(Error::BufferOverflow { .. })));
        assert_eq!(lines.len(), 1);
        assert_eq!(lines[0].len(), MAX_BUFFER_BYTES + 100);
    }

    #[test]
    fn drain_partial_returns_remaining() {
        let mut buf = LineBuffer::new();
        let _ = buf.feed(b"hanging");
        let remaining = buf.drain();
        assert_eq!(remaining, b"hanging");
    }
}
```

- [ ] **Step 2: Test'lerin fail ettiğini doğrula**

```bash
cargo test --lib line_buffer
```

Expected: Compile error.

- [ ] **Step 3: Impl yaz**

Test bloğunun üstüne ekle:

```rust
/// Accumulator that emits complete lines.
pub(crate) struct LineBuffer {
    inner: Vec<u8>,
    last_write: Instant,
}

impl LineBuffer {
    pub(crate) fn new() -> Self {
        LineBuffer { inner: Vec::with_capacity(4096), last_write: Instant::now() }
    }

    /// Feed bytes; return any complete lines (each ending in `\n`).
    ///
    /// If feeding causes the buffer to exceed `MAX_BUFFER_BYTES`, the entire
    /// accumulated buffer (including the new chunk) is flushed as a single
    /// "line" without regex application. The caller is expected to log the
    /// overflow.
    pub(crate) fn feed(&mut self, chunk: &[u8]) -> Vec<Vec<u8>> {
        let (lines, _) = self.feed_with_overflow(chunk);
        lines
    }

    /// Variant of `feed` that also reports overflow events to the caller.
    pub(crate) fn feed_with_overflow(&mut self, chunk: &[u8]) -> (Vec<Vec<u8>>, Option<Error>) {
        self.last_write = Instant::now();
        self.inner.extend_from_slice(chunk);

        let mut lines = Vec::new();
        let mut overflow = None;

        if self.inner.len() > MAX_BUFFER_BYTES {
            // Flush everything as-is, no further line splitting.
            overflow = Some(Error::BufferOverflow { cap: MAX_BUFFER_BYTES });
            lines.push(std::mem::take(&mut self.inner));
            return (lines, overflow);
        }

        while let Some(pos) = memchr_newline(&self.inner) {
            let line: Vec<u8> = self.inner.drain(..=pos).collect();
            lines.push(line);
        }

        (lines, overflow)
    }

    /// If the buffer has been idle since `cutoff`, drain and return it.
    pub(crate) fn flush_if_stale(&mut self, cutoff: Instant) -> Option<Vec<u8>> {
        if self.inner.is_empty() {
            return None;
        }
        if self.last_write <= cutoff {
            Some(std::mem::take(&mut self.inner))
        } else {
            None
        }
    }

    /// Return any remaining bytes (used at shutdown).
    pub(crate) fn drain(&mut self) -> Vec<u8> {
        std::mem::take(&mut self.inner)
    }
}

fn memchr_newline(haystack: &[u8]) -> Option<usize> {
    haystack.iter().position(|&b| b == b'\n')
}
```

UTF-8 safety: `feed_with_overflow` byte-level çalışıyor; \n line-split byte sınırını UTF-8 sınırıyla çakışmaya zorlar (`\n` her zaman UTF-8'de tek byte ve hiçbir multi-byte sequence içinde geçmez). Bu kanıt yorum olarak eklenir:

```rust
// SAFETY/INVARIANT: `\n` (0x0A) is never a continuation byte in UTF-8
// (continuations are 0x80-0xBF), so splitting on '\n' is always at a valid
// codepoint boundary. Multi-byte sequences split across feed() calls remain
// inside `inner` until '\n' arrives.
```

- [ ] **Step 4: `lib.rs`'e modül ekle**

```rust
pub mod error;
pub mod style;
pub(crate) mod line_buffer;
pub(crate) mod rules;

pub use error::{Error, Result};
```

- [ ] **Step 5: Test'lerin geçtiğini doğrula**

```bash
cargo test --lib line_buffer
cargo clippy --all-targets -- -D warnings
```

Expected: 7 test pass.

- [ ] **Step 6: Commit**

```bash
git add src/line_buffer.rs src/lib.rs
git commit -m "feat(line_buffer): UTF-8 safe line accumulator with cap

LineBuffer emits complete lines on \\n, flushes partials on idle
timeout, and flushes-as-is when a single line exceeds 64KB to
prevent unbounded growth (spec §6, CLAUDE.md §3 memory exhaustion).

Splitting on \\n is always at a UTF-8 codepoint boundary because
the byte 0x0A never appears as a continuation byte."
```

---

## Task 6: Pipeline (`pipeline.rs`) — Alt-Screen SM + Rule Application

Pipeline = alt-screen state machine + line buffer adaptörü + rules application. Spec §3.5'teki state machine ve §3.6'daki apply_rules burada birleşir.

**Files:**
- Create: `src/pipeline.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Alt-screen SM testleri yaz**

`src/pipeline.rs`:

```rust
//! Output processing pipeline: alt-screen detection + line buffering +
//! rule application.
//!
//! See spec §3.5 (alt-screen state machine) and §3.6 (rules engine).
//!
//! The state machine never consumes bytes; it only flips an internal flag.
//! When the alt-screen flag is set, bytes go straight to the writer without
//! line buffering or rule application.

use std::io::Write;
use std::time::Instant;

use crate::error::Error;
use crate::line_buffer::{LineBuffer, FLUSH_TIMEOUT};
use crate::rules::Compiled;
use crate::style::Style;

#[cfg(test)]
mod alt_screen_tests {
    use super::*;

    fn feed_collect(sm: &mut AltScreenSm, input: &[u8]) -> Vec<bool> {
        input.iter().map(|&b| sm.step(b)).collect()
    }

    #[test]
    fn enters_on_complete_1049h_in_one_chunk() {
        let mut sm = AltScreenSm::new();
        for &b in b"\x1b[?1049h" {
            sm.step(b);
        }
        assert!(sm.in_alt_screen());
    }

    #[test]
    fn exits_on_complete_1049l() {
        let mut sm = AltScreenSm::new();
        for &b in b"\x1b[?1049h\x1b[?1049l" {
            sm.step(b);
        }
        assert!(!sm.in_alt_screen());
    }

    #[test]
    fn accepts_old_47_and_1047_variants() {
        for seq in [b"\x1b[?47h".as_slice(), b"\x1b[?1047h".as_slice()] {
            let mut sm = AltScreenSm::new();
            for &b in seq {
                sm.step(b);
            }
            assert!(sm.in_alt_screen(), "expected alt-screen after {seq:?}");
        }
    }

    #[test]
    fn split_across_chunks_still_triggers() {
        let mut sm = AltScreenSm::new();
        // First chunk: "\x1b[?104"
        for &b in b"\x1b[?104" {
            sm.step(b);
        }
        assert!(!sm.in_alt_screen());
        // Second chunk: "9h"
        for &b in b"9h" {
            sm.step(b);
        }
        assert!(sm.in_alt_screen());
    }

    #[test]
    fn other_csi_does_not_trigger() {
        let mut sm = AltScreenSm::new();
        for &b in b"\x1b[31m" {
            sm.step(b);
        }
        assert!(!sm.in_alt_screen());
    }

    #[test]
    fn lone_esc_does_not_corrupt_state() {
        let mut sm = AltScreenSm::new();
        for &b in b"\x1bA" {
            sm.step(b);
        }
        assert!(!sm.in_alt_screen());
        // Now a real sequence:
        for &b in b"\x1b[?1049h" {
            sm.step(b);
        }
        assert!(sm.in_alt_screen());
    }
}

#[cfg(test)]
mod rule_tests {
    use super::*;
    use crate::style::Color;

    #[test]
    fn ipv4_in_line_gets_sgr_wrapping() {
        let compiled = Compiled::load_builtins().unwrap();
        let mut out = Vec::new();
        apply_rules(b"connect to 192.168.1.1 now\n", &compiled, &mut out).unwrap();
        let s = String::from_utf8(out).unwrap();
        assert!(s.contains("\x1b["), "expected SGR introducer in: {s:?}");
        assert!(s.contains("192.168.1.1"));
        assert!(s.contains("\x1b[0m"), "expected SGR reset");
    }

    #[test]
    fn no_match_passes_through_unchanged() {
        let compiled = Compiled::load_builtins().unwrap();
        let mut out = Vec::new();
        apply_rules(b"plain text line\n", &compiled, &mut out).unwrap();
        assert_eq!(out, b"plain text line\n");
    }
}

#[cfg(test)]
mod pipeline_tests {
    use super::*;

    #[test]
    fn alt_screen_bytes_bypass_rules() {
        let compiled = Compiled::load_builtins().unwrap();
        let mut pipe = Pipeline::new(compiled);
        let mut out = Vec::new();
        // Enter alt-screen, then write content that would otherwise be colorized.
        pipe.feed(b"\x1b[?1049h", &mut out).unwrap();
        pipe.feed(b"192.168.1.1\n", &mut out).unwrap();
        // Bytes must appear as-is (no SGR wrapping).
        assert!(out.windows(11).any(|w| w == b"192.168.1.1"));
        // The SGR introducer for our IPv4 style must NOT appear.
        // (We accept the alt-screen toggle's own ESC bytes in the output.)
        let post_toggle = &out[8..]; // skip the \x1b[?1049h
        assert!(!post_toggle.windows(2).any(|w| w == b"\x1b["));
    }
}
```

- [ ] **Step 2: Fail doğrula**

```bash
cargo test --lib pipeline
```

Expected: Compile errors.

- [ ] **Step 3: `AltScreenSm` impl**

```rust
/// Alt-screen detector implemented as a 5-state machine. See spec §3.5.
#[derive(Debug)]
pub(crate) struct AltScreenSm {
    state: SmState,
    accum: u32, // accumulated digits while in DIGITS
    active: bool,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum SmState {
    Ground,
    EscSeen,
    Csi,
    Question,
    Digits,
}

impl AltScreenSm {
    pub(crate) fn new() -> Self {
        AltScreenSm { state: SmState::Ground, accum: 0, active: false }
    }

    pub(crate) fn in_alt_screen(&self) -> bool {
        self.active
    }

    /// Advance the machine by one byte. Returns the new `active` value.
    pub(crate) fn step(&mut self, byte: u8) -> bool {
        use SmState::*;
        self.state = match (self.state, byte) {
            (Ground, 0x1b) => EscSeen,
            (Ground, _) => Ground,
            (EscSeen, b'[') => Csi,
            (EscSeen, 0x1b) => EscSeen, // chained ESC
            (EscSeen, _) => Ground,
            (Csi, b'?') => {
                self.accum = 0;
                Question
            }
            (Csi, 0x1b) => EscSeen,
            (Csi, _) => Ground,
            (Question, b) if b.is_ascii_digit() => {
                self.accum = u32::from(b - b'0');
                Digits
            }
            (Question, 0x1b) => EscSeen,
            (Question, _) => Ground,
            (Digits, b) if b.is_ascii_digit() => {
                self.accum = self.accum.saturating_mul(10).saturating_add(u32::from(b - b'0'));
                Digits
            }
            (Digits, b'h') => {
                if matches!(self.accum, 47 | 1047 | 1049) {
                    self.active = true;
                }
                Ground
            }
            (Digits, b'l') => {
                if matches!(self.accum, 47 | 1047 | 1049) {
                    self.active = false;
                }
                Ground
            }
            (Digits, 0x1b) => EscSeen,
            (Digits, _) => Ground,
        };
        self.active
    }
}
```

- [ ] **Step 4: `apply_rules` impl**

`pipeline.rs`'e ekle:

```rust
/// Apply the compiled rule set to a single line. Writes the original bytes,
/// with SGR wrappers inserted around the first non-overlapping match of each
/// rule (in rule definition order).
///
/// v0.1 strategy: "first match wins" — overlapping matches from later rules
/// are dropped. Conflict resolution as configurable priority lands in v0.5.
pub(crate) fn apply_rules<W: Write>(
    line: &[u8],
    compiled: &Compiled,
    out: &mut W,
) -> std::io::Result<()> {
    // Collect (start, end, style) spans without overlapping.
    let mut spans: Vec<(usize, usize, &Style)> = Vec::new();

    for (i, re) in compiled.individuals.iter().enumerate() {
        for m in re.find_iter(line) {
            let (start, end) = (m.start(), m.end());
            // Reject if it overlaps any existing accepted span.
            if spans.iter().any(|&(s, e, _)| !(end <= s || start >= e)) {
                continue;
            }
            spans.push((start, end, &compiled.styles[i]));
        }
    }

    // Sort spans by start position.
    spans.sort_by_key(|&(s, _, _)| s);

    let mut cursor = 0usize;
    for (start, end, style) in spans {
        out.write_all(&line[cursor..start])?;
        let sgr = style.to_sgr();
        if !sgr.is_empty() {
            out.write_all(sgr.as_bytes())?;
        }
        out.write_all(&line[start..end])?;
        out.write_all(Style::reset_sgr().as_bytes())?;
        cursor = end;
    }
    out.write_all(&line[cursor..])?;
    Ok(())
}
```

- [ ] **Step 5: `Pipeline` orchestrator**

```rust
/// Output pipeline. Owns the alt-screen SM, line buffer, and rule set.
pub(crate) struct Pipeline {
    sm: AltScreenSm,
    buffer: LineBuffer,
    rules: Compiled,
}

impl Pipeline {
    pub(crate) fn new(rules: Compiled) -> Self {
        Pipeline { sm: AltScreenSm::new(), buffer: LineBuffer::new(), rules }
    }

    /// Feed a chunk from the PTY master into the pipeline; emit processed
    /// output to `out`. May produce zero, one, or many writes per call.
    pub(crate) fn feed<W: Write>(&mut self, chunk: &[u8], out: &mut W) -> std::io::Result<()> {
        // Step the state machine across every byte and split bytes by mode.
        let mut cursor = 0;
        while cursor < chunk.len() {
            let mode_before = self.sm.in_alt_screen();
            // Advance until mode changes or chunk ends.
            let mut i = cursor;
            while i < chunk.len() {
                self.sm.step(chunk[i]);
                i += 1;
                if self.sm.in_alt_screen() != mode_before {
                    break;
                }
            }
            let segment = &chunk[cursor..i];
            cursor = i;

            if mode_before {
                // We were in alt-screen for the whole segment — passthrough.
                out.write_all(segment)?;
            } else {
                // Normal mode — buffer and apply rules per complete line.
                let (lines, overflow) = self.buffer.feed_with_overflow(segment);
                if let Some(Error::BufferOverflow { cap }) = overflow {
                    tracing::warn!(cap, "line buffer overflowed; flushing as-is");
                }
                for line in lines {
                    apply_rules(&line, &self.rules, out)?;
                }
            }
        }
        Ok(())
    }

    /// Flush any pending partial line if it has been idle long enough.
    pub(crate) fn tick<W: Write>(&mut self, out: &mut W) -> std::io::Result<()> {
        if self.sm.in_alt_screen() {
            return Ok(());
        }
        let cutoff = Instant::now() - FLUSH_TIMEOUT;
        if let Some(partial) = self.buffer.flush_if_stale(cutoff) {
            apply_rules(&partial, &self.rules, out)?;
        }
        Ok(())
    }

    /// Drain remaining bytes at shutdown. Called once after the child exits.
    pub(crate) fn drain<W: Write>(&mut self, out: &mut W) -> std::io::Result<()> {
        let remaining = self.buffer.drain();
        if !remaining.is_empty() {
            apply_rules(&remaining, &self.rules, out)?;
        }
        Ok(())
    }
}
```

- [ ] **Step 6: `lib.rs`'e ekle**

```rust
pub(crate) mod pipeline;
```

- [ ] **Step 7: Test'lerin geçtiğini doğrula**

```bash
cargo test --lib pipeline
cargo clippy --all-targets -- -D warnings
```

Expected: Tüm test'ler pass.

- [ ] **Step 8: Commit**

```bash
git add src/pipeline.rs src/lib.rs
git commit -m "feat(pipeline): alt-screen state machine and rule application

Pipeline orchestrates output processing: a 5-state machine detects
alt-screen mode entry/exit across chunk boundaries (spec §3.5),
LineBuffer accumulates non-alt bytes, and apply_rules wraps the
first non-overlapping match of each rule in SGR. In alt-screen mode
bytes pass through unmodified so vim/htop/less remain intact."
```

---

## Task 7: Shell Discovery (`shell.rs`)

`$SHELL` → `/etc/passwd` (`getpwuid`) → `/bin/sh` cascade.

**Files:**
- Create: `src/shell.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Failing test'ler**

```rust
//! Shell discovery cascade per spec §1 / brainstorming decision 3.

use std::env;
use std::path::{Path, PathBuf};

use crate::error::{Error, Result};

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn shell_override_path_wins() {
        let spec = discover_with_env(Some(Path::new("/usr/local/bin/fish")), || Some("/bin/zsh".into()), || Some("/bin/bash".into())).unwrap();
        assert_eq!(spec.path, PathBuf::from("/usr/local/bin/fish"));
    }

    #[test]
    fn shell_env_var_used_when_no_override() {
        let spec = discover_with_env(None, || Some("/bin/zsh".into()), || Some("/bin/bash".into())).unwrap();
        assert_eq!(spec.path, PathBuf::from("/bin/zsh"));
    }

    #[test]
    fn passwd_used_when_env_empty() {
        let spec = discover_with_env(None, || None, || Some("/bin/bash".into())).unwrap();
        assert_eq!(spec.path, PathBuf::from("/bin/bash"));
    }

    #[test]
    fn sh_fallback_used_when_both_absent() {
        let spec = discover_with_env(None, || None, || None).unwrap();
        assert_eq!(spec.path, PathBuf::from("/bin/sh"));
    }

    #[test]
    fn login_flag_changes_argv0() {
        let mut spec = discover_with_env(None, || Some("/bin/zsh".into()), || None).unwrap();
        assert_eq!(spec.argv0(false), "zsh");
        spec.login = true;
        assert_eq!(spec.argv0(true), "-zsh");
    }
}
```

- [ ] **Step 2: Fail doğrula**

```bash
cargo test --lib shell
```

Expected: Compile errors.

- [ ] **Step 3: Impl**

```rust
/// What to launch and how.
#[derive(Debug, Clone)]
pub(crate) struct ShellSpec {
    pub(crate) path: PathBuf,
    pub(crate) login: bool,
}

impl ShellSpec {
    /// What to pass as argv[0] when spawning. Login shells conventionally
    /// receive a leading `-` (e.g. `-zsh`).
    pub(crate) fn argv0(&self, login: bool) -> String {
        let base = self.path.file_name().and_then(|s| s.to_str()).unwrap_or("sh");
        if login { format!("-{base}") } else { base.to_string() }
    }
}

/// Production entry point. Resolves the shell via `$SHELL` then `/etc/passwd`
/// then `/bin/sh`. The `override_path` (typically the `--shell` CLI flag)
/// short-circuits the cascade.
///
/// # Errors
/// Returns `Error::ShellDiscovery` only if every source fails (extremely
/// unusual; only on truly broken systems).
pub(crate) fn discover(override_path: Option<&Path>, login: bool) -> Result<ShellSpec> {
    let mut spec = discover_with_env(
        override_path,
        || env::var("SHELL").ok().filter(|s| !s.is_empty()),
        passwd_shell,
    )?;
    spec.login = login;
    Ok(spec)
}

fn discover_with_env(
    override_path: Option<&Path>,
    env_lookup: impl FnOnce() -> Option<String>,
    passwd_lookup: impl FnOnce() -> Option<String>,
) -> Result<ShellSpec> {
    if let Some(p) = override_path {
        return Ok(ShellSpec { path: p.to_path_buf(), login: false });
    }
    if let Some(env_path) = env_lookup() {
        return Ok(ShellSpec { path: PathBuf::from(env_path), login: false });
    }
    if let Some(passwd_path) = passwd_lookup() {
        return Ok(ShellSpec { path: PathBuf::from(passwd_path), login: false });
    }
    Ok(ShellSpec { path: PathBuf::from("/bin/sh"), login: false })
}

fn passwd_shell() -> Option<String> {
    use nix::unistd::{getuid, User};
    let user = User::from_uid(getuid()).ok().flatten()?;
    let shell = user.shell.to_string_lossy().to_string();
    if shell.is_empty() { None } else { Some(shell) }
}
```

- [ ] **Step 4: `lib.rs`**

```rust
pub(crate) mod shell;
```

- [ ] **Step 5: Test geçişini doğrula**

```bash
cargo test --lib shell
cargo clippy --all-targets -- -D warnings
```

Expected: 5 test pass.

- [ ] **Step 6: Commit**

```bash
git add src/shell.rs src/lib.rs
git commit -m "feat(shell): discover shell via \$SHELL -> /etc/passwd -> /bin/sh

Implements the brainstorming-decision-3 cascade. The cascade is
split into discover_with_env (pure, testable) and discover
(production wiring) so tests cover the priority logic without
mocking the environment. ShellSpec.argv0 prefixes login shells
with '-' per Unix convention."
```

---

## Task 8: Terminfo (`terminfo.rs`)

`isatty(stdout)` + renk derinliği tespiti (`$COLORTERM`, `$TERM`).

**Files:**
- Create: `src/terminfo.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Failing test**

```rust
//! Terminal capability detection: TTY status and color depth.

use std::env;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn depth_from_colorterm_truecolor() {
        assert_eq!(depth_from_env(Some("truecolor"), Some("xterm-256color")), ColorDepth::Truecolor);
        assert_eq!(depth_from_env(Some("24bit"), None), ColorDepth::Truecolor);
    }

    #[test]
    fn depth_from_term_256() {
        assert_eq!(depth_from_env(None, Some("xterm-256color")), ColorDepth::Indexed256);
        assert_eq!(depth_from_env(None, Some("screen-256color")), ColorDepth::Indexed256);
    }

    #[test]
    fn depth_falls_back_to_basic() {
        assert_eq!(depth_from_env(None, Some("xterm")), ColorDepth::Basic16);
        assert_eq!(depth_from_env(None, None), ColorDepth::Basic16);
    }

    #[test]
    fn depth_handles_dumb_term() {
        assert_eq!(depth_from_env(None, Some("dumb")), ColorDepth::None);
    }
}
```

- [ ] **Step 2: Fail doğrula**

- [ ] **Step 3: Impl**

```rust
/// Maximum color depth supported by the terminal we are connected to.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub(crate) enum ColorDepth {
    /// `TERM=dumb` or no color allowed.
    None,
    /// 16-color ANSI.
    Basic16,
    /// 256-color indexed palette.
    Indexed256,
    /// 24-bit truecolor.
    Truecolor,
}

/// Check whether stdout is connected to a terminal.
pub(crate) fn stdout_is_tty() -> bool {
    use nix::unistd::isatty;
    isatty(libc::STDOUT_FILENO).unwrap_or(false)
}

/// Detect the maximum supported color depth from `$COLORTERM` and `$TERM`.
pub(crate) fn detect_depth() -> ColorDepth {
    let colorterm = env::var("COLORTERM").ok();
    let term = env::var("TERM").ok();
    depth_from_env(colorterm.as_deref(), term.as_deref())
}

fn depth_from_env(colorterm: Option<&str>, term: Option<&str>) -> ColorDepth {
    if matches!(term, Some("dumb")) {
        return ColorDepth::None;
    }
    if let Some(ct) = colorterm {
        if ct.eq_ignore_ascii_case("truecolor") || ct == "24bit" {
            return ColorDepth::Truecolor;
        }
    }
    if let Some(t) = term {
        if t.contains("256color") {
            return ColorDepth::Indexed256;
        }
    }
    ColorDepth::Basic16
}
```

Not: `libc` import edilmedi — `Cargo.toml`'a eklemek yerine `nix::libc::STDOUT_FILENO` kullan:

```rust
use nix::libc::STDOUT_FILENO;
// ...
isatty(STDOUT_FILENO)
```

- [ ] **Step 4: `lib.rs`**

```rust
pub(crate) mod terminfo;
```

- [ ] **Step 5: Test + lint**

```bash
cargo test --lib terminfo
cargo clippy --all-targets -- -D warnings
```

Expected: 4 test pass.

- [ ] **Step 6: Commit**

```bash
git add src/terminfo.rs src/lib.rs
git commit -m "feat(terminfo): detect TTY and color depth from environment

Provides stdout_is_tty (used to bypass colorization for piped output)
and detect_depth (truecolor/256/basic-16/none) derived from
\$COLORTERM and \$TERM. Pure depth_from_env is exposed for testing."
```

---

## Task 9: Logging (`logging.rs`)

`TAYF_LOG` env var ile `tracing-subscriber` init; default off.

**Files:**
- Create: `src/logging.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Impl + smoke test**

```rust
//! Diagnostic logging via tracing-subscriber.
//!
//! Off by default. Writes only to stderr to avoid contaminating user output.
//! Enabled by setting `TAYF_LOG`, e.g. `TAYF_LOG=debug tayf`.

/// Initialize the global subscriber. Safe to call exactly once at program start.
pub(crate) fn init() {
    use tracing_subscriber::EnvFilter;

    let filter = EnvFilter::try_from_env("TAYF_LOG").unwrap_or_else(|_| EnvFilter::new("off"));

    let _ = tracing_subscriber::fmt()
        .with_env_filter(filter)
        .with_writer(std::io::stderr)
        .with_target(false)
        .with_level(true)
        .try_init();
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn init_is_idempotent() {
        init();
        init();
    }
}
```

- [ ] **Step 2: `lib.rs`**

```rust
pub(crate) mod logging;
```

- [ ] **Step 3: Test + lint**

```bash
cargo test --lib logging
cargo clippy --all-targets -- -D warnings
```

- [ ] **Step 4: Commit**

```bash
git add src/logging.rs src/lib.rs
git commit -m "feat(logging): off-by-default tracing-subscriber wired to stderr

TAYF_LOG env var (e.g. TAYF_LOG=debug) activates diagnostic logging
to stderr. Default is off so we never leak credentials or noise to
the user's pipeline (CLAUDE.md §3, credentials/PII)."
```

---

## Task 10: Version Module (`version.rs`) + `build.rs`

`--version` çıktısı için build-time SHA ve rustc versiyonu.

**Files:**
- Create: `src/version.rs`
- Create: `build.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: `build.rs`**

```rust
fn main() {
    built::write_built_file().expect("failed to acquire build-time information");
}
```

- [ ] **Step 2: `src/version.rs`**

```rust
//! Build-time info surfaced by `--version`.

pub(crate) mod built_info {
    include!(concat!(env!("OUT_DIR"), "/built.rs"));
}

/// Format the version banner shown by `--version`.
#[must_use]
pub fn version_string() -> String {
    let sha = built_info::GIT_COMMIT_HASH.unwrap_or("unknown");
    let dirty = built_info::GIT_DIRTY.unwrap_or(false);
    let suffix = if dirty { "-dirty" } else { "" };
    format!(
        "tayf {pkg} (sha {sha}{suffix}, rustc {rustc})",
        pkg = built_info::PKG_VERSION,
        rustc = built_info::RUSTC_VERSION,
    )
}
```

- [ ] **Step 3: `lib.rs`**

```rust
pub mod version;
```

- [ ] **Step 4: Build + run smoke**

```bash
cargo build
cargo run -- --version  # CLI Task 11'de gelene kadar henüz çağrılmıyor, build yeterli
```

Expected: build temiz.

- [ ] **Step 5: Commit**

```bash
git add build.rs src/version.rs src/lib.rs
git commit -m "feat(version): expose build-time SHA and rustc version

Uses the built crate to embed the git SHA, dirty flag, and rustc
version at compile time. Consumed by --version in Task 11."
```

---

## Task 11: CLI (`cli.rs`)

Clap argümanları: `--shell`, `--login`, `--no-color`, `--help`, `--version`.

**Files:**
- Create: `src/cli.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Impl**

```rust
//! Command-line interface definition (clap derive).

use std::path::PathBuf;

use clap::Parser;

/// Terminal-agnostic, PTY-based, regex-driven output colorizer.
#[derive(Debug, Parser)]
#[command(
    name = "tayf",
    author,
    version = crate::version::version_string(),
    about,
    long_about = None,
)]
pub struct Args {
    /// Override the shell to launch. Defaults to $SHELL, then /etc/passwd, then /bin/sh.
    #[arg(long, value_name = "PATH")]
    pub shell: Option<PathBuf>,

    /// Spawn the shell as a login shell (e.g. -zsh as argv[0]).
    #[arg(long, default_value_t = false)]
    pub login: bool,

    /// Disable colorization. Useful for debugging or when stdout is a TTY but
    /// you want raw passthrough.
    #[arg(long, default_value_t = false)]
    pub no_color: bool,
}

impl Args {
    #[must_use]
    pub fn parse_from_env() -> Self {
        <Self as Parser>::parse()
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use clap::Parser;

    #[test]
    fn parses_minimal() {
        let args = Args::try_parse_from(["tayf"]).unwrap();
        assert!(args.shell.is_none());
        assert!(!args.login);
        assert!(!args.no_color);
    }

    #[test]
    fn parses_all_flags() {
        let args = Args::try_parse_from(["tayf", "--shell", "/bin/fish", "--login", "--no-color"]).unwrap();
        assert_eq!(args.shell.as_deref(), Some(std::path::Path::new("/bin/fish")));
        assert!(args.login);
        assert!(args.no_color);
    }
}
```

- [ ] **Step 2: `lib.rs`**

```rust
pub mod cli;
```

- [ ] **Step 3: Test + lint**

```bash
cargo test --lib cli
cargo clippy --all-targets -- -D warnings
```

Expected: 2 test pass.

- [ ] **Step 4: Commit**

```bash
git add src/cli.rs src/lib.rs
git commit -m "feat(cli): clap-derived argument parser

Defines --shell, --login, and --no-color. --version is automatic
via clap and routes through version::version_string for the SHA-
and-rustc banner."
```

---

## Task 12: TTY Guard (`tty_guard.rs`)

`termios` snapshot + RAII restore + panic hook fallback. Spec §3.4 ve §6 (terminal corruption).

**Files:**
- Create: `src/tty_guard.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Impl**

```rust
//! RAII guard that snapshots the controlling-terminal termios state on
//! creation, switches stdin into raw mode, and restores the original state
//! on drop, on panic, and on any normal exit path. See spec §6 (terminal
//! corruption) and CLAUDE.md §3.

use std::os::fd::RawFd;
use std::sync::{Mutex, OnceLock};

use nix::libc::STDIN_FILENO;
use nix::sys::termios::{cfmakeraw, tcgetattr, tcsetattr, SetArg, Termios};

use crate::error::Result;

/// Mutex-protected snapshot used by the panic hook to restore the terminal
/// when a thread panics. Set only while a guard is alive.
static PANIC_RESTORE_STATE: OnceLock<Mutex<Option<(RawFd, Termios)>>> = OnceLock::new();

/// Snapshot stdin's termios, switch it to raw mode, and restore on drop.
pub(crate) struct TtyGuard {
    fd: RawFd,
    original: Termios,
}

impl TtyGuard {
    /// Engage raw mode. Must be called on the main thread.
    ///
    /// # Errors
    /// Returns `Error::Tty` if stdin is not a terminal or termios cannot be set.
    pub(crate) fn engage() -> Result<Self> {
        let fd = STDIN_FILENO;
        let original = tcgetattr(fd)?;

        let mut raw = original.clone();
        cfmakeraw(&mut raw);
        tcsetattr(fd, SetArg::TCSANOW, &raw)?;

        install_panic_hook(fd, original.clone());

        Ok(TtyGuard { fd, original })
    }
}

impl Drop for TtyGuard {
    fn drop(&mut self) {
        let _ = tcsetattr(self.fd, SetArg::TCSANOW, &self.original);
        // Clear the panic-hook snapshot.
        if let Some(mux) = PANIC_RESTORE_STATE.get() {
            if let Ok(mut g) = mux.lock() {
                *g = None;
            }
        }
    }
}

fn install_panic_hook(fd: RawFd, original: Termios) {
    let mux = PANIC_RESTORE_STATE.get_or_init(|| Mutex::new(None));
    if let Ok(mut g) = mux.lock() {
        *g = Some((fd, original));
    }

    static HOOK_INSTALLED: std::sync::Once = std::sync::Once::new();
    HOOK_INSTALLED.call_once(|| {
        let prev = std::panic::take_hook();
        std::panic::set_hook(Box::new(move |info| {
            if let Some(mux) = PANIC_RESTORE_STATE.get() {
                if let Ok(g) = mux.lock() {
                    if let Some((fd, ref original)) = *g {
                        let _ = tcsetattr(fd, SetArg::TCSANOW, original);
                    }
                }
            }
            prev(info);
        }));
    });
}

#[cfg(test)]
mod tests {
    // Termios tests require a real TTY; covered by integration_smoke. Unit-
    // testing this in CI without a TTY would be misleading.
}
```

Not: `cfg(test)` modülünün boş ama dökümante edilmiş olması clippy'i memnun eder; runtime'da TTY olmadığı için unit test koymuyoruz.

- [ ] **Step 2: `lib.rs`**

```rust
pub(crate) mod tty_guard;
```

- [ ] **Step 3: Build + lint**

```bash
cargo build
cargo clippy --all-targets -- -D warnings
```

Expected: temiz.

- [ ] **Step 4: Commit**

```bash
git add src/tty_guard.rs src/lib.rs
git commit -m "feat(tty_guard): RAII raw-mode guard with panic-hook restore

TtyGuard snapshots stdin's termios, switches to raw mode, restores
on Drop, and installs a panic hook that restores the snapshot if a
thread panics. The hook is installed exactly once (call_once) but
the snapshot pointer is swappable so multiple guards across the
program lifetime work correctly."
```

---

## Task 13: PTY Session (`pty.rs`)

`PtySession` + `into_parts()` decomposition: Reader, Writer, Resizer, ChildHandle.

**Files:**
- Create: `src/pty.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Impl**

```rust
//! PTY session built on portable-pty.
//!
//! `PtySession::spawn` opens a master/slave pair, sizes it to the current
//! terminal, and launches the requested shell. `into_parts` decomposes the
//! session into four narrow handles owned by separate threads (spec §3.3).

use std::io::{Read, Write};
use std::sync::{Arc, Mutex};

use portable_pty::{native_pty_system, Child, CommandBuilder, MasterPty, PtySize};

use crate::error::Result;
use crate::shell::ShellSpec;

/// Owning handle to a spawned PTY + child process.
pub(crate) struct PtySession {
    master: Box<dyn MasterPty + Send>,
    child: Box<dyn Child + Send + Sync>,
}

/// Read half of the master.
pub(crate) struct Reader {
    inner: Box<dyn Read + Send>,
}

/// Write half of the master.
pub(crate) struct Writer {
    inner: Box<dyn Write + Send>,
}

/// Thread-safe resizer; cloned for use by the signal thread.
pub(crate) struct Resizer {
    master: Arc<Mutex<Box<dyn MasterPty + Send>>>,
}

/// Child reaper + exit-code accessor.
pub(crate) struct ChildHandle {
    child: Box<dyn Child + Send + Sync>,
}

impl PtySession {
    /// Spawn `spec` in a fresh PTY sized to the current terminal.
    ///
    /// # Errors
    /// Returns `Error::Pty` if the pty system, terminal size lookup, or the
    /// child spawn fails.
    pub(crate) fn spawn(spec: &ShellSpec) -> Result<Self> {
        let pty_system = native_pty_system();
        let size = current_term_size().unwrap_or(PtySize {
            rows: 24,
            cols: 80,
            pixel_width: 0,
            pixel_height: 0,
        });

        let pair = pty_system.openpty(size).map_err(io_err)?;

        let mut cmd = CommandBuilder::new(spec.path.as_os_str());
        cmd.arg0(spec.argv0(spec.login));
        if spec.login {
            // Some shells need an explicit -l flag in addition to '-' argv[0].
            // bash: needs -l ; zsh: argv[0]='-' is enough but -l doesn't hurt.
            cmd.arg("-l");
        }

        let child = pair.slave.spawn_command(cmd).map_err(io_err)?;
        drop(pair.slave);

        Ok(PtySession {
            master: pair.master,
            child,
        })
    }

    /// Split the session into four narrow handles.
    ///
    /// # Errors
    /// Returns `Error::Pty` if the master fd cannot be cloned for reading or
    /// writing.
    pub(crate) fn into_parts(self) -> Result<(Reader, Writer, Resizer, ChildHandle)> {
        let reader = self.master.try_clone_reader().map_err(io_err)?;
        let writer = self.master.take_writer().map_err(io_err)?;

        let master = Arc::new(Mutex::new(self.master));
        let resizer = Resizer { master };

        Ok((
            Reader { inner: reader },
            Writer { inner: writer },
            resizer,
            ChildHandle { child: self.child },
        ))
    }
}

impl Reader {
    pub(crate) fn read(&mut self, buf: &mut [u8]) -> std::io::Result<usize> {
        self.inner.read(buf)
    }
}

impl Writer {
    pub(crate) fn write_all(&mut self, buf: &[u8]) -> std::io::Result<()> {
        self.inner.write_all(buf)
    }

    /// Close the master write side so that the input thread receives EPIPE
    /// on its next write and exits cleanly. See spec §3.4.
    pub(crate) fn shutdown(self) {
        drop(self.inner);
    }
}

impl Resizer {
    pub(crate) fn resize(&self, rows: u16, cols: u16) -> Result<()> {
        let m = self.master.lock().expect("resizer mutex poisoned");
        m.resize(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 }).map_err(io_err)?;
        Ok(())
    }
}

impl ChildHandle {
    pub(crate) fn wait(&mut self) -> Result<i32> {
        let status = self.child.wait().map_err(io_err)?;
        let code: i32 = status.exit_code().try_into().unwrap_or(70);
        Ok(code)
    }

    /// Process ID of the child (used for signal forwarding to its group).
    pub(crate) fn pid(&self) -> Option<u32> {
        self.child.process_id()
    }
}

fn current_term_size() -> Option<PtySize> {
    use nix::libc::{ioctl, winsize, STDOUT_FILENO, TIOCGWINSZ};
    // SAFETY: ioctl with TIOCGWINSZ writes into our local winsize and reads
    // from a file descriptor we own (stdout). The struct layout matches the
    // kernel's expectation. On failure we return None.
    let mut ws: winsize = unsafe { std::mem::zeroed() };
    let rc = unsafe { ioctl(STDOUT_FILENO, TIOCGWINSZ as _, &mut ws as *mut _) };
    if rc != 0 {
        return None;
    }
    Some(PtySize {
        rows: ws.ws_row,
        cols: ws.ws_col,
        pixel_width: ws.ws_xpixel,
        pixel_height: ws.ws_ypixel,
    })
}

fn io_err(e: impl std::fmt::Display) -> crate::error::Error {
    crate::error::Error::Pty(std::io::Error::new(std::io::ErrorKind::Other, e.to_string()))
}
```

Not: Bu modülde `unsafe` blok var (`current_term_size`). `src/lib.rs` üstündeki `#![deny(unsafe_code)]` ile çakışır. `pty.rs` üstüne `#![allow(unsafe_code)]` koyamayız (module-level değil). Çözüm:

`lib.rs` üstündeki crate-level `#![deny(unsafe_code)]`'u kaldır, yerine `#![warn(unsafe_code)]` koy. Sonra `pty.rs` içindeki ioctl bloğuna `// SAFETY:` zaten var. Veya tüm unsafe'i tek bir küçük modüle hapset (`pty/winsize.rs`).

Bu görev tek modülde yapacaksak `lib.rs` upper-cratedirective'i değiştir.

- [ ] **Step 2: `lib.rs`'te unsafe direktifini güncelle**

```rust
#![warn(unsafe_code)]   // was: #![deny(unsafe_code)]
```

Crate seviyesinde tamamen yasak yerine "uyarı, sadece SAFETY yorumlu ve nokta-atışı yerlerde". `clippy.toml`'a ekle:

```toml
# unsafe is permitted only with a SAFETY comment; reviewer enforces.
```

- [ ] **Step 3: `lib.rs`**

```rust
pub(crate) mod pty;
```

- [ ] **Step 4: Build + lint**

```bash
cargo build
cargo clippy --all-targets -- -D warnings
```

Expected: temiz (unsafe uyarısı SAFETY yorumla susturuldu).

- [ ] **Step 5: Commit**

```bash
git add src/pty.rs src/lib.rs
git commit -m "feat(pty): PtySession with four-way ownership split

Wraps portable-pty's master/child handles and exposes
into_parts() to hand out Reader, Writer, Resizer, and ChildHandle
to the threads that need them (spec §3.3). Writer::shutdown
closes the master write side, which is the mechanism main uses
to signal the input thread to exit (spec §3.4 step 6)."
```

---

## Task 14: Signals (`signals.rs`)

`signal_hook::iterator::Signals` ile SIGWINCH→resize ve SIGINT/SIGTERM→child PGID.

**Files:**
- Create: `src/signals.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Impl**

```rust
//! Signal handling thread.
//!
//! Spawns a dedicated thread that consumes signals from the signal_hook
//! iterator API (recommended in the senior review and CLAUDE.md compatible —
//! no signal-safety footguns). The thread forwards SIGWINCH to the PTY master
//! and SIGINT/SIGTERM to the child's process group.

use std::thread::{self, JoinHandle};

use signal_hook::consts::{SIGINT, SIGTERM, SIGWINCH};
use signal_hook::iterator::Signals;

use crate::error::{Error, Result};
use crate::pty::Resizer;

/// Owning handle to the signal thread. Drop closes the signal iterator and
/// joins the thread.
pub(crate) struct SignalGuard {
    signals: Option<Signals>,
    handle: Option<JoinHandle<()>>,
}

impl Drop for SignalGuard {
    fn drop(&mut self) {
        if let Some(s) = self.signals.take() {
            s.close();
        }
        if let Some(h) = self.handle.take() {
            let _ = h.join();
        }
    }
}

/// Install signal handling. `resizer` is moved into the signal thread.
/// `child_pid` is forwarded to as the process-group leader on SIGINT/SIGTERM.
///
/// # Errors
/// Returns `Error::Signal` if signal_hook fails to register handlers.
pub(crate) fn spawn_handler(resizer: Resizer, child_pid: Option<u32>) -> Result<SignalGuard> {
    let mut signals = Signals::new([SIGWINCH, SIGINT, SIGTERM]).map_err(Error::Signal)?;
    let signals_for_thread = signals.handle();

    let handle = thread::Builder::new()
        .name("tayf-signals".into())
        .spawn(move || {
            for sig in &mut signals {
                match sig {
                    SIGWINCH => {
                        if let Some((rows, cols)) = read_winsize() {
                            let _ = resizer.resize(rows, cols);
                        }
                    }
                    SIGINT | SIGTERM => {
                        if let Some(pid) = child_pid {
                            forward_to_pgid(pid as i32, sig);
                        }
                    }
                    _ => {}
                }
            }
        })
        .map_err(|e| Error::Signal(e))?;

    Ok(SignalGuard {
        signals: None, // ownership moved into thread; the handle stays here via signals.handle() not retained
        handle: Some(handle),
    })
}

fn read_winsize() -> Option<(u16, u16)> {
    use nix::libc::{ioctl, winsize, STDOUT_FILENO, TIOCGWINSZ};
    let mut ws: winsize = unsafe { std::mem::zeroed() };
    // SAFETY: TIOCGWINSZ writes into our local winsize from an fd we own.
    let rc = unsafe { ioctl(STDOUT_FILENO, TIOCGWINSZ as _, &mut ws as *mut _) };
    if rc != 0 {
        None
    } else {
        Some((ws.ws_row, ws.ws_col))
    }
}

fn forward_to_pgid(child_pid: i32, sig: i32) {
    use nix::sys::signal::{killpg, Signal};
    use nix::unistd::Pid;
    if let Ok(s) = Signal::try_from(sig) {
        let _ = killpg(Pid::from_raw(child_pid), s);
    }
}
```

Not: `signal_hook::iterator::Handle` ile thread-safe close, ama yukarıdaki impl `signals.close()` çağrısı için `Signals` instance'ına ihtiyaç duyar. Daha temiz yol: `signals.handle()` ile aldığımız `Handle`'ı tutmak ve onun `close()` metoduyla iterator'u durdurmak. Düzeltme:

```rust
pub(crate) struct SignalGuard {
    handle: Option<JoinHandle<()>>,
    closer: signal_hook::iterator::Handle,
}

impl Drop for SignalGuard {
    fn drop(&mut self) {
        self.closer.close();
        if let Some(h) = self.handle.take() {
            let _ = h.join();
        }
    }
}

pub(crate) fn spawn_handler(resizer: Resizer, child_pid: Option<u32>) -> Result<SignalGuard> {
    let mut signals = Signals::new([SIGWINCH, SIGINT, SIGTERM]).map_err(Error::Signal)?;
    let closer = signals.handle();
    let handle = thread::Builder::new()
        .name("tayf-signals".into())
        .spawn(move || {
            for sig in &mut signals {
                // ... same match as before ...
            }
        })
        .map_err(Error::Signal)?;
    Ok(SignalGuard { handle: Some(handle), closer })
}
```

Üstteki temiz versiyonu kullan.

- [ ] **Step 2: `lib.rs`**

```rust
pub(crate) mod signals;
```

- [ ] **Step 3: Build + lint**

```bash
cargo build
cargo clippy --all-targets -- -D warnings
```

- [ ] **Step 4: Commit**

```bash
git add src/signals.rs src/lib.rs
git commit -m "feat(signals): signal_hook-driven SIGWINCH + SIGINT/SIGTERM thread

Dedicated thread consumes the signal_hook iterator; SIGWINCH triggers
PTY resize via the Resizer handle and SIGINT/SIGTERM forward to the
child's process group with killpg (CLAUDE.md §3 signal forwarding).
The SignalGuard's Drop closes the iterator and joins the thread."
```

---

## Task 15: Runtime (`runtime.rs`) — Two Threads + Shutdown Protocol

Output thread + input thread + main-thread orchestration. Shutdown protokolü spec §3.4.

**Files:**
- Create: `src/runtime.rs`
- Modify: `src/lib.rs`

- [ ] **Step 1: Impl**

```rust
//! Two-thread runtime + shutdown orchestration. See spec §3.4.

use std::io::{self, Read, Write};
use std::thread::{self, JoinHandle};
use std::time::Duration;

use crate::error::Result;
use crate::pipeline::Pipeline;
use crate::pty::{ChildHandle, Reader, Writer};
use crate::rules::Compiled;

/// Read chunk size for the master fd.
const READ_BUF_BYTES: usize = 8 * 1024;
/// Tick interval for partial-line idle flushes.
const TICK_INTERVAL: Duration = Duration::from_millis(20);

/// Run the I/O loop until the child exits.
///
/// Spawns the output thread (PTY → stdout via Pipeline) and the input thread
/// (stdin → PTY). The main thread waits on the child, signals shutdown via
/// `writer.shutdown()`, and joins the output thread. The input thread is not
/// joined — it may still be blocked on `stdin.read()`; the OS reaps it on
/// process exit (spec §3.4 step 7, v0.1 limit documented there).
///
/// # Errors
/// Returns the child's exit code on success, or `Error::Pty` on I/O failures.
pub(crate) fn run(
    mut reader: Reader,
    writer: Writer,
    mut child: ChildHandle,
    rules: Compiled,
    apply_colors: bool,
) -> Result<i32> {
    let pipeline = Pipeline::new(rules);

    let output_handle = spawn_output_thread(reader, pipeline, apply_colors);
    let _input_handle = spawn_input_thread(writer);

    let code = child.wait()?;

    // Drain the output thread. It should already be exiting because the
    // child closed the slave; we just join.
    if let Ok(Ok(())) = output_handle.join() {
        // ok
    }

    Ok(code)
}

fn spawn_output_thread(mut reader: Reader, mut pipeline: Pipeline, apply_colors: bool) -> JoinHandle<io::Result<()>> {
    thread::Builder::new()
        .name("tayf-output".into())
        .spawn(move || {
            let mut stdout = io::stdout().lock();
            let mut buf = vec![0u8; READ_BUF_BYTES];
            loop {
                match reader.read(&mut buf) {
                    Ok(0) => break,                    // macOS: child closed
                    Ok(n) => {
                        if apply_colors {
                            pipeline.feed(&buf[..n], &mut stdout)?;
                        } else {
                            stdout.write_all(&buf[..n])?;
                        }
                        stdout.flush()?;
                    }
                    Err(e) if e.raw_os_error() == Some(nix::libc::EIO) => break, // Linux: child closed
                    Err(e) if e.kind() == io::ErrorKind::Interrupted => continue,
                    Err(e) => return Err(e),
                }
            }
            // Final drain of any partial line.
            if apply_colors {
                pipeline.drain(&mut stdout)?;
                stdout.flush()?;
            }
            Ok(())
        })
        .expect("output thread must spawn")
}

fn spawn_input_thread(mut writer: Writer) -> JoinHandle<()> {
    thread::Builder::new()
        .name("tayf-input".into())
        .spawn(move || {
            let mut stdin = io::stdin().lock();
            let mut buf = vec![0u8; READ_BUF_BYTES];
            loop {
                match stdin.read(&mut buf) {
                    Ok(0) => break,
                    Ok(n) => {
                        if writer.write_all(&buf[..n]).is_err() {
                            break;
                        }
                    }
                    Err(e) if e.kind() == io::ErrorKind::Interrupted => continue,
                    Err(_) => break,
                }
            }
        })
        .expect("input thread must spawn")
}

// Avoid unused import warning when TICK_INTERVAL is wired into the tick loop
// in a v0.2 enhancement. For v0.1 we rely on \n-based flushing only; partial
// flush is handled at drain() time. The constant stays for forward planning.
const _: Duration = TICK_INTERVAL;
```

Not: `pipeline.tick()` v0.2'de prompt timeout flush'ı için kullanılacak. v0.1'de sadece `drain()` ile shutdown'da partial alınır. Kullanıcı interaktif prompt'ta beklerken renkleme istemese de bu kabul edilebilir bir v0.1 davranışı.

Daha temizi: `tick`'i v0.1'de aktive et — ayrı bir tick thread'i değil, output thread'in read'inde nonblocking olarak işle. Bu daha karmaşık; v0.2'ye bırakıyoruz. Bu kararı `tick` constant'ı `_` discard ile dokümante ediyor.

Actually `apply_rules` reader bloklayıcı; tick için ayrı thread daha temiz. v0.1'de kullanıcı prompt'unda renk olmaması kabul edilebilir.

Bu görevde önemli düzeltme: `mut reader: Reader` parametresine `mut` eklendi; clippy `unused_mut` uyarısı vermesin diye.

- [ ] **Step 2: `lib.rs`**

```rust
pub(crate) mod runtime;
```

- [ ] **Step 3: Build + lint**

```bash
cargo build
cargo clippy --all-targets -- -D warnings
```

Beklenen: temiz. `unused` veya `dead_code` uyarısı çıkarsa `#[allow(dead_code)]` ile yorum ekle.

- [ ] **Step 4: Commit**

```bash
git add src/runtime.rs src/lib.rs
git commit -m "feat(runtime): two-thread I/O loop with documented shutdown

Output thread reads PTY master, feeds Pipeline, writes stdout.
Input thread reads stdin, writes PTY master. Both terminate cleanly
when the child exits: output sees EIO (Linux) or Ok(0) (macOS), then
drains any pending partial line; input is released by writer.shutdown
in the facade (Task 16) on its next write. The v0.1 limitation about
the input thread potentially blocking on stdin until the next byte is
documented in spec §3.4 step 7."
```

---

## Task 16: lib.rs Facade (`Tayf::run`)

Üst seviye orchestration. `Args` → `ShellSpec` → `PtySession` → `TtyGuard` → `signals::spawn_handler` → `runtime::run` → `ExitCode`.

**Files:**
- Modify: `src/lib.rs`

- [ ] **Step 1: Yenile**

`src/lib.rs` mevcut placeholder `Tayf` impl'ini değiştir:

```rust
//! tayf — terminal-agnostic, PTY-based, regex-driven output colorizer.
//!
//! Public entry point: [`Tayf::run`]. See
//! `docs/superpowers/specs/2026-05-21-tayf-v0.1-design.md` for the full design.

#![warn(unsafe_code)]
#![warn(clippy::pedantic, clippy::cargo)]
#![allow(clippy::module_name_repetitions, clippy::missing_errors_doc)]

pub mod cli;
pub mod error;
pub mod style;
pub mod version;

pub(crate) mod line_buffer;
pub(crate) mod logging;
pub(crate) mod pipeline;
pub(crate) mod pty;
pub(crate) mod rules;
pub(crate) mod runtime;
pub(crate) mod shell;
pub(crate) mod signals;
pub(crate) mod terminfo;
pub(crate) mod tty_guard;

pub use cli::Args;
pub use error::{Error, Result};

use std::process::ExitCode;

/// Top-level facade. Wires everything together.
pub struct Tayf;

impl Tayf {
    /// Run tayf with the given arguments. Returns the exit code that should
    /// be propagated to the OS.
    ///
    /// # Errors
    /// Returns any [`Error`] encountered during setup or execution. Failures
    /// after the TTY guard is engaged are still surfaced; the guard's Drop
    /// restores the terminal.
    pub fn run(args: Args) -> Result<ExitCode> {
        logging::init();

        let apply_colors = !args.no_color && terminfo::stdout_is_tty();

        let spec = shell::discover(args.shell.as_deref(), args.login)?;

        let guard = tty_guard::TtyGuard::engage()?;
        let session = pty::PtySession::spawn(&spec)?;
        let (reader, writer, resizer, child) = session.into_parts()?;
        let child_pid = child.pid();

        let _signals = signals::spawn_handler(resizer, child_pid)?;

        let rules = rules::Compiled::load_builtins()?;
        let exit_code = runtime::run(reader, writer.into_shutdown_aware(), child, rules, apply_colors)?;

        drop(guard); // explicit, even though Drop would handle it
        Ok(ExitCode::from(u8::try_from(exit_code & 0xff).unwrap_or(1)))
    }
}
```

Not: `writer.into_shutdown_aware()` adında bir helper yok aslında. Direkt `writer`'ı geç; `runtime::run` shutdown'ı kendi içinde `Writer::shutdown` ile yapıyor olmalı. Düzeltme:

`runtime.rs`'te `run` imzasını gözden geçir — child wait'ten sonra writer'ı drop etmesi gerek. Mevcut impl'de `spawn_input_thread(writer)` writer'ı thread'e move ediyor; thread loop'tan çıkınca writer drop oluyor → master write fd kapanıyor.

Ama bizim ihtiyacımız: child exit ettikten sonra input thread'i unblock'lamak. Output thread `Reader`'ı move ediyor → child exit'inde okuma EIO/0 → output thread exit → reader drop → master read fd kapanır (bu input thread'i etkilemez).

Input thread'i unblock'lamak için: writer'ı main thread'in tutması ve child wait'ten sonra `writer.shutdown()` çağırması gerek. Ama writer input thread'e move edildi — kontrolü yok.

Çözüm: writer'ı `Arc` ile paylaş. Veya: child wait'ten sonra `signal_hook::low_level::raise(SIGUSR1)` ile input thread'in stdin read'ini EINTR yap. Veya: `nix::unistd::close(STDIN_FILENO)`. Veya en temiz: writer'ı clone et (ama writer Box<dyn Write> generic), iki tane yarat.

`portable-pty` master için iki writer mümkün mü? `MasterPty::take_writer()` `&mut self` alıyor sanırım — sadece bir kez. `try_clone_reader` var ama writer için karşılık yok.

Çözüm: `runtime::run` writer'ı thread'e değil, kendisi yutar; iki "writer kuyruğu" mantığı kurar. Aşırı karmaşık. v0.1 için pragmatik karar:

**Input thread join EDİLMEZ.** Spec §3.4 step 7 bunu zaten dokümante ediyor. `runtime::run` child exit'i bekler, return eder, `main` ExitCode döner, OS input thread'i sonlandırır.

Bu zaten mevcut impl'de var — `_input_handle` underscore ile detach. `writer.shutdown()` aramaya gerek yok.

Düzeltilmiş `lib.rs` `Tayf::run`:

```rust
        let _signals = signals::spawn_handler(resizer, child_pid)?;

        let rules = rules::Compiled::load_builtins()?;
        let exit_code = runtime::run(reader, writer, child, rules, apply_colors)?;

        drop(guard);
        Ok(ExitCode::from(u8::try_from(exit_code & 0xff).unwrap_or(1)))
    }
```

Ve `runtime::run` zaten writer'ı input thread'e move ediyor; senin tek "shutdown sinyali" tüm `Tayf::run` return etmesi.

- [ ] **Step 2: Build + lint**

```bash
cargo build
cargo clippy --all-targets -- -D warnings
```

Expected: temiz.

- [ ] **Step 3: Commit**

```bash
git add src/lib.rs
git commit -m "feat(lib): Tayf::run facade orchestrates the v0.1 pipeline

Wires logging init, shell discovery, TtyGuard, PtySession spawn,
session decomposition, signal handler installation, rule compilation,
and the two-thread runtime, returning ExitCode for main() to surface.
The input thread is intentionally not joined; the OS reaps it on
process exit (spec §3.4 step 7 v0.1 limit)."
```

---

## Task 17: main.rs

`Tayf::run` çağrısı + Error → ExitCode mapping.

**Files:**
- Modify: `src/main.rs`

- [ ] **Step 1: Yenile**

```rust
//! tayf CLI entry. Parses args, runs the facade, and maps errors to exit codes.

use std::process::ExitCode;

use tayf::{Args, Error, Tayf};

fn main() -> ExitCode {
    let args = Args::parse_from_env();
    match Tayf::run(args) {
        Ok(code) => code,
        Err(err) => {
            eprintln!("tayf: {err}");
            ExitCode::from(map_error_to_exit_code(&err))
        }
    }
}

fn map_error_to_exit_code(err: &Error) -> u8 {
    match err {
        Error::ShellDiscovery(_) => 71,   // EX_OSERR
        Error::Pty(_) => 71,
        Error::Tty(_) => 71,
        Error::Signal(_) => 71,
        Error::RegexCompile(_) => 70,     // EX_SOFTWARE
        Error::BufferOverflow { .. } => 70,
    }
}
```

- [ ] **Step 2: Build + smoke**

```bash
cargo build --release
ls -lh target/release/tayf
./target/release/tayf --version
./target/release/tayf --help
```

Expected:
- Binary mevcut, < 5 MB (strip ile)
- `--version` → `tayf 0.1.0 (sha <hash>, rustc 1.X.Y)` formatında
- `--help` → clap-generated yardım

- [ ] **Step 3: Commit**

```bash
git add src/main.rs
git commit -m "feat(main): wire CLI to Tayf::run with exit-code mapping

main parses args, invokes the facade, prints any error to stderr,
and returns ExitCode mapped per BSD sysexits: 71 EX_OSERR for I/O
and signal failures, 70 EX_SOFTWARE for logic failures."
```

---

## Task 18: Manual Smoke Test (Yerel doğrulama)

Yerinde test — gerçek terminal. Otomatik integration test'ler Task 19'da.

Bu görevde commit yok; sadece doğrulama.

- [ ] **Step 1: Interaktif shell smoke**

```bash
./target/release/tayf
```

Beklenen:
- Mevcut shell'in açıldığı görülür (prompt görünür)
- `echo 192.168.1.1` → IPv4 sarı kalın
- `ls /etc | head -5` → renksiz (FQDN/etc match yok)
- `journalctl -n 20 --no-pager` → log seviyeleri parlak kırmızı
- `ping -c 2 8.8.8.8` → 8.8.8.8 IPv4 boyalı, süre metrikleri yeşil
- `cat /etc/hosts` → hem IPv4 hem FQDN/hostname'ler renklenir
- `exit` → tayf kapanır; terminal kullanılabilir durumda (termios restore doğru)

- [ ] **Step 2: Alt-screen smoke**

```bash
./target/release/tayf
# içeride:
vim /tmp/test.txt
# vim'de yaz, esc, :wq
less /etc/services
# q ile çık
htop
# q ile çık
exit
```

Beklenen: vim/less/htop bozulmaz; çıkıştan sonra prompt temiz.

- [ ] **Step 3: Exit code propagation**

```bash
./target/release/tayf
# içeride:
exit 42
echo $?
```

Beklenen: `42`.

- [ ] **Step 4: SIGWINCH**

`tayf` açıkken terminal penceresini yeniden boyutlandır → açık programlar (örn. `htop` ya da `top`) doğru boyuta uyar.

- [ ] **Step 5: SIGINT forwarding**

```bash
./target/release/tayf
# içeride:
ping 8.8.8.8
# Ctrl-C
```

Beklenen: ping durur, prompt geri döner; tayf hayatta kalır.

- [ ] **Step 6: Pipe redirect (no-color)**

```bash
./target/release/tayf | cat > /tmp/tayf-pipe-out
# tayf içinde:
echo 1.2.3.4
exit
cat /tmp/tayf-pipe-out
```

Beklenen: `/tmp/tayf-pipe-out` içinde `1.2.3.4` görünür AMA SGR escape yok (`grep -P '\x1b\[' /tmp/tayf-pipe-out` boş).

- [ ] **Step 7: Termios crash test**

```bash
./target/release/tayf
# içeride:
kill -SEGV $$
```

Beklenen: tayf çöker, ama terminal hâlâ kullanılabilir. (Eğer çalışmazsa `reset` çalıştır.)

- [ ] **Step 8: Sonuçları dökümante et**

`docs/superpowers/specs/2026-05-21-tayf-v0.1-design.md` §8 ("Done" listesi) içinde manuel doğrulama checkbox'larını işaretle.

---

## Task 19: Integration Smoke Tests

`tests/common/mod.rs` ile yardımcı fixture; `tests/integration_smoke.rs` ile birkaç end-to-end smoke.

**Files:**
- Create: `tests/common/mod.rs`
- Create: `tests/integration_smoke.rs`

- [ ] **Step 1: `tests/common/mod.rs`**

```rust
//! Shared fixtures for integration tests.

use std::time::Duration;

use portable_pty::{native_pty_system, CommandBuilder, PtySize};

/// Spawn `cmd` inside a PTY and return its stdout bytes once the process
/// exits or `timeout` elapses.
pub fn spawn_capture(cmd: &str, args: &[&str], timeout: Duration) -> Vec<u8> {
    use std::io::Read;
    let pty_system = native_pty_system();
    let pair = pty_system
        .openpty(PtySize { rows: 24, cols: 80, pixel_width: 0, pixel_height: 0 })
        .expect("openpty");
    let mut builder = CommandBuilder::new(cmd);
    for a in args {
        builder.arg(a);
    }
    let mut child = pair.slave.spawn_command(builder).expect("spawn");
    drop(pair.slave);

    let mut reader = pair.master.try_clone_reader().expect("clone reader");
    let mut out = Vec::new();
    let start = std::time::Instant::now();
    let mut buf = [0u8; 4096];
    while start.elapsed() < timeout {
        match reader.read(&mut buf) {
            Ok(0) => break,
            Ok(n) => out.extend_from_slice(&buf[..n]),
            Err(_) => break,
        }
    }
    let _ = child.kill();
    out
}
```

- [ ] **Step 2: `tests/integration_smoke.rs`**

```rust
//! End-to-end smoke tests for the tayf binary.
//!
//! These exercise the spawn → output → exit path with real PTY allocation.
//! Run with `cargo test --test integration_smoke`.

mod common;

use std::time::Duration;

use common::spawn_capture;

fn tayf_bin() -> &'static str {
    env!("CARGO_BIN_EXE_tayf")
}

#[test]
fn version_flag_prints_banner() {
    let out = spawn_capture(tayf_bin(), &["--version"], Duration::from_secs(5));
    let s = String::from_utf8_lossy(&out);
    assert!(s.contains("tayf"), "missing tayf in: {s}");
    assert!(s.contains("rustc"), "missing rustc in: {s}");
}

#[test]
fn help_flag_prints_usage() {
    let out = spawn_capture(tayf_bin(), &["--help"], Duration::from_secs(5));
    let s = String::from_utf8_lossy(&out);
    assert!(s.contains("Usage:") || s.contains("USAGE:"), "missing usage: {s}");
    assert!(s.contains("--shell"));
    assert!(s.contains("--login"));
    assert!(s.contains("--no-color"));
}

#[test]
#[ignore = "requires interactive PTY; run manually"]
fn echo_ipv4_gets_colorized() {
    // Manual: ./target/release/tayf -c 'echo 192.168.1.1' style; we keep this
    // as a documentation test for v0.2 once --command flag exists.
}
```

- [ ] **Step 3: Run**

```bash
cargo test --test integration_smoke
```

Expected: 2 test pass (3. ignored).

- [ ] **Step 4: Commit**

```bash
git add tests/
git commit -m "test: smoke tests for --version and --help via PTY fixture

tests/common spawns the tayf binary inside a freshly allocated PTY
using portable-pty so the binary sees a real tty and version/help
output reaches us. The interactive echo-and-assert test is ignored
until a non-interactive --command flag lands in v0.2."
```

---

## Task 20: Documentation Finalization

`README.md` ve `CHANGELOG.md`'i v0.1 release-ready hale getir.

**Files:**
- Modify: `README.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: `README.md` doldur**

```markdown
# tayf

Terminal-agnostic, PTY-based, regex-driven output colorizer in Rust.

`tayf` wraps your shell inside a pseudo-terminal and applies a small set of
regular expressions to the byte stream, so common patterns (IP addresses,
log levels, HTTP status codes, durations, FQDNs) appear colorized — in any
terminal emulator, with any shell, with no aliases or per-command wrapping.

> **Status:** v0.1 is the working skeleton. Use it for casual sessions; expect
> rough edges around interactive programs that aren't full-screen. Full ANSI
> awareness, configuration via TOML, and the OSC 133 / shell-integration
> story land in v0.2 → v0.4.

## Install (from source)

```bash
git clone https://github.com/beraartuc/tayf
cd tayf
cargo install --path .
```

## Use

```bash
# Replace your shell:
tayf

# Or launch it from a terminal-emulator config (e.g. Kitty):
# shell tayf
```

`tayf` automatically discovers your shell from `$SHELL`, falling back to
`/etc/passwd` and then `/bin/sh`. Override with `--shell /path/to/shell` or
request a login shell with `--login`.

When stdout is not a TTY (e.g. `tayf | tee log.txt`), colorization is
disabled automatically.

## Built-in rules (v0.1)

| Name        | Color           | Example                       |
|-------------|-----------------|-------------------------------|
| IPv4        | bold yellow     | `192.168.1.1`                 |
| IPv6        | bright yellow   | `fe80::1`, `2001:db8::1`      |
| MAC         | cyan            | `aa:bb:cc:dd:ee:ff`           |
| Log level   | bold bright-red | `ERROR`, `WARN`, `INFO`, ...  |
| HTTP status | magenta         | ` 200 `, `/404`, `:500`       |
| FQDN        | blue            | `api.example.com`             |
| Duration    | green           | `20.291 ms`, `1.5s`, `100ms`  |

Configuration via TOML and user-defined patterns are slated for v0.2.

## Known v0.1 limits

- **Alt-screen passthrough only.** Full-screen programs (vim, less, htop)
  receive bytes unmodified — they look correct, but tayf does not color
  their output. Full ANSI-aware colorization arrives in v0.3.
- **No capture-group colorization.** Each match is wrapped in one style;
  per-group styling lands in v0.5.
- **First match wins.** Overlapping rule matches resolve by definition order.
- **Partial-line prompts are not colorized.** Interactive prompts that lack
  a trailing newline within 50ms are flushed without rule application;
  this is fixed in v0.2 when the tick-flush goes live.
- **Linux + Unix + macOS only.** Windows support is post-v1.0.

## Diagnostics

Set `TAYF_LOG=debug` to send diagnostic logs to stderr.

## Security posture

`tayf` sits in the I/O path of every command in your shell. Its built-in
patterns are hand-tuned to be linear-time (no catastrophic backtracking),
its line buffer is hard-capped at 64KB, and its only ANSI emission is
through a single audited SGR sequence. The full threat model is in
[`CLAUDE.md`](./CLAUDE.md) §3.

If you find a security issue, please open an issue with the label
`security`, or email the maintainer if it requires private disclosure.

## License

Dual-licensed under either of:

- [Apache License, Version 2.0](./LICENSE-APACHE)
- [MIT license](./LICENSE-MIT)

at your option.

## See also

- [`tayf-tasarim.md`](./tayf-tasarim.md) — full design (Turkish)
- [`docs/superpowers/specs/`](./docs/superpowers/specs/) — versioned design specs
```

- [ ] **Step 2: `CHANGELOG.md` doldur**

```markdown
# Changelog

All notable changes to this project are documented here. The format is based on
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this project
adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.1.0] — 2026-05-21

### Added

- PTY-based shell wrapper that spawns the user's shell inside a pseudo-terminal
  and pipes output through a regex-driven rule engine.
- Seven built-in patterns: IPv4, IPv6, MAC, log level, HTTP status, FQDN,
  duration metric.
- Shell discovery cascade: `$SHELL` → `/etc/passwd` (`getpwuid`) → `/bin/sh`,
  with `--shell <path>` and `--login` overrides.
- CLI flags: `--shell`, `--login`, `--no-color`, `--help`, `--version`.
- Build-time SHA and rustc version surfaced via `--version`.
- `TAYF_LOG` environment variable activates `tracing-subscriber` diagnostics on
  stderr (off by default).
- 5-state machine for alt-screen detection (`\x1b[?1049h` and legacy variants);
  the rule engine bypasses bytes while alt-screen mode is active so full-screen
  programs render correctly.
- 64 KB hard-capped, UTF-8-safe line buffer.
- Raw-mode termios RAII guard with panic-hook fallback so the terminal is
  restored on any exit path.
- Signal handling thread (signal_hook): `SIGWINCH` resizes the PTY, `SIGINT`
  and `SIGTERM` are forwarded to the child process group with `killpg`.
- Exit-code propagation from the child shell; OS-error and software-error
  codes per BSD sysexits when tayf itself fails.
- Automatic colorization bypass when stdout is not a TTY.

### Security

- All built-in patterns are linear-time (verified by inspection); no
  catastrophic backtracking.
- Line buffer is hard-capped to bound memory under adversarial output.
- ANSI emission is restricted to SGR sequences by a single audited function
  (`Style::to_sgr`) with a unit test asserting the output grammar.
- Shell is spawned via `argv`-style `CommandBuilder`, never via `sh -c`.

[0.1.0]: https://github.com/beraartuc/tayf/releases/tag/v0.1.0
```

- [ ] **Step 3: Commit**

```bash
git add README.md CHANGELOG.md
git commit -m "docs: finalize v0.1 README and CHANGELOG

README explains install, use, the seven built-in rules, the
documented v0.1 limits (alt-screen passthrough, no capture groups,
first-match-wins, partial-line prompts), the TAYF_LOG diagnostic
toggle, and the security posture pointer to CLAUDE.md §3.

CHANGELOG records the 0.1.0 feature set under Added and the
security posture under Security per Keep a Changelog."
```

---

## Self-Review — Spec Coverage Map

Plan'ı yazdıktan sonra spec §2 (scope) ile karşılaştır:

| Spec scope item | Hangi task'ta? |
|---|---|
| PTY açma + iki yönlü byte kopya | T13, T15 |
| Stdin raw mode + Drop guard | T12 |
| SIGWINCH → resize | T14 |
| SIGINT/SIGTERM → child PGID | T14 |
| Exit code propagation | T13 (ChildHandle), T17 (main mapping) |
| 7 hardcoded pattern | T4 |
| Shell discovery cascade | T7 |
| CLI flag'leri | T11 |
| isatty bypass | T8, T16 (Tayf::run apply_colors) |
| Alt-screen detection (state machine) | T6 |
| TAYF_LOG tracing | T9 |
| --version build info | T10 |

| Spec §3.4 Shutdown protocol step | Hangi task'ta? |
|---|---|
| 1. Child exit | T13 |
| 2. Output thread read returns EIO/Ok(0) | T15 |
| 3. Pipeline drain | T6, T15 |
| 4. Output thread exit | T15 |
| 5. Main thread wait | T16 |
| 6. master_writer.shutdown | T15 (input thread'in writer drop'u) |
| 7. Input thread exit | T15 |
| 8. SignalGuard drop | T14, T16 |
| 9. TtyGuard drop | T12, T16 |
| 10. main returns ExitCode | T17 |

Tüm spec §3.4 adımları planlandı. Boşluk yok.

| Spec güvenlik (§6) tehdit | Hangi task'ta? |
|---|---|
| ReDoS | T4 (hand-tuned patterns) |
| Memory exhaustion | T5 (64KB cap), T6 (pipeline overflow log) |
| Escape injection | T3 (SGR audit gate) |
| Terminal corruption | T12 (panic hook), T17 (no process::exit) |
| Shell injection | T13 (argv-style spawn) |
| Signal forwarding | T14 (killpg) |
| Credential leak | T9 (off-by-default tracing) |

Spec §8 "Done" tanımına maps eder:
- `cargo build --release` ve `cargo fmt --check && cargo clippy -- -D warnings && cargo test`: Her task'ın son adımı.
- Unit + smoke testler: T2-T11, T19.
- Manuel doğrulama: T18.
- README + CHANGELOG: T20.

Spec'in kapsamadığı bir öğe yok.

---

## Plan'ın bilinen sınırları (ilerideki iterasyonlarda iyileştirilecek)

- **`pty.rs` ve `signals.rs`'te `unsafe ioctl` blokları var.** Tek bir `pty/winsize.rs` helper modülüne hapsetmek temizdir; v0.2'de refactor edilir.
- **`pipeline.rs`'te 50ms tick flush v0.1'de devre dışı.** Output thread'i tek bir nonblocking read loop'una çevirmek gerekir; v0.2.
- **Input thread'in stdin read'inden uyanması için poll/self-pipe yok.** v0.1 limit, spec §3.4 step 7.
- **`benches/throughput.rs` v0.1 planında yok.** Spec §8'de mevcut ama task listesine alınmadı çünkü CI'yle birlikte v0.2'de gelecek (kullanıcı kararı). README'de v0.2 milestone'una yazılacak.

---

_Plan v0.1 kapsamındaki her şeyi 20 task'a böler. Her task TDD adımlarını içerir, her dosyanın amacı CLAUDE.md ve spec doc'una atıfta bulunur._
