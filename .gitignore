/target
/fuzz/target
**/*.rs.bk
Cargo.lock.bak
.DS_Store
*.profraw

# Claude Code session state (per-developer; not source-controlled)
.claude/

# Superpowers brainstorming visual companion (mockups + server state)
.superpowers/

# Fuzzer-discovered corpus inputs are non-deterministic generated artifacts;
# track only the committed seed-* files (see fuzz/corpus/*/seed-*).
/fuzz/corpus/*/*
!/fuzz/corpus/*/seed-*
